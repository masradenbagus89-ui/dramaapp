<!-- LINTAS:SEKSI §4.14 -->

## 4.14. Paket Stack (Stack Packs) — checklist profesional per-teknologi (DETAIL on-demand)

> Menutup gap "review per-bahasa/stack". Onderdil dari ECC v2.0.0 (MIT) `rules/` + `agents/*-reviewer` + skill `postgres-patterns`/`deployment-patterns`/`seo` + standar OWASP/WCAG — **ditulis-ulang bahasa non-programmer, BUKAN disalin**. Detail on-demand.

### Cara kerja (OTOMATIS, tanpa staff mengetik apa pun)
Saat AI menyentuh berkas di stack tertentu, AI **auto-deteksi** dari `package.json`/config (mis. `next` → Next.js; `@supabase/*` → Supabase; `wrangler.toml` → Cloudflare Workers; `vercel.json`/`render.yaml` → deploy; `requirements.txt`/`pyproject.toml`/`*.py` → Python; `composer.json`/`artisan` → PHP/Laravel; `go.mod` → Go) lalu **terapkan paket stack yang cocok DI ATAS baseline 8 divisi §4.13** — staff cukup prompt biasa. Tiap paket = standar profesional minimum (lantai), bukan opsi. Stack di luar daftar → pakai baseline §4.13 + (kalau client punya keahlian) bungkus skill kustom §4.9.

> **Next.js/React punya 2 berkas** (induk penuh, dipecah §4.18): `4.14-1-nextjs.md` (Server/Client, performa, a11y) + `4.14-1b-frontend-lanjutan.md` (tes komponen RTL, race `useEffect`, Motion/animasi) — dibaca bersama saat stack React/Next terdeteksi.

> **Batas jujur (jangan over-claim):** kedalaman **"kelas-expert"** = checklist pra-jadi yang dalam — berlaku PENUH hanya untuk stack yang **punya Paket di §4.14** (Next.js/React, Supabase/Postgres+Prisma, Cloudflare, Vercel/Railway/Render, Python, PHP/Laravel, Go). Stack di luar daftar (mis. Java/Spring, Ruby/Rails, .NET, Vue/Nuxt, Angular, React Native/Flutter) tetap dapat **baseline 8 divisi profesional + konvensi resmi stack itu** — dan AI **WAJIB cek dokumentasi resmi versi terpasang** (§8.2 Aturan 1), bukan ingatan. Jadi mereka **tidak tanpa standar** — hanya tanpa checklist sedalam stack inti. Kalau target pasar memakai stack tertentu yang sering, owner bisa minta tambah Paket Stack baru / bungkus skill kustom §4.9.

### Robot pendamping (opsional, on-demand) — menjadikan paket ini BISA-DIJALANKAN
Checklist di bawah = PRINSIP (AI terapkan saat menulis/menilai kode). Untuk **benar-benar menjalankan alat-cek** per-bahasa ada robot deterministik: `npx lintasai stack-check run --repo-root .` (atau `node .claude-kit/lib/stack-check.mjs run --repo-root .`; di repo kit: `node lib/stack-check.mjs run --repo-root .`). Robot auto-deteksi bahasa (`getStackType`) lalu jalankan alat-cek **STATIS standar** — Go: `go vet`/`staticcheck`/`govulncheck`; Python: `ruff`/`mypy`/`bandit`; Node-TS: `tsc --noEmit`/`eslint`/`npm audit`; Rust: `cargo clippy`/`cargo fmt --check`; PHP: `phpstan`/`pint --test`. Sifat (pola `ai-config-check`): **(a)** robot kasih FAKTA (alat, kode-keluar, cuplikan), **AI menafsir + terjemah** ke bahasa awam + naikkan ke GENTING kalau keamanan; **(b) cuma-periksa** — TIDAK auto-fix, TIDAK jalankan TES (tes = §4.15-B); **(c) config-gated** anti alarm-palsu (mis. `tsc` hanya kalau ada `tsconfig.json`); **(d)** alat belum terpasang → **"DILEWATI"**, bukan diam-diam "0 masalah" (§6.3 #4). Gerbang §4.6 TIDAK hard-fail dari robot ini (mutu kode = saran owner-gated). Dikunci `tests/stack-check.test.mjs`. Detail: docstring `lib/stack-check.mjs`.

> **Anti-halusinasi (WAJIB tiap paket):** framework/library berubah cepat antar-versi. Paket ini = PRINSIP yang stabil; untuk detail API/fungsi versi-terpasang, AI WAJIB cek dokumentasi resmi dulu (§8.2 Aturan 1) — jangan andalkan ingatan.


> **Kredit (MIT):** adaptasi `rules/` + `agents/*-reviewer` + skill `postgres-patterns`/`deployment-patterns`/`seo`/`laravel-*`/`golang-*` ECC v2.0.0 (MIT © Affaan Mustafa; pola Postgres kredit tim Supabase) + standar OWASP/WCAG (W3C). Ditulis ulang bahasa non-programmer, bukan menyalin.
