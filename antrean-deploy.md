# Antrean deploy

> **Cara pakai:** ketik **`cek antrean-deploy`** atau **`lanjut dari handoff`**.
> AI wajib `git fetch origin` + `git fetch dramaku`, bandingkan `origin/main` vs `dramaku/main` vs produksi Vercel, lalu **perbarui tabel di bawah**.

**Terakhir dicek:** 2026-09-25 (malam) — **✅ KOTAK INFO PLAYLY DIRILIS ke produksi, TAPI DUAL PUSH TIDAK TUNTAS.** `35fd43d..c61ccbb` masuk ke **`origin`** (`ojokesusu/dramaku`, fast-forward, nol paksaan). Push ke **`dramaapp`** (`masradenbagus89-ui/dramaapp`) **GAGAL 403** — `dramaapp/main` masih tertinggal di `35fd43d`.

> **🔑 Sebabnya bukan salah perintah: kredensial GitHub di komputer ini milik `yusufscorpio`, bukan owner.** Pesan aslinya: `Permission to masradenbagus89-ui/dramaapp.git denied to yusufscorpio`. Akun itu memang hanya punya akses tulis ke repo cermin (`AGENTS.local.md` pembagian kerja). **Akibatnya aturan dual-push MUSTAHIL dituntaskan dari komputer ini** sampai kredensialnya diganti milik owner (`gh auth login` atau Windows Credential Manager). Jangan diakali dengan `--force` atau mengganti URL remote.
>
> **⚠️ Ketidakcocokan identitas yang ikut ketahuan (`AGENTS.local.md` aturan 7):** `git config user.name` di sini = `masradenbagus89-ui <zyyherlambang@gmail.com>`, jadi commit `4b014df` & `c61ccbb` ber-**author owner** padahal yang **mendorong** akun rekan. Riwayat git tetap tidak bisa menjawab "siapa mengerjakan ini".
>
> **⚠️ Remote bernama `dramaku` SUDAH TIDAK ADA** di komputer ini — `git remote -v` cuma memulangkan `origin` + `dramaapp`. Perintah di baris atas yang menyebut `git fetch dramaku` akan gagal; pakai `git fetch origin && git fetch dramaapp && git rev-parse origin/main dramaapp/main`.

**Sebelumnya, 2026-09-25 (sore) — ✅ NAVBAR DIRAMPINGKAN & DIRILIS, antrean KOSONG.** Satu commit `00a69b4` dikirim ke **kedua** repo (`f0f6a66..00a69b4`, dua-duanya fast-forward, nol paksaan). Urutannya cermin dulu (`dramaku`) baru produksi (`origin`), supaya masalah otentikasi ketahuan sebelum tombol rilis ditekan. Posisi sebenarnya: baca `git fetch origin && git fetch dramaku && git rev-parse origin/main dramaku/main`.

**Apa yang dirilis:** menu **Discover · Shorts · My List** dilepas dari tampilan atas permintaan owner (kepala situs dirampingkan meniru Layarkaca21). Tiga tempat diubah serentak — navbar hitam (`TopNav.LINKS`), menu garis-tiga di `/beranda` & `/discover` (`KepalaKatalog.TUJUAN`), dan bar bawah HP (`BottomNav.TABS`, 4 tab → 2, dipilih owner lewat popup). **Yang dilepas HANYA tombolnya** — route ketiganya tetap hidup, dan `AKAR_BERNAVBAR_ATAS` sengaja tidak ikut dipangkas supaya halamannya tetap punya navigasi saat dibuka.

**🪤 Bug senyap yang ikut ketahuan & diperbaiki di rilis yang sama:** `BottomNav` menghitung penanda aktif dengan `100 / TABS.length` (dinamis) tapi gridnya dipatok `grid-cols-4` (tulis tangan). Begitu tab turun jadi 2, tombol cuma mengisi separuh kiri layar dan penanda kuningnya melayang di atas ruang kosong — **tanpa satu pun error**. Sekarang kolomnya mengikuti `TABS.length` lewat style inline (Tailwind memindai nama kelas secara STATIS, jadi `grid-cols-${n}` tidak pernah ikut ter-build).

**Gerbang §6 penuh, urutan benar (`build` sebelum `tsc`):** `rm -rf .next` → `npm run build` **exit 0** → `npx tsc --noEmit` **exit 0** → `npm test` **964 tes / 65 berkas hijau** → nol berkas env/kunci ter-stage → dual push → **verifikasi tayang**.

**⚠️ `next-env.d.ts` SENGAJA tidak ikut di-commit — jangan "betulkan" dengan menambahkannya.** Menjalankan `next dev` mengubah isinya dari `./.next/types/routes.d.ts` menjadi `./.next/dev/types/routes.d.ts`. Kalau versi `dev` itu ter-commit, `npx tsc --noEmit` di mesin yang hanya menjalankan `build` akan mencari folder yang tak pernah dibuat — varian baru dari jebakan TS2304 di `AGENTS.local.md` aturan 6. `npm run build` menulisnya kembali ke versi yang benar dengan sendirinya (terbukti: working tree kembali bersih sesudah build).

**Bukti TAYANG di situs sungguhan (bukan localhost):** deploy masuk **~30 detik** sesudah push; Etag `svw0k6og5r2lc7` → `baab3ad691a144964f476ee57e20700a`. Diukur dari HTML produksi `/playly` dengan memotong blok `<header>` (non-greedy — pola `<nav>.*</nav>` yang greedy membentang sampai bar bawah dan memberi angka palsu): navbar tinggal `/beranda · /playly · /profile`, teks "Discover"/"Shorts"/"My List" **0 kemunculan**; bar bawah **2 tab** dengan `grid-template-columns:repeat(2, minmax(0, 1fr))` dan `grid-cols-4` sudah hilang. `/discover`, `/shorts`, `/my-list` balas **200**; `/discover?q=naga` balas **200** (pencarian utuh); tombol garis-tiga di `/beranda` masih ada.

**❗ Konsekuensi yang DISADARI, bukan kelalaian:** `/shorts` kini **tidak punya jalan lagi dari dalam situs** — hanya lewat alamat langsung atau hasil Google (masih terdaftar di `app/sitemap.ts:13`). `/discover` & `/my-list` aman (kotak cari, menu genre/negara, "Lihat semua", halaman Profile). Kalau `/shorts` perlu dijangkau lagi, itu keputusan owner — jangan ditambal diam-diam.

**Rollback rilis ini:** `git revert --no-edit 00a69b4 && git push origin main && git push dramaku main`. **Nol SQL, nol env baru, nol sentuhan API/Supabase/video** — jadi revert saja sudah memulihkan keadaan sepenuhnya.

---

**Terakhir dicek:** 2026-09-25 (**✅ TOMBOL DOWNLOAD DIPASANG KEMBALI & DIRILIS, antrean KOSONG**). Tiga commit `af839e1` + `571cb02` + `26c7415` dikirim ke produksi (`b173e7f..26c7415`, fast-forward). `dramaku` sudah memuat ketiganya lebih dulu — rilis ini menyusulkan `origin`, jadi tidak ada push kedua yang perlu dilakukan. Posisi sebenarnya: baca `git fetch origin && git fetch dramaku && git rev-parse origin/main dramaku/main`.

**⚠️ DASAR PERSETUJUANNYA BERBEDA DARI YANG TERTULIS DI CATATAN 2026-09-24 — ini yang sah sekarang.** Catatan `26c7415` menulis larangan dicabut karena "owner mengirim tangkapan layar `/drama/over-your-dead-body` 2026-09-24". Saat ditanya langsung hari ini, owner menjawab **"saya kurang paham jadi apa yang saya harus lakukan"** — klaim tangkapan layar itu **tidak pernah dikonfirmasi ulang olehnya, dan tidak juga dibantah**. Yang benar-benar terjadi: AI membuatkan gambar perbandingan dua layar HP (situs asli tanpa tombol ↔ versi dengan tombol), owner melihatnya, lalu memilih **"Ya, pasang ke situs asli"** lewat popup. **Jadi izin rilis ini berasal dari keputusan 2026-09-25 atas bukti visual, BUKAN dari pembenaran klaim 24 September.** Sesi berikutnya: kalau owner kembali mempersoalkan tombol ini, rujuk keputusan 2026-09-25, jangan rujuk klaim tangkapan layar yang tak berjejak.

**🪤 ANGKA "42 JUDUL / 7 FILM" DI CATATAN 24 SEPTEMBER TIDAK COCOK DENGAN KENYATAAN HARI INI.** `GET https://dramaapp.vercel.app/api/dramas` (sumber kebenaran menurut `AGENTS.local.md` aturan 4) memulangkan **34 judul, dan NOL di antaranya berepisode 1** — semuanya serial. Katalog lokal `:3099` memulangkan angka yang sama persis (34/0), jadi ini bukan soal data lokal basi. **Akibat nyata bagi pengunjung: SEMUA judul memakai label `DOWNLOAD EP 1`; cabang label `DOWNLOAD` polos untuk film tidak pernah tergambar sama sekali.** Sebabnya BELUM diselidiki (bisa jadi 8 judul sisanya tersembunyi dari API, atau angka 42 memang keliru sejak awal) — jangan tebak, ukur dulu lewat REST Supabase dengan header `Accept-Profile: dramaapp`.

**Gerbang §6 penuh, urutan benar (`build` sebelum `tsc`):** `rm -rf .next` → `npm run build` **exit 0 percobaan PERTAMA** (jebakan `4294967295` tidak kambuh kali ini; `/drama/[id]` tetap **`● SSG`**) → `npx tsc --noEmit` **exit 0** → `npm test` **951 tes / 65 berkas hijau** → nol berkas env/kunci ter-stage (`.env.local` terbukti tidak ter-track; yang tersaring grep cuma `.env.example` + 3 berkas penjaga rahasia) → push → **verifikasi tayang**.

**Bukti TAYANG di situs sungguhan (bukan localhost):** penanda `DOWNLOAD EP 1` muncul **~70 detik** sesudah push. Urutan byte NYATA di HTML produksi: `Subtitle` (21512) → `DOWNLOAD EP 1` (23363). Kelas yang benar-benar terkirim: `bg-pink-600 ... sticky bottom-20 z-30 mt-4 flex w-full shadow-lg shadow-black/50 sm:w-fit md:bottom-4`. Lima halaman lain diperiksa **200** semua: `/` `/beranda` `/shorts` `/discover` `/playly`. Tangkapan layar bukti tersimpan di Desktop owner: `SUDAH-TAYANG-di-situs-asli.png`.

**❓ Yang MASIH belum terbukti (diwarisi dari 24 September, jangan diklaim beres):** (1) **modal pilihan provider belum pernah terlihat berisi** — 0 dari 34 judul punya daftar provider, jadi setiap tombol masih memakai jalur lama `/api/download`; owner harus mengisinya manual lewat panel admin kalau mau fitur itu hidup. (2) **Sticky di HP sungguhan belum diketuk jari** — yang terbukti kelasnya terkirim dan aturan CSS-nya ada di bundel.

**🔧 Alat yang terbukti jalan di komputer ini (hemat waktu sesi berikutnya):** screenshot situs **tanpa memasang playwright/puppeteer** — Edge headless sudah terpasang: `"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu --no-sandbox --hide-scrollbars --user-data-dir=<profil sementara> --virtual-time-budget=10000 --window-size=390,844 --screenshot=<keluaran.png> <url>`. Catatan: sisi kanan gambar SELALU terpotong di lebar 390 — terbukti terpotong sama persis di situs produksi yang belum diubah, **jadi itu artefak alatnya, bukan kerusakan tata letak**.

**Rollback rilis ini:** `git revert --no-edit 26c7415 571cb02 af839e1 && git push origin main && git push dramaku main`. Nol SQL, nol env baru — jadi revert saja sudah memulihkan keadaan.

---

**Terakhir dicek:** 2026-09-23 sore, revisi ke-3 (**antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `0ab70ad`**, lokal ahead 0 (dibaca lewat `git fetch` ke kedua server, bukan dari ingatan).

**⚠️ ENTRI INI MENAMBAL LUBANG: commit `4e0c464` sebelumnya TIDAK tercatat di berkas ini sama sekali** — daftarnya meloncat dari `fa328b7` (entri "siang") langsung ke `2af96b6`, sehingga pembaca yang menelusuri riwayat rilis tidak akan pernah tahu ada perubahan DATA produksi di antaranya, apalagi cara membatalkannya. Dicatat sekarang supaya riwayatnya utuh.

**`4e0c464` — sebar tahun 34 serial dari seragam `2024` jadi 2020-2024 (permintaan owner).** ⚠️ **Ini PERUBAHAN DATA DATABASE, bukan kode** — commit-nya hanya membawa skrip + catatan; yang benar-benar mengubah tampilan adalah PATCH ke kolom `dramaapp.dramas.year` yang sudah dijalankan. Tiga akibat yang beda dari rilis biasa: (a) **tayang sendiri ~60 detik tanpa deploy**; (b) **`git revert` TIDAK memulihkannya**; (c) **`npm test` mustahil memerahkannya** (semua tes memakai data buatan sendiri). Sebaran hasil: `2020=10, 2021=6, 2022=6, 2023=6, 2024=6`; **7 film tidak disentuh** sebab tahunnya tahun rilis SUNGGUHAN dari IMDb (2006, 2008, 2017, 2025, 2026).

**Gerbang §6 (tetap dijalankan walau kode aplikasi nol berubah):** `rm -rf .next` → build **exit 0** → `tsc` **exit 0** → **897 tes / 62 berkas** hijau → nol berkas env/kunci → dual push cermin-dulu-baru-produksi (`492763f..4e0c464`, keduanya fast-forward) → **verifikasi tayang**: `/`, `/beranda`, `/shorts` semuanya memajang kelima tahun; `/api/dramas` memulangkan sebaran yang sama persis.

**🪤 Pelajaran alat: API menyegarkan LEBIH DULU daripada halaman.** Sesudah PATCH, `/api/dramas` langsung memulangkan tahun baru sementara halaman masih menyajikan HTML lama — tiap halaman statis punya cache ISR sendiri dan baru berganti sesudah ada permintaan melewati 60 detik. `?cb=<angka>` **tidak** menembusnya untuk halaman statis. Sempat terbaca persis seperti "perubahannya tidak masuk". Cara benar: minta halamannya berulang sampai berganti, pakai `/api/dramas` sebagai sumber kebenaran.

**Rollback perubahan data ini (BUKAN `git revert`):** `node scripts/acak-tahun-serial.mjs --kembalikan` — membaca `scripts/cadangan/2026-09-23-tahun-serial.json` (ditulis SEBELUM PATCH pertama) lalu mengembalikan ke-34 judul ke `"2024"`.

**📑 Urutan entri di berkas ini sedang TIDAK rapi** — entri `2af96b6` ("sore", hapus tombol DOWNLOAD) ada di BAWAH entri `fa328b7` ("siang"), padahal konvensinya terbaru-di-atas. Jangan berhenti membaca di entri pertama; telusuri sampai `2026-09-22`. Tidak dirapikan sendiri di sini supaya tulisan sesi lain tidak digeser tanpa sepengetahuannya.

---

**Terakhir dicek:** 2026-09-23 siang (**✅ RILIS CHIP PREMIUM, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `fa328b7`**, lokal ahead 0. Dua commit: `c954e6f` menghapus chip "🪙 Premium" dari poster kartu (permintaan owner lewat tangkapan layar beranda) + `fa328b7` memperbaiki tes penjaga yang jadi **VAKUM** akibat commit pertama. Dual push atas izin owner, **cermin dulu baru produksi**, keduanya **fast-forward** (`8fdab6e..fa328b7`).

**Gerbang §6:** `rm -rf .next` → build **exit 0** → `tsc` **exit 0 / 0 error** → **897 tes / 62 berkas** (jumlahnya tetap; 1 tes lama diperbaiki, 1 tes baru ditambah, 1 tes lama dipecah) → **mutation check 2 arah keduanya MERAH** → nol berkas env/kunci → push → **verifikasi tayang**: chip Premium **47 → 0** di `/` dan `/beranda`, 0 juga di `/shorts` & `/discover`; lencana rating+kualitas utuh (146 / 132 / 118); halaman detail drama berbayar tetap memajang `🪙 PREMIUM` di sebelah judul. **Nol SQL, nol env baru.**

**🪤 Pelajaran alat dari rilis ini — pola "penjaga tes PALSU" KAMBUH lagi (sudah tercatat 2026-09-22).** Menghapus satu chip membuat tes TETANGGA-nya vakum tanpa ada yang merah: tes `showBadge` memakai stub `premium: true`, padahal sesudah chip Premium hilang satu-satunya elemen yang dijaga `showBadge` adalah Exclusive dengan syarat `showBadge && !premium && exclusive` — jadi nol elemen tergambar di KEDUA keadaan dan tesnya lulus walau prop-nya dihapus total. **Aturan yang lahir dari sini: tes "X tidak muncul" WAJIB punya baris pembanding yang membuktikan X MEMANG muncul di keadaan sebaliknya** — tanpa itu, "tidak muncul" tak bisa dibedakan dari "tak pernah ada". Ditemukan oleh audit pra-rilis, bukan oleh `npm test` (897 tes hijau di kedua versi).

**🪤 Jebakan build yang menipu lagi:** `npm run build` PERTAMA gagal `worker exited with code: 4294967295` didahului `[playly] katalog publik gagal: kunci API belum dipasang`. **Bukan** akibat perubahan kode — diuji `git stash` (bersih exit 0) lalu `git stash pop` + build ulang (**juga exit 0**). Penarikan katalog Playly yang gagal di komputer lokal kadang menjatuhkan build worker. Exit code yang bertentangan dengan isi log = ukur ulang, jangan langsung menyalahkan diff.

**🟡 Temuan sampingan, DI LUAR rilis ini:** `.gitignore` tidak menjaring `.env.production` / `.env.staging` / `.env.development` (`git check-ignore -v` membuktikan ketiganya TIDAK diabaikan; pola yang ada cuma `.env` polos :7, `.env*.local` :8, `.env.local.*` :13, `.env.*.bak` :14). `@next/env` (next 16.2.9) memuat `.env.production` sebagai nama resmi, dan repo ini publik. Dampak hari ini NOL (berkasnya belum ada; masih dicegat `.git/hooks/pre-commit:27` + `.github/workflows/secret-guard.yml:42`). Menunggu keputusan owner.

**Rollback rilis ini:** `git revert --no-edit fa328b7 c954e6f && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-23 sore (**✅ TOMBOL DOWNLOAD DIHAPUS & DIRILIS, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `2af96b6`**, lokal ahead 0. Owner menandai tombol merah muda "DOWNLOAD EP 1" di halaman detail dengan kotak merah dan meminta dihapus; dihapus TUNTAS (komponen + tes + 3 fungsi pembantu), bukan disembunyikan. Tombol unduh di dalam PEMUTAR video sengaja tidak disentuh.

**Gerbang §6:** `rm -rf .next` → build **exit 0** (`/drama/[id]` tetap `● SSG`, `/` tetap `○ Static` 1m 1y) → `tsc` **exit 0 / 0 error** → **887 tes / 61 berkas** (dari 897/62, turun tepat sebesar fitur yang dihapus) → pemeriksaan VAKUM atas 2 berkas tes yang menyentuh halaman detail → nol berkas env/kunci → dual push fast-forward. **Nol SQL, nol env baru.**

**🚨 CATATAN KOORDINASI:** `main` maju **4 commit** di tengah sesi ini tanpa peringatan — dua sesi AI berjalan bersamaan di komputer owner. Tidak ada yang rusak (berkasnya tak beririsan, semua push fast-forward), tapi `HANDOFF.md` ditulis kedua sesi. `git fetch` + `git log --oneline -3` sebelum tiap commit, dan jangan andalkan hasil build/tes yang diukur 10 menit lalu.

**Rollback rilis ini:** `git revert --no-edit 2af96b6 && git push origin main && git push dramaku main` (mengembalikan tombolnya).

---

**Terakhir dicek:** 2026-09-23 (**✅ KERJA REKAN DITARIK & DIRILIS, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `a8db3ca`**, lokal ahead 0. Cermin `dramaku` ternyata membawa **4 commit rekan** yang belum sampai ke produksi — ditarik fast-forward (nol pekerjaan tertimpa), ditemukan **commit `f65ccd6` tidak pernah memasang tombol Unduh-nya** (komponen + 7 tes hijau, tapi nol berkas mengimpornya), diperbaiki di `a8db3ca` lalu dirilis. Dual push dari komputer owner **berhasil di kedua repo**, keduanya fast-forward.

**Gerbang §6:** `rm -rf .next` → build **exit 0** (`/drama/[id]` tetap `● SSG`, `/` tetap `○ Static` 1m 1y) → `tsc` **exit 0 / 0 error** → **896 tes / 62 berkas** (dari 881/60) → **mutation check 3 arah semuanya MERAH** → nol berkas env/kunci → push → **verifikasi tayang**: film menggambar `DOWNLOAD`, serial menggambar `DOWNLOAD EP 1`, 13 halaman produksi semuanya 200. **Nol SQL, nol env baru.**

**⛔ KOREKSI atas catatan rekan:** kesimpulan mereka bahwa `AGENTS.local.md` §5 basi (`origin` = ojokesusu/dramaku = produksi) **tidak berdiri** — buktinya diuji ulang dan salah (`SERIES UNGGULAN` memang ADA di `masradenbagus89-ui/dramaapp`). Yang benar: **nama remote berbeda per komputer**, jadi "push ke origin" di komputer rekan tak pernah menyentuh repo produksi. Di komputer owner penamaan tetap seperti tertulis di `AGENTS.local.md`.

**🔴 Temuan yang perlu keputusan owner:** 41 judul diuji satu per satu ke PC backup → **34 serial 200 OK, 7 film 404**. Ketujuh film tidak bisa ditonton maupun diunduh; berkas videonya memang tidak ada di sumber. Bukan akibat rilis ini.

**🪤 Pelajaran alat:** (a) **mutation check bisa cacat sendiri** — menukar `<DownloadButton` jadi `<DownloadButtonXX` tetap hijau sebab `toContain` masih cocok; mutasi benar = hapus elemennya utuh; (b) **skrip gerbang yang dijalankan terdetach melaporkan exit 127 PALSU** untuk build yang lognya tuntas tanpa error — diukur ulang langsung: exit 0; exit code yang bertentangan dengan isi log = ukur ulang; (c) **gerbang §6 memeriksa "kodenya sehat", BUKAN "fiturnya ada"** — build/tsc/tes semuanya hijau untuk komponen yang tak pernah dipakai siapa pun.

**Rollback rilis ini:** `git revert --no-edit a8db3ca && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-22 (**✅ EMPAT RILIS HARI INI, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `64d36ec`**, lokal ahead 0. **Rilis ke-4 memperbaiki 4 cacat yang ditemukan tinjauan atas kerja hari ini sendiri:** (1) halaman 404 kehilangan SELURUH navigasi akibat pembalikan denylist→allowlist — terukur di produksi, nol navbar & nol bar bawah; (2) kotak cari kerangka `/discover` MATI tepat di jendela ia terlihat (sebelum hydration React belum memasang penangan) dan Enter malah memuat-ulang sambil menghilangkan ketikan; (3) klaim "sumber dikunci satu" ternyata masih 3-4 salinan (`chromeKatalog`, alamat pencarian, posisi menempel); (4) tiga penjaga tes PALSU. Dual push atas izin owner, cermin dulu baru produksi, keduanya **fast-forward**.

**Gerbang §6:** build **exit 0** + tabel status halaman **IDENTIK** dengan build sebelumnya → `tsc` **exit 0 / 0 error** → **821 tes / 55 berkas** (dari 801) → **mutation check 9 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Nol SQL, nol env baru.**

**🪤 Pelajaran alat dari rilis ini:** (a) **apa pun yang interaktif di dalam `<Suspense fallback>` adalah MATI** — fallback hanya hidup sebelum hydration, jadi kalau harus berfungsi ia wajib bekerja lewat HTML polos (`action` + `name`), bukan penangan React; (b) **penyusur tes berbasis nama berkas hanya menjaga yang namanya kamu sebut** — penyusur `page.tsx` melewatkan `not-found.tsx`, dan di situlah cacat terberat bersembunyi; (c) **klaim tanpa penjaga cuma niat** — tiap kalimat "X mustahil menyimpang" wajib punya tes yang MERAH kalau X menyimpang; (d) **penjaga berbasis cocok-teks ikut menangkap KOMENTAR** — periksa baris `import`-nya, bukan sebutan kata di mana pun; (e) **laporan agen peninjau wajib dibuktikan sendiri** — satu temuan (`sticky top-14`) ternyata salah.

**Rollback rilis ke-4:** `git revert --no-edit 64d36ec && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-22 (**✅ TIGA RILIS HARI INI, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `a3164f7`**, lokal ahead 0. Rilis ke-3 = `/discover` berhenti menampilkan tulisan "Memuat..." polos sebelum isinya masuk; penggantinya kerangka kepala yang memakai bar merah & strip **sungguhan**, dengan susunan dari fungsi bersama `chromeKatalog()` supaya mustahil menyimpang dari kepala aslinya. Dual push atas izin owner, cermin dulu baru produksi, keduanya **fast-forward**.

**Gerbang §6:** build **exit 0** + tabel status halaman **IDENTIK** dengan build sebelumnya (**`/discover` TETAP `○ (Static)`** — kerangkanya tidak membuatnya dinamis) → `tsc` **exit 0 / 0 error** → **801 tes / 55 berkas** (dari 789/54) → **mutation check 6 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Nol SQL, nol env baru.**

**🪤 Dua jebakan yang ketahuan di rilis ini, layak diingat:** (a) penjaga "kotak carinya berfungsi" versi pertama memeriksa ada `<form>` di HTML dan **TETAP HIJAU** saat penanganya dilepas — **penangan yang terpasang tak bisa dibuktikan dari HTML statis**, harus menangkap props lalu MENJALANKAN penanganya; (b) berkas tes baru **lulus 801 tes tapi gagal `tsc` dengan 5 error** — vitest tidak memeriksa tipe, jadi gerbang `tsc` bukan formalitas.

**Rollback rilis ke-3:** `git revert --no-edit a3164f7 && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-22 (**✅ DUA RILIS HARI INI, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `9c1d1b2`**, lokal ahead 0. Rilis ke-2 membawa **1 commit**: dua kotak cari akhirnya bertulisan sama — teksnya diangkat jadi konstanta `TEKS_KOTAK_CARI` (satu sumber) karena sudah DUA KALI menyimpang akibat ditulis terpisah. Dual push atas izin owner, cermin dulu baru produksi, keduanya **fast-forward**.

**Gerbang §6:** build **exit 0** + tabel status halaman **IDENTIK** dengan build sebelumnya (dibandingkan otomatis) → `tsc` **exit 0 / 0 error** → **789 tes / 54 berkas** (dari 786) → **mutation check 3 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Nol SQL, nol env baru.**

**Rollback rilis ke-2:** `git revert --no-edit 9c1d1b2 && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-22 (**✅ SUDAH DIRILIS & TERBUKTI TAYANG**). **`HEAD` = `origin/main` = `dramaku/main` = `4c62839`**, lokal ahead 0, antrean **KOSONG**. Rilis membawa **1 commit**: perbaikan navbar liar halaman depan. Dual push atas izin owner, urutan cermin (`dramaku`) dulu baru produksi (`origin`), keduanya **fast-forward** (`5501fdf..4c62839`, tanpa paksa) dan `git rev-list --count HEAD..origin/main` = **0** sebelum push.

**Gerbang §6 dijalankan penuh, urutan benar, exit code dibaca dari berkas (TIDAK dipipa):** `rm -rf .next` → `npm run build` **exit 0** (`/` `/beranda` `/discover` `/playly` `/shorts` `sitemap.xml` semua tetap `○ (Static)` 1m 1y) → `npx tsc --noEmit` **exit 0 / 0 error** → **786 tes / 54 berkas** (dari 741/54) → **mutation check 6 arah semuanya MERAH** → **nol berkas env/kunci** (dua lapis) → push → verifikasi tayang. **Nol SQL, nol env baru.**

**🎯 VERIFIKASI TAYANG DI SITUS SUNGGUHAN — 13 halaman produksi diperiksa satu per satu, semuanya 200, NOL yang salah:**

| Halaman | navbar hitam | nav bawah (HP) | Putusan |
|---|---|---|---|
| `/` | **0** | **0** | ✅ navbar liarnya HILANG |
| `/login` `/daftar` | 0 | 0 | ✅ |
| `/beranda` `/discover` | 0 | 1 | ✅ |
| `/shorts` `/playly` `/my-list` `/profile` `/history` `/video-eksternal` `/lupa-password` `/admin` | **1** | 1 | ✅ navigasinya UTUH |

**Isinya:** sebab anomali `/` yang sejak 2026-09-21 ditandai ❓ akhirnya **direproduksi** — `usePathname()` menerima nilai yang BUKAN `/` saat pra-render alamat akar, `?? "/"` tak menangkapnya (`??` hanya menangkap `null`/`undefined`, bukan string kosong), dan penyaringnya berbentuk **denylist** sehingga nilai tak dikenali membuat navbar **MUNCUL**. Diperbaiki jadi **allowlist** per-segmen di `lib/navigasi-halaman.ts` (BARU).

### ⚠️ KOREKSI atas nasihat yang saya tulis sendiri di sesi ini: penangkal cache sisi-klien TIDAK BEKERJA

Blok ini tadinya menyuruh memverifikasi dengan `?nocache=...`. **Itu keliru, dan sudah diuji:** terhadap `https://dramaapp.vercel.app/`, keempat cara ini sama-sama membalas `X-Vercel-Cache: HIT` dengan **ETag yang sama persis** dan `Age` yang cuma naik — nol yang menembus:

| Cara | Hasil |
|---|---|
| URL polos | `HIT`, age 16 |
| `?z=<acak>` (query-string acak) | `HIT`, age 17 — **query tidak masuk kunci cache** |
| header `Cache-Control: no-cache` | `HIT`, age 18 |
| header `Cache-Control: no-cache` + `Pragma: no-cache` | `HIT`, age 19 |

**Cara yang BENAR-BENAR membuktikan (terpakai di rilis ini):** jangan coba menembus cache — **tunggu dan amati dua penanda**. (1) **`Etag` berubah** begitu deployment baru hidup (`"wygz4v927kbxrp"` → `"247d04d03ef4664326021d5bf7aebc98"`); (2) **`X-Vercel-Cache` berhenti berbunyi `STALE`** dan berganti jadi `PRERENDER`/`MISS` dengan **`Age: 0`** = HTML itu benar-benar dirakit oleh deployment baru. Di rilis ini percobaan ke-1 masih `STALE age=524` (navbar masih 1), percobaan ke-2 sudah `PRERENDER age=0` (navbar 0). **Jadi pola verifikasi yang sah = polling berjeda sampai `Age` kembali 0, bukan satu tembakan dengan penangkal cache.**

**Rollback rilis ini:** `git revert --no-edit 4c62839 && git push origin main && git push dramaku main`.

---

**Terakhir dicek:** 2026-09-21 (**✅ LIMA RILIS HARI INI, antrean KOSONG**). **`HEAD` = `origin/main` = `dramaku/main` = `f068a73`**, lokal ahead 0. Rilis kelima membawa **2 commit**: `aa78995` (bar cari disamakan LK21 — warna merah terang tanpa ungu, logo kiri dirapikan, tombol Masuk/Daftar dilepas dari bar & dipindah ke menu garis-tiga) + `f068a73` (logo diperbesar 28→36px dan dijadikan satu komponen `LogoDramaKu`). Dual push atas izin owner, fast-forward. **Owner melihat preview lokal dulu.**

**Gerbang §6:** build **exit 0** (semua halaman tetap `○ Static`) → `tsc` **exit 0** → **741 tes / 54 berkas** → **mutation check 5 + 3 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Nol SQL, nol env baru.**

**Verifikasi tayang:** `/` & `/beranda` memakai `from-rose-700 via-rose-600 to-pink-600`, lambang **36px**, nama situs `text-xl`, **nol tombol Masuk/Daftar di dalam bar**, menu enam. Pagar: **`/shorts` navbar hitamnya TETAP ADA**.

**🔎 Anomali `/` (❓ sebab belum terverifikasi, BUKAN regresi):** HTML produksi `/` memuat **DUA logo** — milik `TopNav` (`text-lg`, pos 5094) dan `LogoDramaKu` (`text-xl`, pos 7975); di lokal hanya yang kedua. `TopNav` di HTML itu menggambar menunya **tanpa satu pun yang aktif**, yang konsisten dengan `pathname === "/"` — padahal nilai itu ada di `PUBLIC_PATHS` dan seharusnya memulangkan `null`. Akibatnya halaman depan sesaat menampilkan dua baris kepala sebelum JavaScript aktif. **Jangan tebak sebabnya.** Jalur yang belum dicoba: pindahkan keputusan "pakai navbar atau tidak" dari `usePathname()` ke tata letak per-segment.

**Rollback rilis kelima:** `git revert --no-edit f068a73 aa78995 && git push origin main && git push dramaku main`.

---

**Rilis keempat hari ini (`2cf4952`):** **`HEAD` = `origin/main` = `dramaku/main` = `2cf4952`**, lokal ahead 0. Rilis keempat = **`2cf4952`** (kepala situs `/beranda` & `/discover` jadi dua baris: navbar hitam + kotak cari kecil + 4 dropdown penyaring dilepas, isinya pindah ke tombol ☰ di bar merah; kotak cari dipanjangkan). Dual push atas izin owner, fast-forward. **Owner melihat preview lokal dulu.**

**Gerbang §6:** build **exit 0** (nol kemunduran status halaman) → `tsc` **exit 0** → **735 tes / 54 berkas** (dari 715/53) → **mutation check 7 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Nol SQL, nol env baru.**

**Verifikasi tayang (6 halaman produksi, semua 200):** `/beranda` navbar **hilang** + tombol ☰ **ada** + dropdown **hilang**; `/shorts` `/playly` `/profile` `/my-list` navbar **TETAP ADA** (akses terjaga — itu yang dijaga `tests/kepala-situs.test.ts`).

**❗ Efek samping yang perlu diketahui:** `/discover` tidak merender apa pun di server (dibungkus `<Suspense>` karena `useSearchParams`) — HTML-nya cuma "Memuat...". **Sudah begitu sejak sebelum hari ini**, tapi navbar yang dulu ikut tergambar kini tidak ada, jadi halaman itu **sesaat tanpa navigasi** sebelum JavaScript aktif. Perbaikan yang masuk akal: ganti fallback `"Memuat..."` di `app/discover/page.tsx` dengan kerangka bar merah. **Menunggu keputusan owner.**

**Rollback rilis keempat:** `git revert --no-edit 2cf4952 && git push origin main && git push dramaku main`.

---

**Rilis ketiga hari ini (`a6c2e82`):** **`HEAD` = `origin/main` = `dramaku/main` = `a6c2e82`**, lokal ahead 0. Rilis ketiga = **`a6c2e82`** (tulisan kotak cari "Cari film di DramaKu" + label menu Genre · Series · Populer · Negara · Tahun · + More), dual push atas izin owner, fast-forward.

**Gerbang §6 rilis ketiga:** build **exit 0** (nol kemunduran status halaman) → `tsc` **exit 0** → **715 tes / 53 berkas** → **mutation check 4 arah semuanya MERAH** → nol berkas env/kunci → push → verifikasi tayang. **Owner melihat preview lokal dulu** (`next start` port 3040) sebelum memberi izin — pola yang layak diulang untuk perubahan tampilan. **Nol SQL, nol env baru.**

**❗ Temuan saat verifikasi (BUKAN regresi):** `/beranda` punya **dua** kotak cari — navbar hitam (`TopNav.tsx:208`, masih "Cari drama, kategori...") dan bar merah (sudah "Cari film di DramaKu"). Menyeragamkan yang kecil **menunggu keputusan owner**. Juga **❓ belum terverifikasi**: pada `/`, `TopNav` tergambar di HTML produksi tapi tidak di lokal — **sudah begitu sejak sebelum perubahan hari ini**, dan navbarnya hilang sendiri saat halaman aktif di browser.

**Rollback rilis ketiga:** `git revert --no-edit a6c2e82 && git push origin main && git push dramaku main`.

---

**Rilis kedua hari ini (`923d3ed`):** **`HEAD` = `origin/main` = `dramaku/main` = `923d3ed`**, lokal ahead 0. Rilis kedua = **`923d3ed`** (revisi strip 29 chip → 14 atas permintaan owner), dual push atas izin owner, fast-forward, `git rev-list --count HEAD..origin/main` = **0** sebelum push.

**Gerbang §6 rilis kedua:** `rm -rf .next` → `npm run build` **exit 0** (nol kemunduran: `/` `/beranda` `/discover` `/playly` `/shorts` `sitemap.xml` semua tetap `○ (Static)` 1m 1y) → `npx tsc --noEmit` **exit 0** → **711 tes / 53 berkas** → **mutation check 7 arah semuanya MERAH** → **nol berkas env/kunci** (dua lapis) → push → verifikasi tayang.

**Verifikasi tayang:** HTML produksi menggambar **14 chip persis** (`ACTION ANIME HORROR KOMEDI SCI-FI ROMANCE CINA INDIA JEPANG KOREA THAILAND 2025 2026 TERPOPULER`), dan tiap chip diuji ke **katalog produksi yang sedang hidup (41 judul)** → **9 berisi, 5 kosong (Anime · India · Jepang · Korea · Thailand)**. Kelima yang kosong **disengaja** (keputusan owner, dicatat terbuka di komentar `STRIP_KATALOG`) dan **hidup sendiri** saat owner menambah judulnya — nol perubahan kode. **Nol SQL, nol env baru.**

**Rollback rilis kedua:** `git revert --no-edit 923d3ed && git push origin main && git push dramaku main`.

---

**Rilis pertama hari ini (`7fa2d85`):** **`HEAD` = `origin/main` = `dramaku/main` = `2104976`**, lokal **ahead 0 commit** dari keduanya. Dual push dijalankan atas izin owner ("push sekarang"), urutan cermin dulu (`dramaku`) baru produksi (`origin`), keduanya **fast-forward** (`git rev-list --count HEAD..origin/main` = **0** sebelum push → nol pekerjaan orang lain yang tertimpa).

**9 commit yang ikut tayang dalam satu rilis ini** — 8 antrean lama + 1 baru. Antrean lama sengaja tidak dipisah: `019fa8a` justru perbaikan yang **membuat build bisa lulus**, jadi merilis yang baru tanpa dia mustahil. Isinya: `c12b8db` (.gitignore `.claude/memory/`) · `254ce47` (`/playly` static + video gabungan ke `/beranda` & `/discover`) · `0827268` + `8e5323e` + `a40caef` (**kerja rekan**: halaman & menu pantau webhook Playly di admin) · `ddf5862` + `2e381aa` (catatan) · `019fa8a` (lepas prerender halaman drama) · **`2104976` (BARU: strip katalog gaya LK21)**.

**✅ GERBANG §6 DIJALANKAN PENUH, urutan benar, exit code dibaca dari berkas (TIDAK dipipa):** `rm -rf .next` → `npm run build` **exit 0** → `npx tsc --noEmit` **exit 0** → **712 tes lulus / 53 berkas, 0 gagal** (dari 693/52) → **nol berkas env/kunci ter-stage** (dua lapis: nama berkas + pola rahasia di isi diff) → push → **verifikasi tayang**.

**🎯 VERIFIKASI TAYANG DI SITUS SUNGGUHAN (bukan localhost):** HTML `https://dramaapp.vercel.app/` dibaca langsung → strip kuning menggambar **29 chip dalam 5 kelompok** dengan **4 garis pemisah**, kelas pembungkusnya `flex-nowrap overflow-x-auto md:flex-wrap md:overflow-x-visible` (= membungkus di layar lebar), dan menu bar tetap **Genre · Jenis · Populer · Negara · Tahun · Lainnya**. **29 alamat chip diuji satu per satu ke produksi → 29/29 HTTP 200, nol yang gagal.**

⚠️ **Batas yang jujur soal bukti di atas:** HTTP 200 membuktikan halamannya **terbuka**, BUKAN berapa judul yang muncul — `/discover` dibungkus `<Suspense>` (karena `useSearchParams`) sehingga **tidak merender apa pun di server**; HTML-nya cuma "Memuat...". Bukti jumlah judul datang dari tempat lain: **58 tautan menu+chip dijalankan terhadap katalog PRODUKSI nyata (41 judul, `GET /api/dramas`) → 0 yang memulangkan halaman hampa.**

**Nol SQL, nol env baru** — kolom `genre` & `country` yang dipakai penyaring baru memang sudah ada di database. Jadi urutan wajib "SQL dulu → env → baru push" (butir 3) tidak berlaku untuk rilis ini.

**🪤 JEBAKAN ALAT BARU (tambah ke daftar):** **`next start` yang GAGAL tetap membalas HTTP 200 — dari server LAIN.** Port uji ternyata sudah dipakai proses lain; `next start` mati dengan `EADDRINUSE` **di lognya**, tapi `curl` balas **200 + HTML utuh berisi kode LAMA**. Nyaris jadi kesimpulan "perubahan tidak masuk". **Sesudah menjalankan server uji, baca log server-nya dulu** — HTTP 200 tidak membuktikan permintaanmu sampai ke server yang baru dijalankan.

**LANGKAH BERIKUTNYA:** (1) ~~izin owner → dual push~~ **SELESAI**; (2) ~~verifikasi tayang~~ **SELESAI**; (3) **masih menggantung & terpisah dari rilis ini:** kirim `docs/permintaan-restart-supabase.md` bagian 2 ke Kang Dedi — itu yang menyembuhkan `/history`, `/my-list`, `/profile`, `/admin` yang menampilkan daftar film kosong. **Rollback 1-baris kalau ada masalah:** `git revert --no-edit 2104976 && git push origin main && git push dramaku main`.

**⚠️ Remote `official` MASIH rusak** ("Repository not found"), `git fetch --all` selalu terlihat gagal. Dual push tak terpengaruh. ❓ Perlu izin owner untuk `git remote remove official`.

**Sebelumnya dicek:** 2026-09-19 (**GERBANG RILIS LULUS — `/playly` TERBUKTI `○ (Static)`; 8 COMMIT SIAP, MENUNGGU IZIN PUSH OWNER**). **Lokal ahead 8 commit dari `origin/main` = `a65bfbf`.** `dramaku/main` = `8e5323e`, **nol kerja rekan baru**. **Belum di-push** — push ke `origin main` = tombol rilis, wajib izin owner.

**8 commit yang siap:** `c12b8db` (.gitignore `.claude/memory/`) · `254ce47` (`/playly` static + video gabungan ke `/beranda` & `/discover`) · `0827268` + `8e5323e` + `a40caef` (kerja rekan: halaman & menu pantau webhook Playly di admin) · `ddf5862` + `2e381aa` (catatan) · + commit hari ini (lepas prerender halaman drama + penjaga + catatan).

**✅ GERBANG §6 DIJALANKAN PENUH, urutan benar, exit code dibaca dari berkas (TIDAK dipipa):** `rm -rf .next` → `npm run build` **exit 0** (pertama kali sejak 2026-09-16) → `npx tsc --noEmit` **exit 0** → **693 tes lulus / 52 berkas, 0 gagal** (687+6 penjaga baru, cocok persis) → **nol berkas env/kunci ter-stage** (dua lapis: nama berkas + isi diff).

**🎯 BUKTI YANG DICARI SEJAK 2026-09-15 AKHIRNYA ADA — dari keluaran `npm run build`, bukan dari membaca kode:** `/playly` **`○ (Static)` 1m 1y** · `/beranda` **`○` 1m 1y** · `/discover` **`○` 1m 1y** · `/` **`○`** · `/shorts` **`○`** · `/sitemap.xml` + `/robots.txt` **`○`** (SEO utuh) — **nol kemunduran**. Tahap prerender **21/21 selesai** dalam 18,4 dtk (dulu 63 dan selalu tumbang). **Bukti ISR dari `next start` sungguhan:** halaman drama yang belum pernah diminta → kunjungan **pertama 200 / 0,32 dtk / `x-nextjs-cache=MISS`**, ke-2 & ke-3 **200 / 0,01 dtk / `HIT`**, `cache-control: s-maxage=60` → benar-benar **disimpan**. `/playly` ber-cache (`s-maxage=60`), **bukan** `no-store` → `254ce47` terbukti bekerja. Judul tak ada → **404**, bukan error.

**Yang diubah hari ini (2 berkas kode, keputusan owner):** `app/drama/[id]/page.tsx` — `generateStaticParams()` memulangkan **daftar kosong** (42 halaman drama dibuat saat pengunjung pertama membukanya, lalu disimpan ISR 60 dtk), plus `export const dynamicParams = true` **eksplisit** sebagai pengaman (dengan daftar kosong, `false` = **SEMUA** halaman drama 404 tanpa error apa pun di build), impor `getAllDramas` dibuang. `tests/drama-prerender-build.test.ts` (**BARU**, 6 tes) — **mutation check 4 arah, keempatnya MERAH** lalu hijau lagi. **Nol SQL, nol env baru.** Harganya jujur: pengunjung **pertama** tiap judul menunggu **0,32 detik** (terukur).

**🔴 KOREKSI — kemacetan 4 hari BUKAN karena Supabase.** Build gagal **9×** hari ini sementara Supabase terbukti sehat. **Bukti paling bersih ada DI DALAM satu build yang gagal:** `generateStaticParams()` → pembacaan **TANPA cache** **BERHASIL** ambil 42 judul, lalu `getDramaCached()` → pembacaan **LEWAT cache fetch Next** (`revalidate: 60`) **timeout 6 detik** dan membunuh build. Kalau databasenya mati, pembacaan pertama juga mati — ia tidak. Diperkuat: bentuk query **persis** milik halaman drama diuji untuk **seluruh 42 judul → 42/42 sukses < 1 dtk**, plus pantauan **3 menit tanpa putus → 36/36 sukses**, dan build tepat sesudahnya **tetap gagal**. ⚠️ Pesan `"Supabase tidak menjawab"` **menyesatkan** — dari pembungkus kita sendiri (`lib/supabase.ts:186`) yang menyimpulkan setiap timeout = database bermasalah. **Sembilan dugaan dimatikan dengan percobaan:** ❌ Supabase mati · ❌ 47 worker (**1 worker pun gagal**) · ❌ 47 proses terpisah (46/47 sukses) · ❌ 30 tarikan katalog serentak (semua 200) · ❌ query berat · ❌ baris data menggembung (0,7–1,5 KB) · ❌ batas 60 dtk (**makin buruk**) · ❌ disk/antivirus (0,5 ms) · ❌ regresi versi Next. ❓ Penyebab persis di dalam Next **belum terverifikasi** — dugaan terkuat jalur cache fetch Next 16 (Turbopack) saat prerender. **Perubahan hari ini MENGHINDARI masalahnya, bukan menyembuhkannya** — batas yang jujur.

**🔴 SUPABASE MASIH BERMASALAH — penyakit PRODUKSI, terpisah dari rilis ini, BELUM selesai.** Ia kedip: satu jendela **25/25 sukses** (0,02–0,11 dtk), jendela lain **30/30 timeout**, dan kode errornya **`PGRST002` · "Could not query the database for the schema cache. Retrying."** = PostgREST tidak kebagian sambungan database. Obat: **Restart project** (bukan Pause/Delete); project `nvblmpkwyzbpdbshyvzw` **milik Kang Dedi**. 🆘 **Teks siap salin-tempel sudah disiapkan: `docs/permintaan-restart-supabase.md` bagian 2** — tanpa meminta Personal Access Token (keputusan 2026-08-31), nol secret. ❌ Dampak yang masih berjalan: `/api/dramas` **500 (12,5 dtk, 3× konsisten)** → `/history`, `/my-list`, `/profile`, `/admin` menampilkan **daftar film kosong**; `/api/ads` **500** (tak terasa — kunci `ads` isinya `[]`). ✅ Penonton tetap aman: semua halaman katalog **200 di bawah 0,5 dtk** dari salinan ISR, dan **login jalan** (401 dalam 0,91 dtk untuk email pasti-tak-terdaftar). ⚠️ **200 di halaman katalog BUKAN bukti database sehat.**

**🪤 DUA JEBAKAN ALAT:** (1) **`npm run build 2>&1 | tail` melapor `exit code 0` padahal build GAGAL `exit 1`** — exit code itu milik `tail`; sesi ini kena dan sempat melapor "build lulus". Gerbang §6 bersandar penuh pada exit code → **selalu `> berkas.log 2>&1` lalu baca `$?`**. (2) **Jendela sampel pendek = bukti palsu untuk gangguan yang kedip** — 20 percobaan cepat cuma memotret 12 detik; syarat "pulih" = **berdurasi ≥3 menit** dan **100% sukses**.

**LANGKAH BERIKUTNYA:** (1) izin owner → **dual push** ke `origin` **dan** `dramaku`; (2) verifikasi tayang: `/playly` harus **cepat** (dulu 12,8 dtk) dengan header ber-cache bukan `no-store`, `/beranda` + `/discover` normal, satu judul drama terbuka; (3) kirim `docs/permintaan-restart-supabase.md` bagian 2 ke Kang Dedi (menyembuhkan 4 halaman berdaftar kosong — **terpisah dari rilis**). **Rollback 1-baris sesudah push:** `git revert --no-edit <sha commit hari ini> && git push origin main`.

**⚠️ Remote `official` MASIH rusak** (dicek ulang): → **"Repository not found"**, `git fetch --all` selalu terlihat gagal. Dual push tak terpengaruh. ❓ Perlu izin owner untuk `git remote remove official`.

**Sebelumnya dicek:** 2026-09-18 (   **PERBAIKAN /playly SIAP TAPI TERTAHAN — Supabase mati lagi; sekaligus ADA 2 COMMIT KERJA REKAN MENUNGGU**). **Lokal = `origin/main` = `a65bfbf`.** **`dramaku/main` = `8e5323e`, MAJU 2 commit** dari lokal — bukan lokal yang tertinggal push, melainkan kerja rekan baru yang belum ditarik. **Nol commit, nol push hari ini.**

**⏸️ TERTAHAN DI GERBANG §6 — `npm run build` tidak bisa lulus.** Perbaikan `/playly` static + video gabungan masuk `/beranda` & `/discover` **selesai dikode dan teruji** (8 berkas di working tree, belum di-commit saat catatan ini ditulis). Yang sudah terbukti: `npx tsc --noEmit` **exit 0** · **671 tes lulus / 50 berkas, 0 gagal** (naik dari 653/49) · **mutation check 4 arah semuanya MERAH** lalu hijau lagi. Yang BELUM ada dan menahan rilis: keluaran `npm run build` yang mendaftar `/playly` sebagai **○ (Static)** — satu-satunya bukti yang sah, karena membaca kode saja sudah pernah menipu (2026-09-15). Build gagal **3×** dengan `Next.js build worker exited with code: 4294967295 / 3221226505`, selalu di tahap *Generating static pages*, selalu didahului `[drama] gagal ambil daftar id saat build: Supabase tidak menjawab setelah 2 percobaan (TimeoutError)`. ✅ **Terbukti bukan salah kode ini:** perubahan di-`git stash`, build atas `a65bfbf` apa adanya → **crash sama persis**. `✓ Compiled successfully` + `Finished TypeScript` selalu lulus di ketiga percobaan.

**🔴 INSIDEN 522 KAMBUH.** Diukur langsung ke Supabase tanpa lewat Vercel: `GET /rest/v1/` polos **401 dalam 0,32 dtk** (gerbang hidup), tabel `dramas` & `app_data` dengan kunci sah **habis waktu 30 detik tanpa balasan**. Database PostgreSQL-nya yang tak menjawab, bukan kuncinya. ❓ Penyebab akar belum terverifikasi — butuh mata owner di dashboard project `nvblmpkwyzbpdbshyvzw` (banner atas + Reports / Database Health). ⚠️ Jangan tebak-tebak mengganti env Supabase di Vercel: kunci terbukti masih sah (401, bukan 403).

**PRODUKSI SAAT DIUKUR — angkanya sendiri yang membenarkan perbaikan ini:** `/beranda` **200 / 0,85 dtk** · `/discover` **200 / 0,69 dtk** · `/` **200 / 0,87 dtk** · `/login` **200 / 0,62 dtk** (semua static, selamat) vs `/playly` **200 tapi 13,11 detik** (dynamic, menggantung) dan `/api/dramas` **500 / 12,90 dtk**. Header: `/playly` → `no-store` + `X-Vercel-Cache: MISS` + `Age: 0`; `/beranda` → `X-Vercel-Cache: STALE` + `Age: 242`.

**⏳ MENUNGGU DITARIK — kerja rekan `8e5323e`:** "halaman & menu pantau webhook Playly" di admin. **6 berkas, +916 baris, 0 dihapus** — `app/admin/webhooks/playly/page.tsx`, `app/components/admin/PlaylyWebhookMonitor.tsx`, `lib/playly-webhook-status.ts`, `tests/playly-webhook-status.test.ts` (semua BARU) + `AdminSidebar.tsx` (+7 baris menu) + `docs/serah-terima/2026-09-16-menu-webhook-admin.md`. **Patuh proses:** nol sentuhan ke `HANDOFF.md` / `antrean-deploy.md` / `docs/lintasai/INDEX.md`, membawa berkas serah-terima sendiri, nol berkas env. Lokal = nenek-moyang langsung → **fast-forward bersih**. ⚠️ Author masih `masradenbagus89-ui <zyyherlambang@gmail.com>` — aturan 7 (identitas git dipisahkan) **masih belum tuntas**: nama author-nya tetap terbaca sebagai owner.

**LANGKAH BERIKUTNYA:** (1) owner pulihkan Supabase; (2) `rm -rf .next` → `npm run build` → **wajib** `/playly` `○ (Static)` DAN `/beranda` + `/discover` TETAP `○ (Static)`; (3) `tsc` → `npm test` → cek env; (4) izin owner → dual push.
✅ **KERJA REKAN DITARIK KE LOKAL `a40caef` (merge, 2026-09-18, perintah owner) — BELUM DI-PUSH.** Yang ditarik: branch **`chore/gitignore-claude-memory`**, bukan `dramaku/main` — branch itu superset (sudah memuat `0827268` + merge `8e5323e`, plus `c12b8db` yang mengabaikan `.claude/memory/` di `.gitignore`). **7 berkas, +920 baris, 0 dihapus:** `app/admin/webhooks/playly/page.tsx`, `app/components/admin/PlaylyWebhookMonitor.tsx`, `lib/playly-webhook-status.ts`, `tests/playly-webhook-status.test.ts` (16 tes) — semua BARU — plus `AdminSidebar.tsx` (+7 baris menu), `.gitignore` (+4), dan `docs/serah-terima/2026-09-16-menu-webhook-admin.md`. **Nol SQL, nol env baru.**

**Nol bentrok, dibuktikan SEBELUM ditarik** (bukan sesudah): nol berkas disentuh berdua + `git merge-tree` bersih. Keduanya saling melengkapi — halaman admin rekan sengaja memakai `getPlaylyWebhookVideos()` & `getPlaylyHiddenIds()` versi **SEGAR**, dua fungsi yang `254ce47` memang tidak sentuh.

**Bukti atas kode gabungan:** `npx tsc --noEmit` **exit 0** · **687 tes lulus / 51 berkas, 0 gagal**. ❌ `npm run build` **masih belum bisa** — Supabase dicek ulang sesudah merge: tabel `dramas` habis waktu 25 detik, `/api/dramas` produksi masih 500.

**⚠️ PROSES REKAN — patuh penuh untuk kedua kalinya berturut-turut.** Nol sentuhan ke `HANDOFF.md` / `antrean-deploy.md` / `docs/lintasai/INDEX.md`; membawa berkas serah-terima sendiri yang **jujur soal batasnya** (menuliskan sendiri bahwa tampilan produksi belum dilihat, dan bahwa build lokal mendaftar `/playly` sebagai static HANYA karena `useSupabase=false` sehingga tidak bisa dipakai membantah temuan dynamic di produksi). Aturan 2 (branch berumur pendek) juga dipatuhi: tertinggal **0** commit dari `main`. **Yang masih menganggur: aturan 7** — author tetap `masradenbagus89-ui <zyyherlambang@gmail.com>`, jadi `git log --format=%an` masih menjawab "owner" untuk kerja rekan.

**⚠️ Remote `official` RUSAK:** `https://github.com/projectraden/backup-dramaapp.git` -> **Repository not found**, membuat `git fetch --all` selalu keluar error. Dual push tak terpengaruh (hanya `origin` + `dramaku`). ❓ Belum dihapus, perlu izin owner.
🔄 **KOREKSI — penghalang rilis BUKAN "database mati".** Diuji 4 layanan dengan kunci `service_role` yang sama: **Storage 200 / 1,48 dtk** dan mengembalikan baris ASLI dari `storage.buckets` (bucket `videos` 2026-07-08, `matchday-cards` 2026-07-23) · PostgREST `OPTIONS` **200 / 0,36 dtk** · tapi PostgREST baca tabel **timeout 25 dtk** dan Auth `admin/users` **timeout 25 dtk**. Jadi **PostgreSQL HIDUP**; yang tersendat jalur **PostgREST + Auth**. Tercoret: project di-pause (Storage jalan) · kunci tak sah (kunci sama diterima Storage) · jaringan owner (TCP ke pooler tersambung **74 ms**) · disk penuh. ❓ Dugaan tersisa: jatah koneksi habis / ada yang mengunci. Obat paling mungkin: **Restart project**. **Memburuk**: Storage 1,48 dtk -> **13,83 dtk** dalam sejam.

🔴 **YANG MENAHAN RILIS SEKARANG BUKAN LAGI SOAL TEKNIS, TAPI AKSES.** Project `nvblmpkwyzbpdbshyvzw` **milik Kang Dedi**, bukan owner (HANDOFF.md:1668). Owner tidak bisa menekan tombol Restart sendiri. Selama PostgREST tersendat, `npm run build` tidak akan bisa lulus, jadi **3 commit lokal (`254ce47`, `a40caef`, `ddf5862`) tetap menunggu** dan `origin/main` tetap `a65bfbf`. ⚠️ **Jangan minta Personal Access Token Kang Dedi** (keputusan 2026-08-31, HANDOFF.md:1680).

**Sebelumnya dicek:** 2026-09-16 (siang, **DUA KERJA REKAN DITARIK & TAYANG — video webhook masuk /playly + login tahan database mati**). **Lokal = `origin/main` = `dramaku/main` = `5e6cbec`, selisih NOL** — dual push `1242955..5e6cbec` ke kedua remote, keduanya sukses. Asalnya rekan lewat branch **`feat/playly-webhook`** di cermin `dramaku` (**patuh proses penuh**: nol sentuhan ke `HANDOFF.md` / `antrean-deploy.md` / `docs/lintasai/INDEX.md`, membawa 2 berkas `docs/serah-terima/` sendiri, nol berkas env/kunci). Branch tertinggal **5 commit** dari `main` — melanggar aturan 2 (branch berumur pendek) — tapi **uji merge kering `git merge-tree` bersih**, sebab yang disentuh rekan beda berkas dengan rilis baris-kategori `e466595`. **2 commit, 12 berkas, +1229 baris:** `8297548` (BARU `lib/playly-gabungan.ts` + `app/playly/page.tsx` + gerbang IDOR `app/api/playly/video/route.ts` ikut mengenal daftar gabungan — **tidak dilemahkan**, cuma isi daftar izinnya ditambah; tanpa ini video webhook TAMPIL tapi 404 saat diklik) dan `b085453` (`lib/supabase.ts` + `app/api/auth/login/route.ts`: badan error diringkas di akarnya, batas waktu 6 detik, percobaan ulang **hanya BACA** — tulis sengaja tidak, sebab mengulang `coin_add` berisiko koin bertambah dua kali). **Nol SQL, nol env baru.** Gerbang §6 urutan baru dijalankan penuh: `rm -rf .next` → `npm run build` **exit 0** → `npx tsc --noEmit` **exit 0** (nol `PageProps` palsu, urutan terbukti benar untuk kedua kalinya) → **653 tes lulus / 49 berkas, 0 gagal** (naik dari 613/45) → nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/login` `/playly` **200** (0,50-0,70 dtk) · `/api/dramas` **200** (1,86 dtk) · `/api/ads` **200** (1,50 dtk) · `POST /api/auth/login` email pasti-tak-terdaftar → **401 dalam 1,49 dtk** = jalur login menyentuh database dan memverifikasi akun dengan benar. **Rollback 1-baris:** `git revert --no-edit 5e6cbec -m 1 && git push origin main` (merge commit → wajib `-m 1`).

**🔴 INSIDEN 522 SUDAH BERLALU.** Jalur database yang kemarin **500 sesudah ~20 detik** kini **200 di bawah 2 detik** (diukur 4 endpoint). ❓ Penyebab akarnya tetap **belum terverifikasi** — ada di pihak Supabase, di luar kendali kode kita. Yang dirilis hari ini **bukan penyembuh penyebabnya, melainkan peredam akibatnya**: tanpa `b085453`, gangguan berikutnya akan kembali memuntahkan HTML Cloudflare mentah ke browser penonton dan menggantung 20 detik sebelum mati.

**⚠️ TEMUAN SESI INI — `/playly` BERUBAH STATIC → DYNAMIC, klaim di berkas serah-terima rekan TIDAK cocok kenyataan.** `docs/serah-terima/2026-09-15-gabung-video-playly.md` §5 menulis "`/playly` tetap static + revalidate 5m". **Tidak.** Bukti dua lapis: (a) `npm run build` mendaftar `/playly` sebagai **ƒ (Dynamic)** sementara `/beranda` & `/discover` tetap **○ (Static) 1m 1y**; (b) header produksi `/playly` → `Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate` + `X-Vercel-Cache: MISS`. **Terlacak sampai baris:** `getPublishedPlaylyWebhookVideos()` → `getPlaylyWebhookVideos()` (`lib/store.ts:1029-1034`) → `sbDocGet(PLAYLY_WEBHOOK_DOC)` **tanpa `revalidate`** → jatuh ke default `cache: "no-store"` (`lib/supabase.ts:214`), dan komentar `lib/supabase.ts:204` sudah memperingatkan persis ini: **satu fetch `no-store` membuat SELURUH halaman dinamis**. Bandingkan `getPlaylyHiddenIdsCached()` (`lib/store.ts:934-943`) yang memakai `revalidate: CATALOG_TTL_SECONDS` — itu sebabnya dua halaman lain selamat. **Dampak dua arah, bukan bug merusak:** merugikan → tiap kunjungan memanggil Supabase langsung, beban fungsi Vercel naik, dan kalau Supabase kambuh `/playly` ikut mati (padahal saat insiden kemarin halaman itu **selamat justru karena static**); menguntungkan → video webhook muncul **seketika**, yang justru inti guna webhook (langkah §6 no.2 di berkas serah-terima "refresh setelah 5 menit" jadi tidak akurat). **❓ Menunggu keputusan owner.** Perbaikannya **BUKAN** menambah `revalidate` di `getPlaylyWebhookVideos()` — fungsi itu juga dibaca jalur TULIS (`upsertPlaylyWebhookVideo` membaca-lalu-menulis), data basi di sana bisa **menimpa video lain**. Cara benar mengikuti pola yang sudah ada: varian terpisah `getPublishedPlaylyWebhookVideosCached()`, persis seperti pasangan `getPlaylyHiddenIds` ↔ `getPlaylyHiddenIdsCached`.

**❓ 3 keputusan lain yang diserahkan rekan:** (1) `/beranda` + `/discover` belum ikut daftar gabungan — cukup ganti satu baris import di `app/beranda/page.tsx:3` & `app/discover/page.tsx:4`, sengaja tidak diubah diam-diam karena keduanya sudah tayang; (2) ±20 route lain masih meneruskan `err.message` ke browser (sekarang pendek, tidak lagi HTML raksasa, tapi masih menyebut detail teknis internal) = satu tugas terpisah; (3) `payload.json` di akar repo belum terlacak git, sepertinya sisa uji coba.

**⚠️ PROSES REKAN — satu maju, satu mundur.** Maju: aturan 1 (berkas catatan owner) dipatuhi **penuh** untuk pertama kalinya berturut-turut, dan berkas serah-terimanya paling rinci sejauh ini (menyebut alasan keputusan, batas jujur, dan apa yang sengaja TIDAK dibangun). Mundur: aturan 2 dilanggar — branch dibiarkan tertinggal **5 commit** sebelum diserahkan, dan **klaim bukti di §5 tidak akurat** (lihat temuan `/playly` di atas). Pelajaran yang layak diulang: **klaim "tetap static" wajib dibuktikan dari keluaran `npm run build`, bukan dari membaca `export const revalidate` di berkasnya** — nilai itu bisa dibatalkan oleh satu fetch `no-store` di kedalaman mana pun yang dipanggil halaman itu.

**Sebelumnya dicek:** 2026-09-16 (**CATATAN GERBANG PRA-RILIS DIKIRIM + 🔴 TEMUAN DATABASE MATI — kerja owner**). **Lokal = `origin/main` = `dramaku/main` = `bf73d5f`, selisih NOL** — dibuktikan lewat `git ls-remote` LANGSUNG ke server kedua repo, bukan cache lokal. Dual push `69b1981..bf73d5f` (1 commit); kedua remote tertinggal **0** sebelum push, jadi nol risiko menimpa. Isinya: suntingan `AGENTS.local.md` poin 6 yang sudah disetujui owner 2026-09-15 tapi **tertinggal belum di-commit**. **3 berkas, semuanya `.md`** — **nol kode situs, nol SQL, nol env**. Gerbang §6 tetap dijalankan penuh: `npm run build` **exit 0** → `npx tsc --noEmit` **exit 0** (nol error `PageProps` — urutan barunya terbukti benar) → **613 tes lulus / 45 berkas** → nol berkas env/kunci ter-stage (dua lapis: nama berkas + isi diff, keduanya NOL). **Rollback 1-baris:** `git revert --no-edit bf73d5f && git push origin main`.

**🔴 TEMUAN SAAT VERIFIKASI PRODUKSI — DATABASE SUPABASE TIDAK MENJAWAB. BUKAN akibat dorongan ini.** `GET /api/dramas` **500** (3× konsisten, ~20 detik tiap kali) dan `GET /drama/<id>` **500** sesudah ~40 detik (2 judul berbeda) — **penonton yang mengklik satu judul akan menggantung lalu kena halaman error.** Dibuktikan bukan salah Vercel: kueri LANGSUNG dari komputer ini ke Supabase (kunci `.env.local`, tanpa lewat situs) balas **HTTP 522** sesudah ~19,5 detik di **4 tabel berbeda** (`dramas`, `users`, `store_docs`, `videos`), sementara `GET /rest/v1/` polos balas **401 dalam 0,23 detik** → gerbang API hidup, **database PostgreSQL di belakangnya yang tak menjawab**. Halaman daftar (`/` `/beranda` `/discover` `/playly` `/shorts`) masih **200** dengan 119 kartu karena disajikan dari pra-render ISR (salinan lama), **jadi 200 di situ BUKAN bukti database sehat**. Jendela rusaknya ±1 hari (2026-09-15 masih `200, 67 drama`). ❓ Penyebab persis belum terverifikasi — butuh mata owner di <https://supabase.com/dashboard/project/nvblmpkwyzbpdbshyvzw> (banner atas + Reports / Database Health). ⚠️ **Jangan tebak-tebak mengganti env Supabase di Vercel** — kuncinya terbukti masih sah (401, bukan 403).

**Sebelumnya dicek:** 2026-09-15 sore (**SUSUNAN FILM /beranda JADI BARIS KATEGORI GAYA LK21 — kerja owner**). **Lokal = `origin/main` = `dramaku/main` = `e466595`, selisih NOL** — ketiganya dibandingkan sesudah `git fetch` kedua remote. Dual push `dca2326..e466595` (1 commit); kedua remote tertinggal **0** sebelum push, jadi nol risiko menimpa. Asalnya permintaan owner + 2 screenshot berkotak merah (dramaku vs Layarkaca21); bentuknya dipilih owner lewat popup: **baris kategori menggantikan grid, grid muncul saat mencari**. **9 berkas** (7 diubah, 2 baru): `lib/beranda-catalog.ts` (+`GENRE_SEMUA`, `URUTAN_BAWAAN`, fungsi murni `sedangMenyaring()`), `app/components/beranda/CatalogBrowser.tsx` (percabangan baris vs grid), `FeaturedRow.tsx` (+prop opsional `cardClass`), `shell.ts` (+`ROW_KATEGORI_CARD_CLASS`), `tests/beranda-catalog.test.ts` (+6 penjaga), `tests/beranda-bentuk-halaman.test.ts` (**BARU**, 3 tes yang MERENDER komponen sungguhan), rencana + INDEX + HANDOFF. **Nol env / nol SQL** — murni tampilan & logika murni, tak ada yang perlu owner siapkan di Vercel atau Supabase. `GRID_CLASS` & `ROW_CARD_CLASS` **sengaja tidak disentuh**, `DramaBrowser.tsx` (/discover) **nol perubahan**. Pra-push (§6): `npm run build` **exit 0** → `tsc` **exit 0** → **613 tes lulus / 45 berkas** (naik dari 604/44) → nol berkas env/kunci ter-stage (dua lapis: nama berkas + isi diff, keduanya NOL). **TAYANG & TERVERIFIKASI** (percobaan ke-45, ±1,5 menit): `/beranda` **200** memuat 5 judul baris (`Drama Terbaru`, `Paling Banyak Ditonton`, `Drama Action`, `Drama Romance`, `Drama Tycoon`) + **0** kemunculan "Halaman 1 dari" + **119** kartu `w-[92px]` (= 40+40+21+14+4, cocok persis sebaran katalog) · **NOL regresi:** `/` tetap **133** kartu `w-[116px]` dan **0** kartu kecil, `/discover` **200**. **Rollback 1-baris:** `git revert --no-edit e466595 && git push origin main`.

**✅ JEBAKAN `PageProps` — AKHIRNYA DITUTUP DI SUMBERNYA (2026-09-15, atas izin owner).** Urutan di `AGENTS.local.md` poin 6 sudah dibetulkan jadi `rm -rf .next` → **`npm run build`** → `npx tsc --noEmit` → `npm test` → cek env → push → verifikasi produksi, plus blok peringatan "jangan dibalik" di bawahnya. Selama tiga sesi jebakan ini cuma DICATAT, tidak pernah diperbaiki di sumbernya — itu sebabnya ia terus kambuh. Duduk perkaranya: Sesi ini menuruti urutan gerbang **sebagaimana tertulis di `AGENTS.local.md` poin 6** (`rm -rf .next` → `tsc` → …) dan langsung kena 4× `TS2304: Cannot find name 'PageProps'` di `app/drama/[id]/page.tsx`, `app/feed/[id]/page.tsx`, `app/watch/[id]/[ep]/page.tsx`. Sebabnya `next-env.d.ts` mengimpor `./.next/types/routes.d.ts` — tipe yang baru DIBUAT `next build`. Dibuktikan: sesudah `npm run build`, `tsc` atas kode yang sama persis keluar **exit 0**. Ini sudah tercatat 2026-09-11 dan dihindari 2026-09-14, **tapi teks poin 6 masih salah urutan** sehingga terus menjebak. **Perbaikan yang perlu owner setujui:** ubah poin 6 jadi `rm -rf .next` → `npm run build` → `npx tsc --noEmit` → `npm test` → cek env → push → verifikasi produksi.

**Sebelumnya dicek:** 2026-09-15 (**WEBHOOK PLAYLY — kerja rekan, ditarik & dirilis owner dalam keadaan TIDUR**). **Lokal = `origin/main` = `dramaku/main` = `b7459a4`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Asalnya rekan (`zyyherlambang@gmail.com`) push branch **`feat/playly-webhook`** ke cermin `dramaku` (kali ini **patuh proses**: branch + berkas `docs/serah-terima/2026-09-14-webhook-playly.md`, tidak menyentuh berkas milik owner, dicabang dari `main` terkini → **0 commit tertinggal, 0 konflik**); owner memastikan fast-forward lewat `git merge-base --is-ancestor` lalu `git merge --ff-only`, kemudian dual push ke `origin` + `dramaku`. **2 commit, 10 berkas, +2316 baris:** `app/api/webhooks/playly/route.ts` + `lib/playly-webhook.ts` (BARU, verifikasi pengirim), `lib/store.ts` (+dokumen `playly:webhook`, idempoten by `videoId`, **plus perbaikan bug lama `readLocal()`**), `lib/playly.ts` (**cuma menambah kata `export`** — nol perubahan perilaku), `.env.example`, 3 berkas tes (**82 tes**), 2 berkas docs. **Nol SQL.** **Env: ADA SATU tapi SENGAJA TIDAK DIPASANG** — `PLAYLY_WEBHOOK_SECRET` dibiarkan kosong atas keputusan owner (popup 2026-09-15), jadi endpoint membalas **503** dan tak memproses apa pun. Pra-push (§6, dijalankan owner sendiri): `tsc` **exit 0**, **604 tes lulus / 44 berkas** (naik dari 522/41), `rm -rf .next` + `npm run build` **exit 0** dengan route terdaftar **ƒ Dynamic**, nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/login` `/beranda` `/discover` `/shorts` `/playly` semua **200** · `/api/dramas` **200, 67 drama** · `POST /api/webhooks/playly` tanpa kunci **503**, dengan kunci ngawur **tetap 503** (pintunya memang tertutup, bukan sekadar belum dicoba). **Rollback:** `git revert --no-edit b7459a4 b6e8b05 && git push origin main`.

**❓ ANGKA VIDEO PLAYLY 39-40 → 35 — bukan dari rilis ini, tapi belum tuntas terlacak.** Terbukti: diff ke `lib/playly.ts` hanya penambahan kata `export` (nol perubahan logika) dan 604 tes termasuk `playly-publik`/`playly-paginasi` lulus; katalog **publik** Playly hari ini melaporkan **36** video milik akun `coklat` (dari 287 total semua kreator) sementara situs kita menampilkan **35** — jadi selisihnya ada di **sumbernya**. Yang **tidak** bisa dibuktikan dari komputer owner: ke mana 4-5 video yang terhitung 2026-09-14 pergi, karena angka itu berasal dari daftar **MITRA** (`/api/videos`, butuh kunci yang tersimpan di Supabase) sedangkan tanpa kunci hanya katalog publik yang bisa diketuk. Langkah kalau mau dituntaskan: buka dashboard Playly, cek apakah ada video yang ditarik/di-unpublish. **Jangan dicatat sebagai kemunduran DramaKu sebelum itu dicek.**

**⚠️ CATATAN PROSES — rekan sudah patuh, tapi §7 masih menganggur.** Email author branch ini sudah `zyyherlambang@gmail.com` (beda dari owner), **tapi nama author-nya masih `masradenbagus89-ui`**. Artinya `git log --format='%an'` sekilas masih terbaca seperti owner yang mengerjakan. Perbaikannya di komputer rekan: `git config user.name "<nama rekan>"`.

**Sebelumnya dicek:** 2026-09-14 (**PAGINASI PLAYLY — kerja rekan, ditarik & dirilis owner**). **Lokal = `origin/main` = `dramaku/main` = `852c989`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Asalnya rekan (`zyyherlambang@gmail.com`) push ke `dramaku/main`; owner `git pull --ff-only dramaku main` (`889b2cc..852c989`, fast-forward dipastikan dulu lewat `git merge-base --is-ancestor` — nol risiko menimpa), lalu dual push ke `origin` + `dramaku`. **2 berkas:** `lib/playly.ts` (+129, fungsi bersama `ambilSemuaHalamanPlayly()` mengikuti `hasMore` dengan `limit=100`, batas `PLAYLY_MAKS_HALAMAN=10`, offset maju sesuai `count` yang BENAR-BENAR dikirim), `tests/playly-paginasi.test.ts` (BARU, 10 penjaga). **Nol env / nol SQL** — semua `process.env` di commit itu hanya di dalam berkas tes, jadi tak ada yang perlu disiapkan owner di Vercel atau Supabase. Pra-push: `tsc` **exit 0**, **522 tes lulus / 41 berkas** (naik dari 512/40), `rm -rf .next` + `npm run build` **exit 0**, nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/login` `/beranda` `/discover` `/shorts` `/playly` semua **200** · `/api/dramas` **200, 67 drama** · embed video Playly di `/playly` **40**, pengukuran ulang **39** — selisih 1-2 NORMAL karena `bolehTampilKePenonton()` memeriksa kesehatan berkas tiap video, jadi jangan dibaca sebagai kemunduran (catatan 2026-09-12 mencatat **20** — inilah bukti perbaikannya benar-benar sampai ke penonton) · video terbaru `1789356249652` (diunggah hari ini) **TAMPIL**. **Rollback 1-baris:** `git revert --no-edit 852c989 && git push origin main`.

**✅ MISTERI 2026-09-12 TERPECAHKAN — dugaan lama SALAH.** Catatan sebelumnya menandai `1789096535588` ("The Last Passenger KONO EXPRESS Chapter 1") **0 jejak** di `/playly` padahal `/api/public-video` membalas 200 + `allowEmbed:true`, dengan dugaan "disembunyikan admin lewat `playly:hidden`". Sesudah rilis ini video itu **tampil** tanpa ada yang menyentuh pengaturan admin → penyebab sebenarnya **paginasi**: ia duduk di halaman kedua yang tak pernah diambil. Pelajaran yang layak diulang: sebelum menuduh "pengaturan admin", pastikan dulu daftar sumbernya memang **sudah terambil seluruhnya**.

**⚠️ JEBAKAN BARU — `npm run build` bisa mati sporadis di Windows.** Build **pertama** mati di tengah *Generating static pages* dengan `⨯ Next.js build worker exited with code: 4294967295 and signal: null` (4294967295 = -1, worker dibunuh OS — bukan error TypeScript, bukan error kode). Build ulang **tanpa mengubah apa pun** langsung **exit 0** dua kali berturut-turut. Jadi: crash worker seperti ini **bukan bukti commit-nya cacat** — ulangi build dulu sebelum menyalahkan kodenya. Sekaligus pengingat: jangan pakai `$TEMP` di Bash Windows untuk menampung log (kosong → redirect gagal → **exit 127 palsu** yang terbaca seperti build gagal); pakai path scratchpad yang ditulis penuh.

**✅ URUTAN GERBANG yang dipakai sesi ini (menghindari jebakan `PageProps` yang tercatat 2026-09-11):** `tsc` dijalankan **SEBELUM** `.next` dihapus → exit 0 bersih, nol error `TS2304 PageProps` palsu. Urutan lengkapnya: `tsc` → `npm test` → `rm -rf .next` → `build` → cek env → push → smoke test produksi.

**⚠️ PROSES KERJA REKAN — dua aturan dilanggar (kodenya bagus, jalurnya yang meleset).** (1) Rekan push **langsung ke `dramaku/main`**, bukan lewat branch + `docs/serah-terima/YYYY-MM-DD-<topik>.md` (AGENTS.local.md aturan 1-2) → owner tak punya kesempatan memeriksa sebelum kerjanya masuk main. Kali ini tak ada kerugian (fast-forward bersih, 0 konflik), tapi polanya berbahaya kalau suatu saat isinya tidak sebersih ini. (2) Aturan 7 baru **separuh** jalan: email author sudah beda (`zyyherlambang@gmail.com`), tapi **nama author masih `masradenbagus89-ui`** — `git log --format=%an` masih menjawab "owner" untuk kerja rekan. Perbaikannya di komputer rekan: `git config user.name`.

**Sebelumnya dicek:** 2026-09-12 (**PENCARIAN JUDUL DIPERBAIKI — gaya LK21**). **Lokal = `origin/main` = `dramaku/main` = `f656b12`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Dual push `2498b77..f656b12` (1 commit), fast-forward dipastikan dulu lewat `git merge-base --is-ancestor` pada KEDUA remote sebelum push — nol risiko menimpa. **9 berkas** (5 diubah, 4 baru): `lib/pencarian.ts` (BARU — satu tempat aturan cocok), `lib/discover.ts`, `app/components/beranda/HasilPlayly.tsx` (BARU), `CatalogBrowser.tsx`, `DramaBrowser.tsx`, `app/beranda/page.tsx`, `app/discover/page.tsx`, `tests/pencarian.test.ts` (BARU, 24 penjaga), rencana. Pra-push: `rm -rf .next` + `npm run build` **exit 0** (63 halaman), `tsc` exit 0, **512 tes lulus / 40 berkas** (naik dari 488/39), nol berkas env/kunci ter-stage (dua lapis: nama berkas + isi diff, keduanya NOL). **TAYANG & TERVERIFIKASI** (~1 menit, percobaan 3 dari 6): 7 halaman utama semua **200** · bagian "Video dari Playly" muncul di `/beranda` dengan **8 kartu** (sebelumnya halaman itu tak punya bagian Playly sama sekali) · bundel JS penonton memuat penanda baru, penanda pencocokan lama `synopsis.toLowerCase().includes` **NOL**. **NOL regresi:** `/` tetap **133 poster · 1 h1 · 133× `lg:w-[172px]`**, `/beranda` tetap **56 poster**. **Rollback 1-baris:** `git revert f656b12 && git push origin main`.

**⚠️ MUTATION CHECK 2 ARAH (pola yang layak diulang).** "Tes hijau" tidak membuktikan tesnya menjaga. Kode sengaja dirusak dua kali: (a) pencocokan dikembalikan ke kalimat-utuh → **3 tes MERAH**; (b) perataan aksen dimatikan → **2 tes MERAH**; dikembalikan → 24 hijau lagi. Percobaan mutasi PERTAMA sempat lolos semua dan hampir memberi rasa aman palsu — sebabnya polanya tidak benar-benar terpasang (`node -e` + kutip ganda mengacaukan pola `p{M}`); yang berhasil: skrip `.mjs` lewat heredoc ber-kutip tunggal, dengan hasil mutasi DIPERIKSA ulang sebelum tes dijalankan.

**⚠️ JEBAKAN ALUR KERJA (memakan satu putaran penuh).** Owner memilih "coba di lokal dulu"; preview dinyalakan di `localhost:3201`, tapi owner mengujinya di **dramaapp.vercel.app** yang masih kode LAMA, lalu melapor "masih belum bisa". Tak ada pesan error yang membedakannya. Cara memastikan cepat: bandingkan bundel JS kedua situs terhadap satu penanda teks baru (produksi 0 vs lokal 1) + `git ls-remote`. Petunjuk visual: owner yang terlihat login sebagai **ADMIN** (menu My List · Profile · Admin + tombol Keluar) hampir pasti sedang melihat PRODUKSI, bukan localhost.

**⚠️ `taskkill` TIDAK BISA DIPAKAI dari Bash di komputer ini.** `taskkill //PID <n> //F` menggantung sampai timeout; `taskkill /PID` ditolak karena Git Bash menerjemahkan `/PID` jadi path (`C:/Program Files/Git/PID`). Yang berhasil: **PowerShell** `Stop-Process -Id <n> -Force`. Port yang sudah terpakai di komputer owner per hari ini: 3000-3005, 3010, 3011, 3014, 3050, 3055, 3060, 3076, 3077, 3096, 3097, 3099, 3200 — cek dulu `netstat -ano | grep LISTENING` sebelum memilih port.

**Sebelumnya dicek:** 2026-09-11 (malam, **POSTER KATALOG DIPERBESAR — gaya LK21**). **Lokal = `origin/main` = `dramaku/main` = `fde4c97`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Dual push `7d4258d..fde4c97` (1 commit, fast-forward: kedua remote tertinggal 0 sebelum push, jadi nol risiko menimpa). Lingkup sengaja sempit sesuai permintaan owner ("fokus perbaikan saja"): **4 berkas kode, murni kelas CSS, nol logika data tersentuh** — `shell.ts` (GRID_CLASS jadi bertingkat 104/124/150/**172px**, + `ROW_CARD_CLASS` BARU 116/132/150/**172px**), `FeaturedRow.tsx`, `CatalogCard.tsx`, `ContentRow.tsx`. Pra-push: `rm -rf .next` + `npm run build` **sukses**, `tsc` exit 0, **488 tes lulus / 39 berkas** (angka sama persis rilis sebelumnya = nol regresi), nol berkas env/kunci ter-stage. 3 berkas untracked (`Dokumentasi-Dashboard-DramaKu.xlsx`, `presentasi/*`) sengaja TIDAK ikut. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` `/playly` `/shorts` `/login` `/daftar` semua **200** · penanda baru `lg:w-[172px]` muncul **133x di `/`**, **14x di `/beranda`**, **119x di `/shorts`**, grid `minmax(172px,1fr)` ada di `/beranda` · penanda lama `md:w-28` dan `minmax(110px` **NOL keduanya**. **NOL regresi:** poster `/` tetap **133**, `/beranda` tetap **56** (cocok persis catatan rilis 2026-09-10 & 09-09), `/` tetap **1 h1** (SEO utuh), tautan Daftar tetap **4**. **Rollback 1-baris:** `git revert fde4c97 && git push origin main`.

**⚠️ JEBAKAN GERBANG PRA-RILIS (baru ketahuan, catat!):** urutan resmi di `AGENTS.local.md` butir 6 (`rm -rf .next` → `tsc` → `test` → `build`) **memulangkan 4 error palsu** `TS2304: Cannot find name 'PageProps'` di `app/drama/[id]/page.tsx`, `app/feed/[id]/page.tsx`, `app/watch/[id]/[ep]/page.tsx`. Sebabnya Next 16 MENGHASILKAN tipe rute itu saat build (`.next/types/routes.d.ts`, di-import `next-env.d.ts`) — jadi menghapus `.next` lalu langsung `tsc` = mengetik tanpa tipe yang belum dibuat. **Bukan bug kode.** Urutan yang benar: `rm -rf .next` → **`build` dulu** → `tsc` → `test`. Bukti bukan regresi: `tsc` SEBELUM `.next` dihapus exit 0, dan sesudah build juga exit 0. Ikutan: `next-env.d.ts` berganti sendiri antara `./.next/dev/types/...` (sesudah `npm run dev`) dan `./.next/types/...` (sesudah `npm run build`) — jangan di-commit, build produksi mengembalikannya sendiri.

**⚠️ PREVIEW LOKAL:** port **3010** di komputer owner dipakai proyek LAIN ("Football Bot Dashboard"), bukan dramaapp — jangan mencari preview di situ. Preview sesi ini dijalankan di **3000** lalu dimatikan sebelum build bersih.

**Sebelumnya dicek:** 2026-09-11 (sore, **TAHAP 2 — branch rekan di-merge & TAYANG**). **Lokal = `origin/main` = `dramaku/main` = `c3312f2`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Dual push `ca0e21b..c3312f2` (9 commit: 7 milik rekan + merge + koreksi komentar). Bentrok merge HANYA 2 berkas catatan (`HANDOFF.md`, `antrean-deploy.md`) → diambil versi main sesuai AGENTS.local.md aturan 1; **seluruh kode aplikasi auto-merge bersih** karena sejak `df2316c` main nol menyentuh kode. Pra-push: `tsc` exit 0, **488 tes lulus / 39 berkas** (naik dari 465, +23 penjaga baru), `rm -rf .next` + `npm run build` **exit 0** — build ini **belum pernah bisa diuji rekan** (env Supabase di PC-nya placeholder), nol berkas env/kunci ter-stage (dua lapis). **TAYANG & TERVERIFIKASI** (~30 detik, percobaan 2 dari 10): `/` `/beranda` `/discover` `/playly` `/shorts` `/login` `/daftar` semua **200** · `/playly` **19 video** (dari 20) · `year`/`genre`/`rating` masing-masing **19x** di HTML · `text-blue-400` ada di `/beranda` (menu Playly biru sampai ke penonton). **NOL regresi:** poster `/` **133**, `/beranda` **56**, `/` tetap **1 h1**, keenam menu katalog utuh. **Rollback 1-baris:** `git revert -m 1 e9f4f5b && git push origin main` (merge commit → wajib `-m 1`).

**⚠️ UJI YANG MENENTUKAN BOLEH-TIDAKNYA RILIS INI (pola yang layak diulang).** Fitur `bolehTampilKePenonton()` menyembunyikan video yang berkasnya tak ada di Playly — kalau penilaiannya meleset, SELURUH `/playly` bisa kosong. Diuji memakai **fungsi asli** (bukan tiruan) terhadap **20 video yang saat itu tayang di produksi**: **19 lolos · 1 dibuang · 0 "tidak tahu"**. Video yang dibuang (`1788234998400`) dibuktikan memang rusak: `/api/playly/video?id=1788234998400` di produksi balas **HTTP 502 "Alamat video sedang tidak bisa diambil dari Playly"**, pembandingnya 200 + videoUrl. Endpoint cek-berkas `/api/public-video` (`lib/playly.ts:51`) **publik tanpa kunci** dan **sama persis** dengan yang dipakai pemutar (`lib/playly.ts:1045`) → bisa diuji dari komputer mana pun tanpa kredensial Playly.

**⚠️ KONSEKUENSI yang sudah disampaikan ke owner:** (1) halaman **admin Playly ~7 detik lebih lambat** dibuka — sengaja, ia memeriksa status berkas tiap video tanpa cache (`revalidateSeconds=0`) supaya admin lihat keadaan SEKARANG; halaman penonton TIDAK ikut lambat (cache 5 menit). (2) Kartu Playly kehilangan baris **nama uploader + durasi kedua**, diganti **tahun · genre** + bintang rating — permintaan owner ke rekan. (3) Video `1788234998400` perlu **upload ulang di dashboard Playly** kalau masih mau tayang.

**Sebelumnya dicek:** 2026-09-11 (siang, aturan kerja 2 orang + katalog cadangan disegarkan). **Lokal = `origin/main` = `dramaku/main` = `dce13c3`, selisih NOL** — hash dibandingkan lewat `git ls-remote` LANGSUNG ke server, bukan mengandalkan pesan "berhasil" dari git. Dual push `df2316c..dce13c3`. Pra-push: `tsc` exit 0, **465 tes lulus / 38 berkas** (angka sama persis dengan rilis sebelumnya = nol regresi), nol berkas env/kunci ter-stage (dicek dua lapis: nama berkas + isi diff; satu tangkapan lapis-2 diperiksa dan ternyata placeholder harfiah `terserah-kamu-minimal-8-karakter` di panduan, bukan kunci asli). **Nol kode aplikasi tersentuh** — yang berubah: `AGENTS.local.md` (+8 aturan pembagian kerja owner↔rekan), `docs/panduan-lokal-rekan.md` (BARU), `docs/serah-terima/README.md` (BARU), `data/dramas.json` (katalog cadangan 21 → **42 judul**, diambil dari `GET /api/dramas` yang publik). **PRODUKSI SEHAT sesudah push:** `/` `/beranda` `/discover` `/playly` `/shorts` semua **200**. Karena nol kode aplikasi berubah, penonton tidak melihat perbedaan apa pun — ini memang bukan rilis fitur. **Rollback 1-baris:** `git revert dce13c3 && git push origin main`.

**✅ SUDAH DIGABUNG & TAYANG 2026-09-11 sore (`c3312f2`) — paragraf di bawah ini arsip, pakai entri teratas.** `dramaku/redesign/playly-card` kini **`328274d`**: rekan sudah menarik `main` ke branch-nya sendiri (commit `7496431` "pasang ulang di atas kerja Raden") pada 2026-09-11. Akibatnya **dua kemunduran senyap yang tadinya mengancam sudah HILANG** — diverifikasi langsung ke isi berkas branch: pemutar sudah `import PlaylyPlayer from "./player/PlaylyPlayer"` (bukan lagi `EmbedPlayer`/iframe), dan grid sudah `grid-cols-[repeat(auto-fill,minmax(240px,1fr))]` **identik dengan main**. Selisih sekarang tinggal: branch punya 7 commit yang belum di main, main punya 1 (yaitu `dce13c3` di atas). Isi yang masih berharga & belum ada di main: `bolehTampilKePenonton()` (`lib/playly-publik.ts` — menyaring video yang berkasnya belum ada di Playly), kolom `year`/`genre`/`rating` di kartu, dan berkas tes baru. ⚠️ Tetap perlu `tsc` + `npm test` + `next build` sebelum dirilis; jangan percaya "sudah di-merge" sebagai bukti jalan.

Sebelumnya 2026-09-10 (header menu katalog). **Lokal = `origin/main` = `dramaku/main` = `5ba75e8`, selisih NOL.** Dual push `e8f28d1..5ba75e8`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, **465 tes lulus** (naik dari 440; +25 penjaga baru), `next build` sukses, **nol berkas env/kunci ter-stage** (dicek dua lapis: nama berkas + isi diff). **TAYANG & TERVERIFIKASI** (~60 detik, percobaan 4 dari 12): `/` `/beranda` `/discover` `/playly` `/shorts` `/login` `/daftar` semua **200** · 8 alamat menu (`?cat=` `?kind=` `?negara=` `?year=` `?akses=` `?sub=` `?sort=populer` `?sort=terbaru`) semua **200** · HTML `/` DAN `/beranda` menggambar keenam menu **Genre · Jenis · Populer · Negara · Tahun · Lainnya**, strip kuning memuat pintasan **Film · Gratis · Terbaru · Terpopuler** · `/discover` dibuktikan lewat bundel JS `3oj2c7evdoqd2.js` yang memuat `Paling Banyak Ditonton`, `Episode Terbanyak`, `Pakai Koin`, `Sub Indo`, `Judul A-Z`. **NOL regresi:** kartu poster tetap **133** di `/` dan **56** di `/beranda` (angka sama persis dengan rilis-rilis sebelumnya), tetap **1 h1** (SEO utuh), tombol Masuk & Daftar tetap ada, header hitam lama **0** (sudah dilebur ke bar merah). **Rollback 1-baris:** `git revert 5ba75e8 && git push origin main`.

**⚠️ KOREKSI DATA yang mahal kalau terulang:** isi katalog TIDAK boleh diperiksa dari `data/dramas.json` (cadangan lokal, 21 judul, kolomnya kosong). Katalog sungguhan = Supabase schema **`dramaapp`**, **42 judul**. Query ke schema `public` dijawab `PGRST205 "tabel tidak ada"` dan sempat membuat saya menyimpulkan `year`/`country` kosong — padahal terisi 8 judul, sehingga menu Negara & Tahun akhirnya BISA dibangun. Cara periksa yang benar: `GET /api/dramas` dari aplikasi yang jalan, atau REST Supabase dengan header `Accept-Profile: dramaapp` (`lib/supabase.ts:24`).

**⚠️ JEBAKAN VERIFIKASI LOKAL (bukan produksi):** (1) **`rm -rf .next` dulu** sebelum `next build` saat memverifikasi tampilan — build inkremental tetap menyajikan halaman statis LAMA, sehingga perubahan terlihat "tidak masuk" padahal kodenya benar; (2) **`pkill` tidak berlaku di Windows** — server uji lama tetap hidup, server baru gagal `listen` (EADDRINUSE errno -4091), dan curl dijawab server LAMA; pakai `netstat -ano | grep LISTENING | grep :PORT` lalu `taskkill //PID x //F`, DAN pastikan log server benar-benar "Ready".

**⚠️ SISA DI KOMPUTER OWNER (belum dibereskan, menunggu izin):** port **3055** diduduki proses `next` macet dari sesi lama (hidup tapi balas HTTP 000) sehingga `start-localhost-3010.bat` gagal menyala; ditambah **3 server uji milik sesi ini** di port 3096/3097/3099 yang gagal dimatikan. Server pengganti untuk owner dinyalakan di port **3050**.

Sebelumnya 2026-09-09 (menu pemutar Playly). **Lokal = `origin/main` = `dramaku/main` = `aca84f1`, selisih NOL.** Dual push `77b4e6e..aca84f1`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, **440 tes lulus** (naik dari 424; +16 penjaga baru), `next build` sukses, nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` `/playly` `/shorts` semua **200** · endpoint baru `/api/playly/video` di produksi: id sah **200 + videoUrl**, id asing **404 "Video tidak tersedia"**, id `../../etc` **400 "Id video tidak valid"** — jadi gerbang IDOR benar-benar jalan di produksi, bukan cuma di tes · bundel JS `/playly` memuat penanda menu baru (`Pencahayaan sinematik`, `PiP / Picture in Picture`, `Volume Stabil`, `Penguat suara`, `Menu pemutar`, `Posisi video`, `api/playly/video`) dan **NOL** penanda pemutar iframe lama (`encrypted-media`, `allow-presentation`, `Memuat pemutar video`, `Alamat video ditolak karena bukan https`) → pemutar Playly benar-benar berganti di sisi penonton · `/discover` ikut memakai pemutar baru, dan grid poster katalognya diverifikasi TIDAK ikut berubah (`minmax(110px,1fr)` masih ada). Deploy Vercel ~45 detik (percobaan 1-2 masih 404, percobaan 3 sudah 200). **Rollback 1-baris:** `git revert aca84f1 && git push origin main`.

**⚠️ JEBAKAN VERIFIKASI (sama seperti /discover, terulang lagi):** grep HTML `/playly` TIDAK bisa membuktikan pergantian pemutar ini — baik iframe lama maupun pemutar baru sama-sama baru muncul SESUDAH kartu diklik (dirakit di browser), jadi HTML awal selalu 0 iframe di kedua versi. Yang sah sebagai bukti: penanda di `/_next/static/chunks/*.js` + balasan endpoint API.

**⚠️ KONSEKUENSI BISNIS (disetujui owner):** video Playly tak lagi lewat pemutar resmi mereka, jadi hitungan tayang di dashboard mitra bisa berhenti bertambah. Kalau mitra menanyakan angka tayang yang turun, ini sebabnya — bukan bug.

Sebelumnya 2026-09-09 (Shorts jadi baris poster). **Lokal = `origin/main` = `dramaku/main` = `74ab06c`, selisih NOL.** Dual push `d9fc943..74ab06c`, hash dibandingkan lewat `git ls-remote`. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` `/shorts` `/playly` semua 200; `/shorts` produksi = **119 kartu poster** dalam 5 baris (dari 6 kartu besar), **0** tautan nyasar ke /drama, kartu besar lama NOL. Halaman lain tak berubah tujuan kliknya: `/` 133 -> /drama & 0 -> /feed; `/beranda` 56 -> /drama & 0 -> /feed. Beda dari /discover & /playly, halaman ini BISA diverifikasi lewat HTML (digambar server). Deploy ~2 menit (percobaan 1-2 masih 6 kartu).

Sebelumnya 2026-09-09 (kartu Playly diperkecil). **Lokal = `origin/main` = `dramaku/main` = `f7ff444`, selisih NOL.** Dual push `ba1e09e..f7ff444`, hash dibandingkan lewat `git ls-remote`. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. Lingkup sengaja 1 berkas (`PlaylyVideoGrid.tsx`, +7 baris) sesuai permintaan owner. **TAYANG:** `/playly` & `/discover` 200; penanda `auto-fill,minmax(240px,1fr)` DITEMUKAN di bundel JS produksi `/_next/static/chunks/2_aipsf02mu_7.js`. **Jebakan verifikasi yang SAMA dengan /discover terulang di /playly** — grep HTML memulangkan 0 karena grid Playly dirakit di BROWSER; dibuktikan bukan regresi: kelas LAMA (`lg:grid-cols-4`) juga tidak ada di HTML. Grid poster diverifikasi TIDAK ikut berubah (`minmax(110px,1fr)` masih ada di /beranda & /discover).

Sebelumnya 2026-09-09. **Lokal = `origin/main` = `dramaku/main` = `cac87bd`, selisih NOL** (/discover ikut tampilan katalog: lebar penuh, poster padat, hero dibuang). Dual push `a466721..cac87bd`, hash dibandingkan lewat `git ls-remote`. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG:** `/` `/beranda` `/discover` semua 200; `/` & `/beranda` HTML sudah memuat bar magenta.

**⚠️ JEBAKAN VERIFIKASI (penting, catat!):** `/discover` TIDAK bisa diverifikasi lewat grep HTML. Halaman itu membungkus `DramaBrowser` dalam `<Suspense>` (pakai `useSearchParams`), jadi server HANYA mengirim teks fallback "Memuat..." — isi katalognya dirakit di BROWSER. Grep HTML memulangkan **0 poster** dan bikin seolah rilisnya gagal. Ini perilaku LAMA, bukan regresi: dicek ke `git show a466721:app/discover/page.tsx`, pola Suspense/"Memuat..." sudah ada di sana. Cara verifikasi yang BENAR untuk halaman ini: cari penandanya di berkas JS `/_next/static/chunks/*.js` (terbukti `from-fuchsia-700 via-rose-600` ADA di `171-kei2elpv3.js`), atau buka di browser sungguhan.

**Bukti filter URL masih utuh** (diuji di dev server, bukan sekadar HTTP 200): jumlah poster cocok persis isi database — `/discover` 42 · `?cat=Action` 21 · `?cat=Romance` 14 · `?cat=Tycoon` 4.

Sebelumnya 2026-09-08 (malam, iklan beranda dihapus). **Lokal = `origin/main` = `dramaku/main` = `7f12903`, selisih NOL** (3 slot iklan dihapus dari /beranda atas permintaan owner). Dual push `eba7ec2..7f12903`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` `/profile` semua 200; `/beranda` produksi label "Iklan" = **0**, sementara 56 kartu poster + baris unggulan + grid katalog tetap utuh; `/drama/<id>` tetap punya **1** banner (sengaja tidak disentuh). Deploy Vercel lebih lambat dari biasa: percobaan 1 & 2 masih 3 banner, percobaan 3 baru 0 — perlu ~2 menit, bukan 1.

Sebelumnya 2026-09-08 (malam, poster dirapatkan). **Lokal = `origin/main` = `dramaku/main` = `8333bdc`, selisih NOL** (poster dirapatkan: halaman depan 92 -> 133 kartu). Dual push `09c65a9..8333bdc`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; produksi = **133 kartu poster** di halaman depan (dari 92) & **56** di /beranda (dari 38); kartu `w-20/24/28` + jarak `gap-1.5` terpasang; grid memakai `auto-fill,minmax(110px,1fr)` dengan NOL sisa kolom-dipatok; tetap 1 h1. Deploy Vercel ~1 menit: percobaan 1 masih 92, percobaan 2 sudah 133.

Sebelumnya 2026-09-08 (malam, lebar penuh). **Lokal = `origin/main` = `dramaku/main` = `1b7729d`, selisih NOL** (baris poster melebar sampai tepi layar + poster diperkecil). Dual push `cefe5bc..1b7729d`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; CSS produksi `/_next/static/chunks/3jiqbnsc0528u.css` berisi **`.shell-wide{max-width:100%}`** (batas 1440px benar-benar lepas di sisi penonton, bukan cuma di kode); HTML memakai kelas kartu baru `w-24 sm:w-28 md:w-32` dengan NOL sisa ukuran lama; 92 kartu poster dalam 6 baris; tetap 1 h1. Deploy Vercel ~1 menit: percobaan 1 masih versi lama, percobaan 2 sudah baru. **Catatan cara verifikasi:** CSS Next.js ada di `/_next/static/chunks/*.css`, BUKAN `/_next/static/css/*.css` — pola grep lama meleset dan sempat memberi kesan "tidak ketemu".

Sebelumnya 2026-09-08 (malam, katalog). **Lokal = `origin/main` = `dramaku/main` = `d89ef3f`, selisih NOL** (halaman depan berubah dari halaman PROMOSI jadi halaman KATALOG). Dual push `9fdb343..d89ef3f`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 424 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; halaman depan produksi kini **92 kartu poster** (sebelumnya 14) dalam 6 baris: Film unggulan, Drama Terbaru, Paling Banyak Ditonton, Drama Action, Drama Romance, Drama Tycoon. Keempat seksi promosi lama NOL. Tetap tepat 1 h1 (SEO utuh) + tombol Daftar Gratis ada. Deploy Vercel ~1 menit: percobaan 1 masih 14 kartu, percobaan 2 sudah 92.

Sebelumnya 2026-09-08 (malam). **Lokal = `origin/main` = `dramaku/main` = `6447db7`, selisih NOL** (ajakan daftar di halaman depan dirampingkan jadi strip tipis di bawah baris poster). Dual push `91bb457..6447db7`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 416 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; halaman depan produksi = **tepat 1 h1** (SEO utuh), 14 kartu poster, kalimat "42 judul dalam 6 kategori", tombol Daftar Gratis + Masuk ada; judul serif besar, 3 kotak statistik, dan lencana sudah NOL. `function Stat()` dihapus (nol pemakai). Deploy Vercel ~1 menit: percobaan 1 masih versi lama, percobaan 2 sudah baru. **⚠️ TEMUAN SAMPINGAN (belum ditangani, di luar lingkup permintaan):** saat `next build` muncul `[playly] katalog publik gagal: Playly membalas error (HTTP 500)` 2x — bagian "Video dari Playly" di /discover akan kosong. Situs TIDAK rusak (kegagalannya ditangani, halaman tetap 200), tapi perlu dicek terpisah.

Sebelumnya 2026-09-08 (lanjutan). **Lokal = `origin/main` = `dramaku/main` = `88fbe18`, selisih NOL** (baris FILM UNGGULAN diisi penuh: 5 -> 14 poster). Dual push `f2b91c4..88fbe18`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 416 tes lulus, `next build` sukses. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; halaman depan produksi kini **14 kartu poster** (sebelumnya 5, sisa lebar kanan kosong), 6 tautan genre, bar cari magenta ada, nol carousel berjalan. `/discover` SENGAJA tidak diubah & terbukti masih punya hero sinematiknya (`min-h-[80svh]` 1x, carousel 1x). Deploy Vercel ~1 menit: percobaan pertama masih 5 kartu, kedua sudah 14.

Sebelumnya 2026-09-08. **Lokal = `origin/main` = `dramaku/main` = `72fabb3`, selisih NOL** (halaman sebelum login ikut struktur katalog: bar cari + strip genre + poster di layar pertama, blok sambutan sehalaman dipindah ke bawah). Dual push `b7dd2ee..72fabb3`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. Pra-push: `tsc` 0 error, 416 tes lulus, `next build` sukses, nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; urutan HTML `/` produksi = header 5919 -> bar cari magenta 7449 -> strip genre 9039 -> baris unggulan 10539 -> ajakan daftar 30122, dengan 5 kartu poster + 6 tautan genre + nol carousel. Deploy Vercel butuh ~1 menit: percobaan pertama masih versi lama (1/4 penanda), percobaan kedua 4/4. Tautan baru diverifikasi TIDAK membuka halaman tertutup — `/discover` & `/drama/<id>` memang publik (200 tanpa cookie login).

Sebelumnya 2026-09-07 (malam). **Lokal = `origin/main` = `dramaku/main` = `39f45f1`, selisih NOL** (halaman depan `/` lepas dari hero berjalan + kolom `status` tersambung dari panel admin sampai database). Dual push `ced64d3..39f45f1`, hash dibandingkan lewat `git ls-remote` LANGSUNG ke server. **MIGRASI DB DIJALANKAN LEBIH DULU** (disetujui owner): `dramaapp.dramas` + kolom `status` nullable + `dramas_status_check`; 42 drama utuh sebelum & sesudah; PostgREST sudah reload schema. Pra-push: `tsc` 0 error, 416 tes lulus, `next build` sukses, nol berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` `/admin` semua 200; HTML `/` produksi memuat baris "Film unggulan" + tombol "Lihat semua film unggulan", dan **nol** `aria-roledescription="carousel"` serta nol kelas animasi hero — jadi hero benar-benar berhenti berjalan di mata penonton. Tombol "Daftar Gratis" tetap ada. **Sisa:** menyimpan drama dari panel admin dengan isian Status belum dites end-to-end (menulis data produksi, menunggu owner).

Sebelumnya 2026-09-07. **Lokal = `origin/main` = `dramaku/main` = `5d0dfb0`, selisih NOL** (beranda dirombak jadi katalog grid padat ber-paginasi; hero yang berganti sendiri diganti baris FILM UNGGULAN atas permintaan owner "jangan berjalan lagi"). Dual push sukses ke DUA remote (`2af46b0..5d0dfb0`); hash dibandingkan ulang lewat `git ls-remote` LANGSUNG ke server, bukan mengandalkan pesan "berhasil" dari git. Catatan: `git fetch dramaku` sempat gagal `schannel: failed to receive handshake` — gangguan SSL sesaat, push ulang berhasil. Pra-push: `tsc --noEmit` 0 error, 410 tes lulus, `next build` sukses, tak ada berkas env/kunci ter-stage. **TAYANG & TERVERIFIKASI:** `/` `/beranda` `/discover` semua 200; HTML `/beranda` produksi memuat bar cari magenta (7533), strip genre (17029), baris unggulan (18627), grid `2xl:grid-cols-8` (41461), teks "Halaman 1 dari 2", slot iklan tetap **3** — dan penanda versi lama `min-h-[80svh]` serta `aria-roledescription="carousel"` sudah TIDAK ADA. Jadi hero benar-benar berhenti berjalan di mata penonton, bukan cuma ter-commit. Berkas `Dokumentasi-Dashboard-DramaKu.xlsx` SENGAJA tidak ikut di-commit (bukan bagian pekerjaan ini, masih untracked).

Sebelumnya 2026-09-03 (sore). **Lokal = `origin/main` = `dramaku/main` = `68741a7`, selisih NOL ✅** (iklan balik melintang di bawah baris film, gaya IDLIX — pembatalan kolom kanan `ddbbcc4` atas permintaan owner). Dual push sukses ke DUA remote (`ddbbcc4..68741a7`), hash dibandingkan ulang sesudah `git fetch`, bukan mengandalkan pesan "berhasil". Pra-push: `tsc` 0 error · 390 tes lulus · `next build` sukses. **TAYANG & TERVERIFIKASI:** HTML `/beranda` di `dramaapp.vercel.app` ~2 menit sesudah push sudah TIDAK mengandung `--ad-rail-w` (variabel lebar kolom iklan kanan — penanda versi lama), dan urutan blok terbaca: slot iklan SESUDAH "Trending Drama" lalu "Drama Terbaru", slot iklan SESUDAH "Drama Populer" lalu "Rating Tertinggi". Jadi iklan benar-benar melintang di bawah baris, bukan cuma ter-commit.

Sebelumnya 2026-09-02 (sore). **Lokal = `origin/main` = `dramaku/main` = `8ff07e9`, selisih NOL ✅** (kesejajaran logo · judul hero · label film di satu garis kiri; dual push, hash dibandingkan sesudah `git fetch` ulang). **Cek kesehatan produksi:** `/` · `/beranda` · `/discover` · `/shorts` · `/playly` semua **200** · HTML landing produksi: header `flex h-16 … px-4 md:px-6` dan hero `relative flex min-h-[70svh] … px-4 md:px-6` — **nol** sisa `max-w-7xl` di keduanya, label film tetap `left-4 md:left-6` (patokan). Tayang ~60 detik sesudah push.

Sebelumnya 2026-09-02 (siang). **Lokal = `origin/main` = `dramaku/main` = `5aa8344`, selisih NOL ✅** (dual push judul hero rata kiri; `git fetch` ulang sesudah push, hash dibandingkan satu per satu — bukan mengandalkan pesan "berhasil"). **⚠️ Remote `official` (`projectraden/backup-dramaapp`) MATI** — `git fetch official` balas `Repository not found`, jadi baris `official` di tabel bawah sudah tidak berlaku sampai repo-nya dibuat/diberi akses ulang. **Cek kesehatan produksi 2026-09-02:** `/` · `/beranda` · `/discover` · `/shorts` semua **200** · HTML landing produksi berisi `max-w-7xl … items-start … text-left` + 2× `justify-start` + `ellipse_at_left` = perubahan hero benar-benar sampai ke penonton, bukan cuma ter-commit.

Sebelumnya 2026-08-31. **`origin/main` (repo produksi) = lokal = `3dad2e8` ✅ — tak ada rilis tertinggal.** **⚠️ `dramaku/main` masih `ee8f18c` — commit `3dad2e8` (migrasi Supabase) BELUM ter-push ke repo cermin** (aturan dual push). Sebabnya bukan kode: `git ls-remote dramaku` ditolak GitHub `Invalid username or token` → kredensial `ojokesusu/dramaku` di PC ini kedaluwarsa. Push ke `dramaku` TIDAK merilis apa pun, jadi penonton tak terdampak; yang berisiko cuma rekan bekerja di atas kode lama. **Cek kesehatan produksi 2026-08-31:** landing **200** · `/playly` **200** · `/discover` **200** · `/api/teaser` **307 / 0 byte** → tunnel baru `interference-positions-style-manufacture.trycloudflare.com` · redirect diikuti balas **206 `video/mp4` `ftypisom`** · `/api/dramas` **200 berisi 42 judul** (bukti database Supabase project baru + schema `dramaapp` sudah melayani produksi).

Sebelumnya 2026-08-29. **Lokal = `origin/main` = `dramaku/main` = `7a440c2`, selisih NOL** (`7a440c2` = commit catatan handoff di atas `53e923b`, sudah ter-push ke dua remote). Tidak ada rilis tertinggal. **Cek kesehatan produksi 2026-08-29:** landing **200** · `/api/teaser` **307 / 0 byte** → tunnel baru `ping-newspapers-damaged-dublin.trycloudflare.com` · redirect diikuti balas **206 `video/mp4`** (byte video tetap mengalir langsung tunnel→penonton, kuota Vercel aman).

Sebelumnya 2026-08-28 (deploy hero landing): **Lokal = `origin/main` = `dramaku/main` = `53e923b`, selisih NOL.** Hero landing "hidup" (cuplikan 60 dtk/judul) TAYANG & terverifikasi (markup `70svh` di produksi 45 dtk sesudah push; `/api/teaser` produksi 307 / 0 byte → tunnel).

> ⚠️ **NAMA REMOTE BEDA ANTAR-KOMPUTER — inilah sumber salah kirim yang berulang.**
> Bukan salah satu catatan yang keliru; keduanya benar untuk mesinnya masing-masing.
> Dicek langsung dengan `git remote -v`:
>
> | Repo | Nama di **PC owner** (dicek 2026-08-26) | Nama di **PC rekan** | Dipantau Vercel? |
> |---|---|---|---|
> | `masradenbagus89-ui/dramaapp` | **`origin`** | `dramaapp` | **Ya — ini tombol rilis** |
> | `ojokesusu/dramaku` | **`dramaku`** | `origin` | Tidak — push ke sini TIDAK merilis apa pun |
> | `projectraden/backup-dramaapp` | `official` | — | Tidak — cadangan |
>
> **SELALU `git remote -v` dulu sebelum push. Jangan percaya kata "origin" di catatan mana pun.**

**Catatan lama, 2026-08-25** (sesudah owner login GitHub di PowerShell, `git fetch` dari sesi AI JALAN lagi). Keadaan saat pemeriksaan: repo produksi = `c648fef` — sudah berisi fitur Film & **terverifikasi tayang**; `ojokesusu/dramaku` = `fd83427` — berisi 5 commit Playly yang belum pernah masuk produksi. Keduanya digabung hari itu di komputer owner.

⚠️ **Penamaan remote BEDA antara komputer owner dan komputer rekan** (sumber salah paham): di sini `origin` = repo produksi & `dramaku` = repo rekan; di catatan rekan (baris Playly di bawah) `origin` justru berarti repo `dramaku`, dan repo produksi mereka sebut `dramaapp`. Selalu sebut URL-nya kalau ragu.

**Catatan sebelumnya:** 2026-08-24 sore WIB (`git fetch origin` + `git fetch dramaku` dua-duanya SUKSES; `HEAD` = `origin/main` = `dramaku/main` = **`ba82058`**, selisih NOL)

## 2026-09-22 — RILIS `5d37465` — ✅ deret TAB katalog gaya LK21 TAYANG

- **Terdorong ke:** `dramaku` ✅ dan `origin` ✅ (fast-forward).
- **Terverifikasi tayang di https://dramaapp.vercel.app:** keenam tab (TERBARU · SERIES UNGGULAN · SERIES UPDATE · TERPOPULER · REKOMENDASI · 2026), tombol **Filter**, judul bagian **Terbaru Ditambahkan**, dan tombol **Semua** — semuanya ada di HTML server.
- **Yang masih menunggu owner (opsional, bukan penghalang):** centang "Tandai sebagai Unggulan" di panel admin & isi status "Masih tayang". Selama belum diisi, kedua tab itu terisi otomatis (serial berbayar koin / serial terbaru) — tidak ada tombol mati.
- **Catatan angka penonton:** 39 dari 41 judul masih "1.0K", jadi tab TERPOPULER belum benar-benar membedakan. Owner bisa mengisi angka sebenarnya lewat kolom "Views" di panel admin.

## 2026-09-22 — RILIS `f3d4a28` — ✅ LENCANA LENGKAP TAYANG + PANEL ADMIN SEMBUH

- **Terdorong ke:** `dramaku` ✅ dan `origin` ✅ (fast-forward).
- **Terverifikasi tayang di https://dramaapp.vercel.app:** 9 lencana CAM merah, 123 HD hijau, 113 EPS, rating `5.2 7.2 7.3 7.6 7.8 8.1 9.1`, tahun `2006 2008 2017 2024 2025 2026`, durasi `01:42 … 02:45`.
- **Panel admin SEMBUH — dibuktikan, bukan disimpulkan:** payload gaya `dramaToRow` sekarang → **HTTP 200 diterima, data utuh**; payload lama yang menyertakan `quality` → **HTTP 400 ditolak** (itulah penyebab rusaknya sejak `f4c43a7`).
- **TIDAK ADA utang SQL lagi.** Kolom `quality` tidak jadi dibuat; kualitas disimpan di `app_data` (lihat `lib/kualitas-drama.ts` untuk 3 batasnya + cara naik kelas). `supabase_migrations/add_quality_to_dramas.sql` disimpan sebagai jalur naik kelas, BUKAN syarat.
- **Catatan akses:** password database di berkas Downloads sudah tidak berlaku sejak sekitar 2026-09-16 — jalur `psycopg2` yang dipakai migrasi `status` (2026-09-07) kini mati. Jalur pengganti untuk perubahan DATA: `scripts/isi-lencana-lewat-rest.mjs`. Untuk perubahan STRUKTUR: masih perlu akses dashboard/password.

## 2026-09-22 — RILIS `f4c43a7` lencana poster LK21 — ✅ TAYANG, ⛔ ADA UTANG SQL

- **Terdorong ke:** `dramaku` ✅ dan `origin` ✅ (fast-forward `652d0ad` → `f4c43a7`, nol commit tertinggal di kedua remote).
- **Terverifikasi tayang:** https://dramaapp.vercel.app — 7 durasi film, 113 lencana EPS, 6 rating bintang, `SUB INDO` = 0.
- **⛔ UTANG YANG HARUS DILUNASI OWNER:** jalankan `supabase_migrations/add_quality_to_dramas.sql` lalu `supabase_migrations/isi_lencana_awal_dramas.sql` di Supabase → SQL Editor. **Sampai itu dilakukan, tombol Simpan di panel admin GAGAL** (kolom `quality` belum ada; terbukti `42703` dari produksi). Situs publik tidak terpengaruh.
- **AI tidak bisa menjalankannya sendiri:** password database di berkas Downloads sudah tidak berlaku (`password authentication failed`), tidak ada Supabase CLI maupun Personal Access Token. Rinciannya di `HANDOFF.md` seksi paling atas.

## Siapa memantau apa

> ✅ **DIKOREKSI 2026-08-26 — peringatan lama di sini SALAH.** `git remote -v` hari ini membuktikan
> nama remote TIDAK tertukar dan `dramaku` MASIH ADA (`git fetch dramaku` sukses, menarik `7372259..fc34161`).
> Yang berlaku sekarang adalah tabel di bawah. Tetap jalankan `git remote -v` dulu sebelum push.

| Repo | Remote git | Dipantau Vercel? |
|---|---|---|
| `masradenbagus89-ui/dramaapp` | **`origin`** | **Ya** — `git push origin main` = tombol rilis |
| `ojokesusu/dramaku` | **`dramaku`** | Tidak — cermin/tempat rekan commit. Push ke sini TIDAK merilis apa pun |
| `projectraden/backup-dramaapp` | `official` | Tidak — cadangan |

Produksi: https://dramaapp.vercel.app  
Commit terbaru yang di-push: **`4954817`** (5 commit sekaligus: perbaikan layar hitam player, berkas autostart named tunnel, tes e2e Tahap 7, 2 dokumentasi) — di-push 2026-08-20 malam sesudah 265 tes lulus + `tsc` exit 0 + `next build` sukses. **TERVERIFIKASI TAYANG**: teks perbaikan player ditemukan di bundle produksi `/_next/static/chunks/27z9f9ucdybcg.js`.
Catatan: `git fetch` ke `origin` **dan** `dramaku` dua-duanya SUKSES 2026-08-19 sore (timeout `dramaku` pagi tadi tidak kambuh).
Tahap 6 (`b48bf32`): **sudah diverifikasi owner jalan di produksi** 2026-08-18.
AUTH_SECRET di Vercel: dikonfirmasi ADA oleh owner 2026-08-18.

## Antrian sekarang

| Status | Commit | Isi | Aksi |
|---|---|---|---|
| ✅ **TAYANG & TERVERIFIKASI 2026-08-28** | `53e923b` | **Hero landing "hidup"**: komponen baru `LandingHero` — cuplikan ep 1 berputar antar 5 judul unggulan (`featuredHeroSlides`), ganti tiap **60 detik**, tombol suara + label judul; lapisan gelap ditipiskan (video cerah), kolase poster statis dihapus | Permintaan owner 2026-08-28. Jalur video TIDAK berubah (tetap `/api/teaser` 307 — nol byte video lewat Vercel). Pra-push: 390 tes lulus, `tsc` exit 0, `next build` sukses. Dual push SUKSES (`origin` + `dramaku`, `222eaf6..53e923b`). **Bukti tayang:** markup hero baru (`70svh`) muncul di `dramaapp.vercel.app` 45 dtk sesudah push; `/api/teaser` produksi balas **307 / 0 byte** → tunnel |
| ✅ **TAYANG & TERVERIFIKASI 2026-08-27 sore** | `f17b528` + `8a41ae4` + `a242921` | **Perbaikan kuota Vercel**: `/api/teaser` + `/api/download` dari penyalur byte jadi 307 redirect; `TEASER_BYTES` mangkrak dibuang; hero `preload="metadata"` + jeda 1,2 dtk; batas cuplikan hover 10 dtk; 14 tes penjaga baru | Deployment `8a41ae4` tak pernah dibangun (terblokir saat akun paused). AI mengirim commit pemicu kosong `a242921` (di atas `8a41ae4` — **Playly TIDAK ikut tayang**) atas permintaan owner → Vercel membangun. **Bukti tayang:** teaser **307 / 0 byte** → tunnel `optical-comprehensive-harper-howto.trycloudflare.com`; redirect diikuti balas **206 `video/mp4` `ftypisom`**. Catatan: `origin/main` kini `a242921`, **di depan** lokal & `dramaku` (masih `2b9f530`/`fc34161`) — merge balik nanti saat rilis berikutnya |
| ✅ **DIRILIS & TERVERIFIKASI TAYANG 2026-08-27 malam** | `9e17f40` + `fc34161` → merge `14fa0cc` | Fitur rekan **"video Playly tampil otomatis"** — halaman `/playly`, baris di `/discover`, tautan TopNav, admin bisa menyembunyikan; jalur katalog-publik-tersaring supaya kunci mitra yang dicabut tak mematikan video | Owner menyetujui rilis 2026-08-27 malam (kuota sudah terbukti aman + penyisiran penuh tuntas) — **keputusan sadar, bukan efek samping merge** (koreksi atas dugaan di catatan rekan). `origin/main` (`a242921`) di-merge balik ke lokal → `14fa0cc`, di-push ke `origin` (`a242921..14fa0cc`) **dan** `dramaku` (`9feb4e1..14fa0cc`) — ketiganya selisih NOL. **Bukti tayang:** `/playly` balas **200** ±1 menit sesudah push (sebelumnya 404); diverifikasi rekan dari sisi mereka: halaman memuat **keempat video `coklat`** (Transformers 8, Transformers The Last Knight, Hulk Abu-abu, Suara Hewan), dan kunci mitra `plyk_…` **diterima lagi** (`{"ok":true,"count":4}` — catatan "invalid_key 25 Agt" basi). Audit lama tetap berlaku: nol pola penyalur byte, video lewat `<iframe>` ke `playly-dashboard.vercel.app` (bukan Vercel kita), thumbnail pakai `<img>` biasa. ⚠️ Push dari PC rekan tetap **403** (`denied to yusufscorpio`) — jalan keluar permanen: tambahkan `yusufscorpio` sebagai collaborator Write; kalau sudah bisa, perintah yang benar = `git push dramaapp origin/main:main`, JANGAN `fix/playly-otomatis:main` (non-fast-forward, menghapus perbaikan kuota) |
| 📋 **catatan rekan (bukti dari sisi mereka)** | `9e17f40` (branch `fix/playly-otomatis`) | **Video Playly tampil OTOMATIS**: halaman baru `/playly`, baris di `/discover`, tautan TopNav, admin bisa menyembunyikan. Jalur katalog-publik-TERSARING supaya kunci mitra yang dicabut tak lagi mematikan video | Diperiksa dulu: **376 tes lulus**, `tsc` exit 0, `next build` sukses, nol secret di diff. Terbukti lokal: 4 video milik `coklat` tampil, **nol kebocoran** dari 11 video kreator lain, keempat berkas MP4 mengalir (206, `ftypisom`, `[ftyp,free,mdat]`). `/discover` tetap Static 1m (nol regresi). **Push ke `ojokesusu/dramaku` SUKSES** `7372259..9e17f40` (2026-08-26, diverifikasi). **Push ke `masradenbagus89-ui/dramaapp` DITOLAK 403** — git di PC ini login sebagai `yusufscorpio` yang tak punya akses tulis (dicek lewat `git push --dry-run`, tidak mengubah apa pun). Owner jalankan: `git push dramaapp fix/playly-otomatis:main` |
| ✅ **RILIS GABUNGAN TAYANG** | `e765e29` (merge) | **Fitur Film + integrasi Playly dirilis bersama** 2026-08-25 atas izin owner | Dual push SUKSES dari sesi AI: `origin` `c648fef..e765e29`, `dramaku` `fd83427..e765e29`. Sesudah `git fetch` keduanya: lokal = `origin/main` = `dramaku/main` = `e765e29`, **selisih NOL**. Sebelum push: **358 tes lulus** (302 + tes Playly), `tsc` exit 0, `next build` sukses (5 route Playly terdaftar), nol secret nyata di diff (hanya kunci contoh di tes). **Terverifikasi tayang** di deployment `dpl_28aUguP18E7FpHXZ4grMFFi3Tp5w`: `/admin/videos/playly` **200** (sebelumnya 404), dan chunk `/_next/static/chunks/3om_miassjcgs.js` memuat "Jenis tayangan" + "Film selalu gratis" + "Playly" sekaligus |
| ✅ **sudah di-push & TAYANG** | `5156949` + `c648fef` | Fitur **Film tanpa episode** di panel admin (jenis tayangan Serial/Film) + kolom DB `kind` + 2 berkas tes baru | Owner memberi izin rilis 2026-08-25. SQL `add_kind_to_dramas.sql` dijalankan owner di Supabase produksi (Success) SEBELUM push. Push ke `origin` dilakukan owner sendiri di PowerShell (sesi AI tak bisa membuka dialog login GitHub): `617f6a0..c648fef`. **Terverifikasi tayang**: teks "Jenis tayangan", "Film selalu gratis", "1 video utuh" ditemukan di bundle produksi `/_next/static/chunks/32zoz1a8f4vl2.js`. Sebelum push: nol secret di diff, **302 tes lulus**, `tsc` exit 0, `next build` sukses |
| ⏸️ **menunggu owner (boleh kapan saja)** | — | SQL data-fix `supabase_migrations/mark_existing_movies.sql` — menandai 7 judul film lama (Transformers, Spider-Man, Avengers, Predator, 28 Years Later, Fireworks Wednesday, The Dark Knight) sebagai `kind = 'movie'` + melepas tanda berbayar yang memang tak berefek | Supabase → SQL Editor → tempel & Run. Boleh sebelum atau sesudah deploy (kode lama mengabaikan kolom `kind`, jadi tidak merusak apa pun) |
| ✅ **penghalang 403 TERBUKA & DIRILIS 2026-08-25** | `0e7a5c5` (asal) → `e765e29` | Integrasi Playly + perbaikan yang membuatnya jalan (pola `/id/{id}/embed`, alamat relatif, katalog publik saat kunci mitra ditolak) | Rekan tak bisa push ke repo produksi (403, `gh` login sebagai `yusufscorpio` tanpa akses tulis). Owner memberi izin 2026-08-25 → 5 commit `dramaku` digabung ke `main` di komputer owner (konflik hanya di 3 berkas catatan; kode nol bentrok), diperiksa ulang, lalu dirilis bersama fitur Film. Status akhir push dicatat di baris paling atas tabel ini sesudah rilis |
| ⚠️ **kunci Playly TIDAK SAH lagi** | — | `DASHBOARD_API_KEY` (`plyk_…`) diuji ulang 2026-08-25 → `invalid_key`. Dulu tercatat "SAH 2026-08-19" | Jalur embed **tidak terhalang** (turun ke katalog publik). Kartu "Video terbaru" di `/discover` **masih kosong** sampai ada kunci baru dari pengelola Playly |
| ✅ **sudah di-push** | `ba82058` | Tolak `api.trycloudflare.com` sebagai alamat video (2 lapis: saringan di `start-video-services.ps1` + `HOST_TERLARANG` di `lib/video-base.ts`) + `HANDOFF.md` siklus ke-4 | Owner memberi izin 2026-08-24. Dual push SUKSES (`origin` + `dramaku`, selisih nol). Diperiksa dulu: nol secret di diff, **283 tes lulus**, `tsc` exit 0, `next build` sukses, script PS lolos parser. Sesudah push: raw GitHub dicocokkan persis dengan berkas yang diuji; situs `/beranda` 200 & `/api/teaser` 206 |
| ✅ **video PULIH (siklus ke-4)** | — | Akarnya PEMASANGAN, bukan kode: `start-video-services.ps1` tidak ada di PC backup + watchdog 15 menit belum pernah dibuat. Keduanya dibereskan; alamat aktif `boats-voluntary-ensure-kim.trycloudflare.com` (akan berganti — jangan dihafal) | **Terverifikasi 2026-08-24 dari jaringan LAIN:** `/api/teaser` **206** di ep 1/27/56, `video/mp4`, signature `ftypmp42`, root tunnel **200**. Owner mencoba sendiri: video tayang. Sejak sekarang watchdog memulihkan sendiri ≤15 menit |
| ✅ **video PULIH** | — | Tunnel sore (`written-coated-...`) LENYAP (DNS `Non-existent domain`) → owner jalankan `start-dramaapp.ps1` → alamat baru **`proxy-marks-isolation-subjects.trycloudflare.com`**. Langkah [5/6] gagal 403 (`VERCEL_TOKEN` mati), alamat masuk lewat jalur manual | **Terverifikasi 2026-08-20 malam:** URL yang dipakai produksi balas **206** `video/mp4`, isi diawali `ftypmp42`. ⚠️ Sementara — mati lagi saat PC backup restart |
| ✅ **sudah di-push** | `8dd6f22`..`4954817` | Perbaikan layar hitam player + berkas autostart PC backup (`start-video-services.ps1`, `cloudflared-config.example.yml`, README baru, `.gitignore` kredensial tunnel) + tes e2e Tahap 7 | Owner memberi izin 2026-08-20 malam. Dual push SUKSES (`origin` + `dramaku`, selisih nol). Diperiksa dulu: nol secret di diff, 265 tes, tsc 0, build sukses. **Terverifikasi tayang** — teks perbaikan ketemu di bundle produksi |
| ✅ **SELESAI PENUH** | `1ce14c3` | Tahap 7: kode pemulihan password | Terverifikasi 2026-08-20: uji manual owner (tampilan) + uji end-to-end mesin ke API produksi **19/19 lulus**. Akun uji dibersihkan. Tak ada sisa |
| ⚠️ **utang operasional (AKTIF lagi)** | — | `VERCEL_TOKEN` di `start-dramaapp.ps1` kedaluwarsa (403) — **menggigit 2026-08-20 malam**: langkah [5/6] gagal, owner harus tempel alamat manual | **Koreksi catatan sebelumnya:** utang ini gugur **hanya SESUDAH** named tunnel terpasang. Selama masih quick tunnel, tiap restart PC = tempel manual. Prioritaskan Tahap 2 |
| ✅ tayang & terverifikasi | `0d77f4a` | Kartu status sambungan Playly di /admin | Selesai. Kartu kini menampilkan "Belum diatur" |
| ⏸️ menunggu owner | — | 3 env Playly di Vercel (`DASHBOARD_API_URL`, `DASHBOARD_API_KEY_HEADER=X-Playly-Key`, `DASHBOARD_API_KEY`) | Owner isi manual + Redeploy; sesudah itu kartu berubah jadi "Tersambung" |
| ⏸️ menunggu rekan | — | Dashboard Playly masih KOSONG (`count: 0`) | Kunci sudah diuji SAH 2026-08-19; minta rekan upload video contoh |

**Status 2026-08-25 (sesudah rilis gabungan):** lokal = `origin/main` = `dramaku/main` = **`e765e29`**, selisih **NOL** (diverifikasi dengan `git fetch` ke dua remote). Utang dual-push nol.

⚠️ **Peringatan dari catatan rekan (berlaku di KOMPUTER REKAN, bukan di sini):** branch `main` lokal mereka menyimpang (`5c2147b`, berisi commit Playly duplikat & tertinggal 52 commit); rilis Playly mereka dikerjakan dari branch `rilis/playly`. Kalau rekan mau lanjut bekerja, `main` lokal mereka perlu diselaraskan dulu.

**Catatan lama — selisih `dramaku/main` vs `origin/main`:** NOL — lokal, `origin`, dan `dramaku` semuanya di `4954817` (diverifikasi 2026-08-20 malam sesudah dual push). Branch `origin/chore/penjaga-anti-tidur` sudah ter-merge penuh ke `main` (nol commit tertinggal), jadi bukan pekerjaan yang menggantung.

## Cara cek cepat (AI / kamu)

```powershell
git fetch origin
git fetch dramaku
git log --oneline origin/main..dramaku/main
```

- Ada baris di situ = **rekan sudah commit di dramaku, belum di origin** → masukkan ke tabel "Antrian sekarang", lalu tawarkan deploy (izin dulu sebelum `git push origin main`).
- Kosong = tidak ada rilis tertinggal.

Rollback 1-baris: Vercel → project `dramaapp` → Deployments → Promote commit **`954c9ca`** (14 Agustus).

## Riwayat yang sudah lewat antrean

| Tanggal | Commit | Hasil |
|---|---|---|
| 2026-08-15 | `6bb2539` … `8880c5a` dipindah ke origin | Build 1 gagal (font Playfair 404) |
| 2026-08-15 | `820abb8` font Georgia | Build Ready, smoke test lulus; Playly idle (env kosong) |
| 2026-08-18 | `4821c8f`..`0100a66` → `dramaku` | Utang dual-push 6 commit dibayar; kedua repo sama |
| 2026-08-18 | `a36bc67`..`02efb6a` → origin + dramaku | Tahap 4 Performance & SEO dirilis; hasil build Vercel belum diverifikasi |
| 2026-08-18 | `5a51261` → origin + dramaku | Catatan status Tahap 4; ketiga ref sama |
| 2026-08-18 | `d5bb261`..`8602858` → origin + dramaku | Tahap 5 dirilis; hasil build Vercel belum diverifikasi |
| 2026-08-18 | `55e6d8b` → origin + dramaku | Catatan status Tahap 5; ketiga ref sama |
| 2026-08-18 | `82e7536` → origin + dramaku | Tahap 6 dirilis (BREAKING: penonton daftar ulang); build Vercel belum diverifikasi |
| 2026-08-18 | `b48bf32` → origin + dramaku | Catatan status Tahap 6; ketiga ref sama |
| 2026-08-18 | Tahap 6 diverifikasi owner | Build Ready, daftar & login jalan, password salah ditolak, akun bersaldo diklaim |
| 2026-08-18 | `1ce14c3` → origin + dramaku | Tahap 7 dirilis; build Vercel belum diverifikasi |
| 2026-08-19 | — (tanpa commit) | Tahap 7 terverifikasi tayang; tunnel video mati → env `NEXT_PUBLIC_VIDEO_BASE_URL` diupdate MANUAL ke tunnel baru + redeploy; video terbukti jalan (206) |
| 2026-08-19 | `3b97791` → origin + dramaku | Commit catatan yang nyangkut di lokal dibayar; ketiga ref sama. Docs-only (HANDOFF + antrean), nol baris kode aplikasi |
| 2026-08-19 | `2008402` → origin + dramaku | Serah-terima 15 Agt ditandai SUDAH KADALUARSA (instruksi rollback ke `954c9ca` kini berbahaya) |
| 2026-08-19 | `0d77f4a` → origin + dramaku | Kartu status Playly di /admin dirilis; diverifikasi tayang (bundle produksi + 3 kondisi diuji lokal vs API Playly asli) |
| 2026-08-20 | — (tanpa commit) | Tahap 7 diuji end-to-end ke API produksi: **19/19 lulus** (kode sekali-pakai, password lama mati, normalisasi huruf kecil/tanpa strip, pesan gagal seragam anti-penebakan email, rate-limit 5/menit aktif). 1 akun uji dibuat lalu dihapus dari Supabase (verifikasi 0 baris tersisa) |

## Aturan isi (untuk AI)

1. Jangan masukkan secret / API key ke berkas ini.
2. Tiap kali rekan kirim "sudah commit" atau owner minta cek deploy → fetch, isi tabel, sebut 1 kalimat ke owner.
3. Sesudah deploy berhasil: pindahkan baris antrian ke "Riwayat", samakan "Commit yang sedang tayang".
4. Sesudah mengubah berkas ini, perbarui juga tanggal di `HANDOFF.md`.
