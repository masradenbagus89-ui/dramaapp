# Buku pelajaran / indeks lintasAI (dramaapp)

## Handoff & antrean

- [HANDOFF.md](../../HANDOFF.md) — ketik `lanjut dari handoff` di tab baru
- [antrean-deploy.md](../../antrean-deploy.md) — cek commit rekan yang belum di-deploy

## Rencana

- **PELAJARAN 2026-09-26 (alamat `/playly` → `/film` + penanda VIDEO untuk Google).** Dua sisa dari putaran sebelumnya, dikerjakan sekaligus. **(A) Membuang nama penyedia dari LAYAR saja tidak cukup — kotak alamat browser ikut memperlihatkannya.** Halaman daftar dipindah `git mv app/playly app/film`, dan `next.config.ts` mendapat blok `redirects()` PERTAMANYA: `/playly` → `/film` dengan **`permanent: true` (HTTP 308)**. Kenapa 308 dan bukan 307: 308-lah yang memberi tahu Google "halaman ini PINDAH" sehingga nilai pencarian alamat lama ikut berpindah; 307 membuat Google menahan nilainya di alamat lama dan pemindahannya sia-sia. **Konsekuensi yang wajib disebut ke owner SEBELUM rilis (dan sudah): 308 disimpan browser penonton**, jadi membatalkannya tidak seketika. Alamat panel admin & seluruh endpoint API **sengaja tidak ikut** — dijaga tes dari dua arah. **(B) Penanda `VideoObject`** lewat `videoJsonLd()` + `durasiIso8601()` di `lib/structured-data.ts`, ditanam dengan `toJsonLdScript` yang sudah ada (judul video datang dari luar; `JSON.stringify` polos di dalam `<script>` = lubang XSS). **🔴 BATAS JUJUR yang jangan sampai diklaim lebih oleh sesi berikutnya: `uploadDate` dan `contentUrl`/`embedUrl` SENGAJA tidak dikirim** — tanggalnya memang tidak ada di data Playly (`receivedAt` berarti "kapan notifikasi tiba", bukan kapan diunggah), dan alamat berkas videonya bertanda tangan + berumur ~6 jam sehingga sudah mati saat Google mengunjunginya. **Akibatnya Google mengenali halamannya sebagai video, tapi kartu video besar di hasil pencarian belum tentu muncul.** Cara menutupnya nanti tanpa mengarang: catat kapan sebuah video PERTAMA KALI terlihat oleh situs kita — itu memang arti `uploadDate` menurut schema.org ("diunggah ke situs INI"). Bukti: build exit 0 (`/film` tetap `○ Static`, admin & API utuh) · tsc exit 0 · **1107 tes / 76 berkas** · **mutation check 6 arah semuanya MERAH** · dari server hasil build: **`/playly` → 308 → `/film`**, `/film` → 200, sitemap memuat `/film`

- **PELAJARAN 2026-09-26 (tombol FILTER) — "KOMPONENNYA ADA DAN TERUJI" ≠ "ORANGNYA BISA MELIHATNYA".** Owner melapor "dramaku belum ada filter". Tombol FILTER hijau ternyata **sudah dibuat 2026-09-22**, berfungsi sempurna, dan bentuknya sudah persis contoh owner — tapi **cuma terpasang di halaman depan `/`**, sementara `RedirectIfAuthed` (app/components/RedirectIfAuthed.tsx:9) melempar siapa pun yang **sudah login** dari `/` ke `/beranda`, dan `/beranda` nol tab + nol filter. Owner selalu login, jadi selama 4 hari tombol itu **tidak pernah sekali pun terbuka untuknya**. Diukur di produksi sebelum diperbaiki: `/` → `bg-emerald-600` **2×** + deret tab lengkap; `/beranda` → **0 dan 0**. **Aturan yang lahir: sebelum membangun fitur yang "belum ada", cek dulu apakah ia sudah ada di halaman LAIN yang tidak dilihat pelapor.** Pengalihan otomatis seperti `RedirectIfAuthed` membuat satu halaman praktis tak terjangkau sebagian pengguna — apa pun yang hanya dipasang di sana sama saja tidak ada. Perbaikannya: tombolnya diekstrak ke `app/components/beranda/FilterKatalog.tsx` (SATU sumber, dua pemakai) lalu dipasang di ujung kanan strip kuning `/beranda` lewat prop opsional `aksiKanan` di `StripKatalog`. Jebakan tata letak yang ikut ditutup: tombol **tidak boleh** berada di dalam area `overflow-x-auto` milik chip — di layar HP ia terdorong keluar pandangan, bentuk lain dari masalah yang sama. Bukti: build exit 0 (`/beranda` tetap `○ Static`) · tsc exit 0 · **1088 tes / 75 berkas** · **mutation check 4 arah semuanya MERAH**. **✅ TAYANG `8e9d82a`** — diukur di produksi ±2,5 menit sesudah push: `/beranda` menggambar `bg-emerald-600` **1×** + teks "Filter" **1×** di dalam strip kuning, dan rilis sebelumnya utuh (baris "Film Terbaru" masih ada, tautan `/tonton/` masih **40**)

- [2026-09-26-dramaku-isinya-film.md](./rencana/2026-09-26-dramaku-isinya-film.md) — **✅ TAYANG & TERBUKTI (`19e30ee`, dual push tuntas).** Bukti dari situs sungguhan ±2,5 menit sesudah push: `/drama/<id>` nol `href="/playly"` & `<nav>` tinggal satu tautan · `/beranda` menaruh baris "Film Terbaru" di urutan PERTAMA dengan **40 tautan `/tonton/`** · `/tonton/arcadian-1790342663839` **200** · `/tonton/tebakan-11111` **404** (gerbang IDOR bekerja di produksi). **🔴 Sisa: alamat `/playly` masih bocor** lewat tautan "Lihat semua" — tulisannya nol tapi kotak alamat browser masih memperlihatkan kata itu; obatnya rename route ke `/film` + redirect permanen, belum dikerjakan (menunggu owner). **Pelajaran yang ikut terbukti: "belum terbukti di lokal" ≠ "rusak"** — daftar video kosong di `next start` lokal semata karena `.env.local` tak memuat kunci API Playly; di Vercel kuncinya ada dan 40 video langsung tergambar. Jangan memburu bug di kode sebelum memastikan env-nya memang tersedia di lingkungan yang diuji. Owner: "viewer hanya melihat film/video saja". Navbar halaman film dirampingkan (Playly & Profile dilepas; urusan akun pindah ke balik avatar lewat `MenuAkun` baru yang dipakai DUA kepala situs), video Playly naik jadi baris kartu poster biasa di beranda (`lib/beranda-video.ts`), tiap video dapat halaman tonton beralamat sendiri (`/tonton/[id]`), dan kategori per-video bisa diisi admin (dokumen `app_data` `playly:genre` — **nol SQL untuk owner**). **PREMIS OWNER DIKOREKSI DULU: "sesuai genre" MUSTAHIL apa adanya — `PlaylyVideo` (lib/playly.ts:398) tidak punya field genre sama sekali, dan diukur di HTML produksi hari itu NOL dari 46 video punya genre/kaitan drama/rating** (pembandingnya penanda durasi = 45, jadi cara bacanya terbukti benar; angka ini cocok dengan pengakuan kode sendiri di InfoVideoPlayly.tsx:26). Owner memilih jalan "tampil sekarang tanpa genre, kategori menyusul", jadi baris "Film Terbaru" memuat SELURUH video — kalau hanya yang berkategori, ke-46 video hari ini hilang dari beranda. **Tiga pelajaran alat:** (a) **`ƒ (Dynamic)` vs `● (SSG)` ditentukan ADA-TIDAKNYA `generateStaticParams`, bukan isinya** — versi pertama halaman tonton sengaja tidak memakainya (mengira itu = prerender) dan build menandainya `ƒ`, artinya tiap pembukaan halaman menembak API video; memasangnya kembali dengan daftar **kosong** memberi ISR tanpa prerender, persis pola app/drama/[id]. Ukur dari keluaran `npm run build`, jangan dari niat; (b) **tes cocok-teks menangkap KOMENTAR** — tes "tidak memakai generateStaticParams" merah karena komentar berkas itu sendiri menyebut namanya (kekambuhan pelajaran 2026-09-22 pada halaman 404); saring komentar dulu + pasang jaring pengaman untuk penyaringnya; (c) **`npm test` hijau ≠ tipe benar** — 1075 tes lulus sementara `tsc` menemukan 3 berkas tes yang kehilangan field baru `kategori`; gerbang build→tsc→test wajib lengkap. **Batas jujur yang wajib diteruskan: baris "Film Terbaru" & halaman tonton BELUM terbukti tayang** — `.env.local` tidak punya kunci API Playly (kuncinya di Vercel/database produksi), jadi di server hasil build lokal daftar videonya kosong dan barisnya memang tidak tergambar. Yang SUDAH terbukti dari server hasil build: navbar `/drama/<id>` tinggal satu menu ("Beranda", nol `href="/playly"`), dan `/tonton/<alamat asing>` balas **404** (gerbang IDOR). Bukti gerbang: build exit 0 · tsc exit 0 · **1077 tes / 74 berkas** · **mutation check 9 arah semuanya MERAH**

- **PELAJARAN 2026-09-25 (kotak info Playly) — `npm run build` SAAT `npm run dev` HIDUP MEMBUNUH DEV SERVER, dan browser melaporkannya sebagai "Internal Server Error" yang terbaca persis seperti kode rusak.** AI menjalankan gerbang `AGENTS.local.md` §6 (build → tsc → test) untuk memverifikasi pekerjaannya, **tanpa memeriksa dulu apakah owner sedang membuka localhost**. Dev server (PID di port 3000) memakai folder `.next/` yang sama dengan `next build`; build menulis ulang folder itu dan **menghapus berkas yang sedang dipakai dev server**. Server tetap hidup tapi kehilangan isi perutnya → **SETIAP** halaman jadi 500, bukan cuma halaman yang sedang dikerjakan. **Cara mengenalinya dalam 10 detik, jangan berburu di kode:** `(a)` halaman depan `/` ikut 500 padahal tidak disentuh = bukan kode fitur; `(b)` `.next/dev/logs/next-development.log` berisi `ENOENT: no such file or directory ... build-manifest.json` — berkas internal Next yang HILANG, bukan kesalahan sintaks/tipe. **Obatnya: hentikan dev server → `rm -rf .next` → `npm run dev` lagi** (semua isi `.next` dibuat ulang otomatis; tidak ada yang hilang permanen). **Aturan yang lahir: sebelum menjalankan `npm run build`, cek dulu `netstat -ano | grep :3000`.** Ada yang LISTENING = ada dev server hidup; pilih salah satu — minta owner menutupnya dulu, ATAU pakai folder terpisah (`next build --distDir .next-verify`) supaya keduanya tidak berebut. Gerbang §6 menyuruh `rm -rf .next` + build sebagai langkah rutin **tanpa menyebut syarat ini**, jadi jebakannya akan kambuh persis seperti ini kalau tidak diingat. **Pelajaran kedua (ringan tapi memakan satu putaran): `$TMPDIR` KOSONG di Git Bash Windows** — `npm run dev > "$TMPDIR/dev.log"` berubah jadi `> /dev.log` dan mati dengan `Permission denied`, yang terbaca seperti "dev server gagal start" padahal perintahnyalah yang salah; tulis path penuh. **Pelajaran ketiga: tes komponen TIDAK membuktikan komponen itu dipasang** (kekambuhan pelajaran 2026-09-23) — `tests/playly-info-video.test.ts` menguji kotaknya sendirian dan akan tetap hijau walau kotaknya tak pernah dirender; penjaga sambungannya dipisah ke `tests/playly-info-video-sambungan.test.ts` (klik kartu → kotak + 3 tombol tergambar). Total repo sesudah sesi ini: **969 tes / 67 berkas**

- **PELAJARAN 2026-09-24 (verifikasi rilis) — MEMANTAU PRODUKSI DENGAN POLLING OTOMATIS MEMICU "Vercel Security Checkpoint" DAN MEMBLOKIR KOMPUTER SENDIRI.** Sesudah push, AI menunggu tombol muncul lewat loop `curl` **40 permintaan / 20 detik sekali selama 13 menit**. Hasilnya seluruh situs berubah **200 → 403** untuk klien itu — `/`, `/beranda`, `/api/dramas` semuanya 403, isinya halaman `<title>Vercel Security Checkpoint</title>` (verifikasi bot), **bukan** situs mati. Ironinya: alat yang dipakai untuk membuktikan rilis justru menghancurkan kemampuan membuktikannya. **Aturan: verifikasi tayang = beberapa permintaan berjarak menit, JANGAN loop otomatis**; kalau harus menunggu deploy, minta owner membuka browser sungguhan atau melihat dashboard Vercel — sedetik, nol permintaan. **Pelajaran kedua yang ikut tertangkap: katalog produksi (34 judul) ≠ katalog lokal (42).** Judul yang owner tunjuk di tangkapan layar (`over-your-dead-body`) **404 di produksi** — ia dan 7 film lain cuma ada di `data/dramas.json` (berkas cadangan lokal), bukan di Supabase yang menyuplai situs live. Kekambuhan jebakan `AGENTS.local.md` aturan 4. **Akibatnya tangkapan layar owner bisa berasal dari localhost tanpa ada yang menyadarinya** — sebelum memburu "kenapa tidak muncul di produksi", pastikan dulu judulnya memang ADA di produksi lewat `GET /api/dramas`
- **PELAJARAN 2026-09-24 (memasang kembali tombol DOWNLOAD) — `git merge` MENERIMA PENGHAPUSAN TANPA MENANDAINYA SEBAGAI BENTROK, dan itu lolos dari daftar bentrok, dari `git status`, DAN dari `npm test`.** Commit lawan (`2af96b6`) menghapus `DETAIL_DOWNLOAD_EP` + `detailDownloadUrl()` + `downloadFileName()` dari `lib/video.ts`. Sisi kita **tidak menyentuh berkas itu**, jadi git menilai "tak ada yang menentang penghapusan" dan menerimanya **diam-diam** — 3 bentrok dilaporkan (`HANDOFF.md`, `DownloadButton.tsx`, `INDEX.md`), **nol** peringatan soal `lib/video.ts`, padahal `DownloadButton.tsx` mengimpor ketiga nama itu dan situs akan error. **Aturan yang lahir: sesudah merge yang melibatkan berkas TERHAPUS di satu sisi, jangan percaya daftar bentrok sebagai daftar lengkap kerusakan** — baca dulu `git show <commit> --stat` milik sisi lawan, lalu telusuri siapa mengimpor isi berkas yang ia hapus. Bentrok = "dua orang mengubah baris yang sama"; menghapus fungsi yang **dipakai kode pihak lain** bukan bentrok menurut git, tapi tetap kerusakan. **Tiga jebakan alat yang ikut tercatat:** (a) **`worker exited with code: 4294967295` kambuh lagi** — build gagal di percobaan pertama lalu **exit 0** atas kode yang sama persis di percobaan kedua; ukur ulang sebelum berburu penyebab (kekambuhan pelajaran 2026-09-23 siang); (b) **`${PIPESTATUS[0]}` sesudah `echo` membaca status `echo`**, bukan status perintah yang dipipa — sempat melaporkan build `127` palsu padahal `0`; (c) **berkas CSS Next.js 16 ada di `.next/static/chunks/`, bukan `.next/static/css/`** — grep ke folder lama memulangkan kosong dan terbaca persis seperti "kelas Tailwind menguap dari bundel"; ambil lewat HTTP dari server hasil build supaya yang diperiksa benar-benar yang diterima browser. **Pelajaran proses:** kerja 2026-09-23 selesai penuh (build+tsc+950 tes hijau) tapi **tak pernah di-commit** — ia bertahan semalam hanya sebagai working tree, dan bertemu produksi yang sudah maju 10 commit. Kerja yang belum di-commit bukan kerja yang aman; amankan ke commit + branch cadangan **sebelum** menarik apa pun. Penjaga diuji-balik: `DETAIL_DOWNLOAD_EP` → episode berbayar = **2 merah**; `<DownloadButton>` dilepas = **6 merah**. Total repo **951 tes / 65 berkas**
- [2026-09-23-tombol-unduh-melayang.md](./rencana/2026-09-23-tombol-unduh-melayang.md) — **di-commit `af839e1` 2026-09-24, belum di-push.** Tombol DOWNLOAD halaman detail pindah ke **bawah badge Subtitle, di atas SINOPSIS** dan **melayang** (`sticky bottom-20 md:bottom-4`) sampai kolom komentar; ikon **Unduh** baru di rail pemutar `/feed` membuka modal provider yang sama. **Jebakan yang dikunci tes: `position: sticky` SELALU membuat stacking context** — modal yang dipasang DI DALAM elemen sticky/absolute akan tertimbun `BottomNav` z-30 walau angkanya z-50, dan itu cuma kelihatan di HP. Dua premis owner dikoreksi lebih dulu: tombolnya tidak pernah sejajar "Lanjut Nonton", dan halaman detail tidak punya player (video diputar di halaman terpisah `/feed/[id]`). Bukti: build exit 0 · tsc exit 0 · **950 tes hijau** · urutan Subtitle->tombol->Sinopsis diverifikasi di HTML server hasil build `:3099`. Penjaga diuji-balik 4× (masing-masing 1 merah)
- [2026-09-23-modal-provider-unduhan.md](./rencana/2026-09-23-modal-provider-unduhan.md) — **di-commit `af839e1` 2026-09-24, belum di-push.** Tombol DOWNLOAD halaman detail jadi pembuka **modal pilihan provider** (Google Share/Telegram/Cast/Mega); daftar kosong = kembali ke perilaku unduh lama, jadi 42 judul lama tak kehilangan apa pun. Data di `app_data` key `unduhan` (kolom DB masih mustahil — akses DDL tertutup), pintu masuknya kotak isian baru di form admin. Bukti: build exit 0 · tsc exit 0 · **939 tes hijau** · tombol terbukti tayang lewat server hasil build di `:3099`. Penjaga diuji-balik (pagar URL dilemahkan → 5 merah; tombol dilepas dari halaman → 2 merah)
- **PELAJARAN 2026-09-23 (yang paling mahal di sesi ini) — KODE MATI TERBACA SEBAGAI DEPLOY GAGAL.** `DownloadButton` dibuat di `f65ccd6` lengkap dengan helper + 9 tes hijau, tapi **nol halaman mengimpornya** — komponennya tidak pernah tergambar di situs. Sesi 2026-09-22 mengukur "penanda `DOWNLOAD` nol di produksi" lalu menyimpulkan **Vercel belum membangun commit-nya**, dan mengarahkan owner berburu build merah di dashboard. Padahal penanda itu akan tetap nol walau deploy-nya sukses sempurna. Tiga pelajaran: (a) **tes hijau tidak membuktikan fitur tayang** — tes lama menguji `detailDownloadUrl()` & aturan paywall, semuanya benar, tapi tak satu pun bertanya "apakah komponennya dipasang?"; (b) **sebelum menuduh infrastruktur, buktikan dulu kodenya memang memuat fitur itu** — `git log --all -S "<NamaKomponen" -- app/` sedetik, dashboard Vercel butuh owner; (c) **komponen baru wajib punya penjaga PEMASANGAN**, bukan cuma penjaga perilaku — sekarang ada di `tests/unduhan-pemasangan.test.ts` (diuji-balik: melepas `<DownloadButton` dari halaman → 2 tes MERAH)
- **PELAJARAN 2026-09-23 sore (sebar tahun 34 serial, PERUBAHAN DATA tanpa commit) — data produksi punya aturan main yang BEDA TOTAL dari kode, dan tiga pengaman andalan kita semuanya buta terhadapnya.** Owner melihat semua poster serial bertahun `2024` dan minta disebar. Penyebabnya `scripts/isi-lencana-lewat-rest.mjs:33` yang mengisi kolom `year` kosong dengan satu nilai tetap — komentarnya sendiri mengakui nilainya "DARI OWNER, bukan dari sumber teknis mana pun". Karena tahun tinggal di kolom `dramaapp.dramas.year` dan **bukan** di berkas kode, tiga hal berlaku yang tidak berlaku untuk rilis biasa: (a) **tayang sendiri dalam ~60 detik tanpa commit/push/deploy** (`CATALOG_TTL_SECONDS = 60` + `revalidate = 60`); (b) **`git revert` TIDAK bisa memulihkannya** — rollback wajib disiapkan sendiri, di sini berupa berkas cadangan yang ditulis SEBELUM PATCH pertama + mode `--kembalikan`; (c) **`npm test` mustahil memerahkannya** — seluruh tes memakai data buatan sendiri, jadi PATCH yang salah sasaran akan lolos 897 tes dengan mulus. **Pengaman penggantinya: penyaring DUA syarat + baca-ulang.** Sasarannya bukan "semua serial" melainkan "bukan film **DAN** tahunnya masih persis 2024" — satu syarat saja akan menyerempet 7 film yang tahunnya justru tahun rilis SUNGGUHAN dari IMDb; dan buktinya diambil dengan membaca ULANG dari database sesudah PATCH, bukan dari anggapan PATCH-nya berhasil. **🪤 Jebakan verifikasi yang nyaris menyesatkan: API menyegarkan LEBIH DULU daripada halaman.** Sesudah PATCH, `/api/dramas` langsung memulangkan tahun baru sementara `/`, `/beranda`, `/shorts` masih menyajikan HTML lama — tiap halaman statis punya cache ISR sendiri dan baru berganti sesudah ada permintaan melewati 60 detik; `?cb=<angka>` **tidak** menembusnya untuk halaman statis. Sempat terbaca persis seperti "perubahannya tidak masuk". Cara benar: minta halamannya berulang sampai berganti, dan pakai `/api/dramas` sebagai sumber kebenaran. **Pilihan teknis yang layak ditiru: pengacak BER-BENIH, bukan `Math.random()`** — dengan benih tetap, rencana yang dilihat saat simulasi sama persis dengan yang dikirim saat eksekusi; `Math.random()` membuat keduanya berbeda diam-diam sehingga simulasinya tidak membuktikan apa pun. Pembagian berputar saja juga tidak cukup: tanpa diacak, hasilnya pola berulang yang terbaca LEBIH palsu daripada seragam. **❓ Batas kejujuran yang wajib diteruskan:** tahun 2020-2024 yang baru ini BUKAN tahun rilis terverifikasi, sama seperti 2024 yang lama juga bukan — drama pendek China ini tidak punya sumber tahun (sudah dibuktikan 2026-09-22: tidak ada di IMDb). Yang berubah "salah seragam" jadi "salah bervariasi"; jangan ada sesi berikutnya yang mengira angka ini data terverifikasi. Tercatat juga: `data/dramas.json` sengaja TIDAK disentuh sebab ia sudah basi lebih dulu (34 entri bertahun KOSONG, mendahului pengisian massal) — menyamakan satu kolom saja justru membuatnya setengah-benar.
- **PELAJARAN 2026-09-23 siang (hapus chip Premium dari poster, `c954e6f`+`fa328b7`) — MENGHAPUS satu elemen bisa membuat tes TETANGGA-nya jadi VAKUM tanpa satu pun tes berubah merah.** Owner minta tulisan "🪙 Premium" hilang dari poster kartu; penghapusannya sendiri sepele (1 blok JSX di `app/components/Poster.tsx`, menutup semua halaman sekaligus sebab semua kartu memakai komponen yang sama). Yang mahal: tes tetangga *"halaman detail boleh mematikan chip PREMIUM"* memakai stub `premium: true`, dan sesudah chip Premium hilang, **satu-satunya** elemen yang masih dijaga `showBadge` adalah Exclusive dengan syarat `showBadge && !premium && exclusive`. Dengan `premium: true` dan `exclusive` tak pernah diisi stub, **nol elemen tergambar di KEDUA keadaan** → tesnya lulus hijau bahkan kalau prop `showBadge` dihapus total dari komponen. **897 tes hijau di kedua versi**; yang menemukannya audit pra-rilis, bukan `npm test`. **Aturan yang lahir: tes berbentuk "X tidak muncul" WAJIB punya baris pembanding yang membuktikan X MEMANG muncul di keadaan sebaliknya** — tanpa itu, "tidak muncul" tak bisa dibedakan dari "tak pernah ada", dan tesnya mati diam-diam begitu sebab kemunculan X berubah. Ini **kekambuhan** pola "penjaga tes PALSU" yang sudah tercatat 2026-09-22 (rilis ke-4) dan 2026-09-23 pagi (mutation check yang cacat sendiri) — tiga kali berturut-turut di berkas berbeda, jadi perlakukan sebagai cacat SISTEMIK, bukan kelalaian sekali. Dua catatan ikutan: (a) **paywall sengaja TIDAK ikut dimatikan** — `PAYWALL_ENABLED` tetap `true`, yang hilang cuma penandanya di kartu; akibat yang harus diketahui owner: penonton kini baru tahu judul berbayar SETELAH mengklik; (b) **jebakan build Playly menipu lagi** — `worker exited with code: 4294967295` didahului `[playly] katalog publik gagal: kunci API belum dipasang` ternyata gangguan lokal, dibuktikan `git stash` (bersih exit 0) lalu `git stash pop` + build ulang (juga exit 0); exit code yang bertentangan dengan isi log = ukur ulang. Penjaga: tes baru *"kartu drama berbayar TIDAK memajang tulisan Premium di poster"* + tes `showBadge` yang diperbaiki, **mutation check 2 arah keduanya MERAH** = **897 tes / 62 berkas** total repo. Terverifikasi tayang: chip Premium **47 → 0** di HTML produksi.
- [2026-09-23-tombol-unduh-tak-terpasang.md](./rencana/2026-09-23-tombol-unduh-tak-terpasang.md) — **TAYANG (`a8db3ca`).** Tombol Unduh yang "selesai" tapi **tidak pernah terpasang**: `f65ccd6` membawa komponen + fungsi pembantu + 7 tes HIJAU, tapi **nol berkas mengimpornya** sehingga tombolnya tak pernah tergambar. **Pelajaran utama: gerbang pra-rilis memeriksa "kodenya sehat", BUKAN "fiturnya ada"** — `npm test` hijau (tes fungsi MURNI buta terhadap siapa pemanggilnya), `tsc` exit 0 (komponen tak terpakai bukan error), `next build` exit 0 (berkas tak diimpor tidak dikeluhkan), dan catatan serah-terimanya sendiri menyatakan sudah terpasang sehingga pembaca berikutnya punya alasan untuk tidak memeriksa. Empat pelajaran lain: (a) **tes fungsi murni tak bisa membuktikan fiturnya ADA** — tiap komponen baru yang dilihat penonton butuh penjaga yang menuntut halamannya benar-benar memakainya; (b) **klaim di catatan serah-terima = klaim, bukan fakta** — dibantah dalam 5 detik lewat `git diff --name-only`; (c) **rebase bisa menelan suntingan tanpa peringatan** — git tak pernah bilang "berkas yang tadinya kamu ubah sekarang tidak"; (d) **mutation check bisa cacat sendiri** — menukar `<DownloadButton` jadi `<DownloadButtonXX` tetap HIJAU sebab `toContain` masih cocok, sempat terbaca sebagai penjaga palsu; mutasi benar = hapus elemennya utuh. Penjaga: 3 tes baru di `tests/download-button.test.ts` (import UTUH · elemennya dirender · prop `dramaId`+`episodes` dikirim), **uji-balik 3 arah semuanya MERAH** = **896 tes / 62 berkas** total repo
- [2026-09-22-playly-katalog-wajib-kunci.md](./rencana/2026-09-22-playly-katalog-wajib-kunci.md) — **TAYANG (`2fbb8b0`).** Playly menutup `/api/catalog`: sejak 22 Sep 2026 wajib API key — **kunci yang SAMA** dengan `/api/videos`, bukan kunci baru. Diverifikasi langsung, bukan dari surat: tanpa kunci **401 `missing_key`**, dengan kunci kita **200 `total:299`**; `/api/videos`, `/api/public-video`, dan embed terbukti **tidak** berubah. Empat pelajaran: (a) **kegagalannya SENYAP** — `fetchPlaylyVideosKita()` sengaja gagal-aman, jadi katalog yang ditolak cuma jadi daftar KOSONG tanpa error merah, jenis kerusakan yang tak pernah dilaporkan siapa pun; (b) **jaring pengaman bisa mati tanpa pernah dicabut** — jalur katalog dibangun untuk kasus kunci-dicabut (nyata, 2026-08-26), tapi karena katalog kini memakai kunci yang SAMA, kunci mati = kedua jalur mati; jaringnya sudah putus sebelum perbaikan ini, hanya belum kelihatan; (c) **kunci lewat header, bukan query** `?key=` yang juga dilayani Playly — URL ikut tercetak di log server/proxy/`Referer`; (d) **komentar yang berbohong lebih mahal daripada tak ada komentar** — tiga komentar "tanpa kunci" ikut dikoreksi. Penjaga: `tests/playly-katalog-kunci.test.ts` (5 tes) — **diuji-balik**: header dilepas → 2 tes MERAH, dikembalikan → hijau
- **PELAJARAN 2026-09-22 (rilis malam, `2fbb8b0`+`f65ccd6`) — nama remote bisa BERGESER diam-diam, dan `AGENTS.local.md` §5 sudah basi soal itu.** Di komputer ini `origin` kini = `ojokesusu/dramaku` (**produksi**, inilah yang di-deploy Vercel) dan `dramaapp` = `masradenbagus89-ui/dramaapp` (tertinggal 35 commit); remote bernama `dramaku` **tidak ada lagi**. Aturan tertulisnya menyatakan kebalikannya — mengikutinya berarti mendorong rilis ke repo yang tidak di-deploy. **Cara membuktikan tanpa menebak:** ambil penanda teks dari commit yang cuma ada di satu remote (`SERIES UNGGULAN` dari `5d37465`), lalu `curl` situs produksi dan cari penanda itu. Dua pelajaran ikutan: (a) **gerbang §6 tidak berlaku lintas-basis** — cabang kerja tertinggal 32 commit, jadi build/tsc/tes wajib diulang seluruhnya sesudah pindah basis; (b) **perubahan lokal yang belum di-commit bisa diam-diam jadi USANG** — paket 2026-09-18 di working tree ternyata sudah tayang lewat jalan lain, dan meng-commitnya akan **memundurkan** kode yang sudah tayang; periksa dengan `git grep <simbol baru> origin/main` sebelum commit, bukan sesudah
- **PELAJARAN 2026-09-22 (tab katalog LK21, `5d37465`) — "buat seperti situs X" hampir selalu menabrak data yang belum ada.** Owner meminta 6 tab; **3 di antaranya tidak punya data pembedanya** (penanda unggulan 0/41, status tayang 0/41, dan **39 dari 41 judul angka penontonnya identik "1.0K"**). Memaksanya = 6 tombol berisi sama, persis jebakan 2026-09-21. Jalan keluarnya **rantai cadangan**: penanda owner → hitungan otomatis → seluruh serial, sehingga tab TIDAK PERNAH kosong DAN makin akurat begitu owner mengisi penandanya — tanpa ganti kode. **Pola yang patut ditiru:** menjaga halaman tetap STATIS sambil punya tab ber-URL — jangan baca `searchParams` di halaman (itu membuat Next membangun ulang untuk tiap pengunjung); baca di komponen browser, lalu **pisahkan BENTUKNYA ke komponen tanpa hook** supaya markup yang sama dipakai server (isi `<Suspense fallback>`) dan browser. **Cacat yang cuma ketahuan TES RENDER:** baris poster diam-diam menggambar tombol "Lihat semua film unggulan" di dalam bagian tab, berdampingan dengan tombol SEMUA milik tab — tak terlihat saat menulis kodenya. **Label yang dihitung, bukan ditulis tetap:** tab tahun mengambil tahun terbaru dari katalog; menulis "2026" akan berbohong sendiri begitu ada judul 2027. Penjaga: `tests/tab-katalog.test.ts` (20) + `tests/tab-katalog-render.test.ts` (11) = **881 tes / 60 berkas**
- [2026-09-22-lencana-kartu-poster-lk21.md](./rencana/2026-09-22-lencana-kartu-poster-lk21.md) — **SELESAI, TAYANG & TERBUKTI** (`f3d4a28`). **PELAJARAN TERBESAR: jangan simpulkan "tidak ada jalan" setelah SATU jalur gagal.** Sesi ini buntu seharian karena menambah kolom butuh DDL sementara password database sudah tidak berlaku, tak ada Supabase CLI/PAT, dan owner tak punya akses dashboard — lalu owner menegur "cari alternatif lain", dan jalannya memang ada: **`GET /rest/v1/` (spesifikasi OpenAPI PostgREST) memulangkan daftar tabel & fungsi yang boleh disentuh service_role**, dan di situ terlihat tabel **`app_data` bisa DITULIS** (diuji POST 201 / DELETE 204). PostgREST tak bisa mengubah STRUKTUR tapi bisa mengubah ISI — dan itu cukup, sebab rating/tahun/durasi kolomnya (`imdb_rating`,`year`,`runtime`) memang SUDAH ADA; hanya kualitas yang tak punya kolom, jadi disimpan sebagai satu dokumen `app_data`. **Pelajaran kedua: rilis yang "terbukti tayang" bisa tetap merusak yang tak terlihat** — `f4c43a7` tayang mulus di situs publik, tapi selama itu SETIAP penyimpanan drama dari panel admin ditolak `42703` karena `dramaToRow` mengirim kolom yang belum ada; dibuktikan dan diperbaiki di `f3d4a28` (payload baru → HTTP 200, payload lama ber-`quality` → HTTP 400). **Pelajaran ketiga: "merapikan" kode pengaman bisa melepas pengamannya** — menghapus `process.exit(0)` demi menghilangkan pesan galat kosmetik membuat mode SIMULASI ikut menerapkan perubahan; ketahuan sebelum dijalankan. **Pelajaran keempat: menulis `
`/`` lewat heredoc python menghasilkan karakter sungguhan** (newline/backspace) di berkas — terjadi dua kali sehari.** (`add_quality_to_dramas.sql` = struktur, `isi_lencana_awal_dramas.sql` = isi data). **REVISI SORE:** owner melihat hasilnya lalu (a) meminta cadangan "SUB INDO" DIHAPUS dari pojok tahun — sudah dikerjakan, terbukti 68 → 0; (b) membatalkan keputusan "kosongkan dulu" dan memilih isi massal: 34 serial = HD + tahun 2026, film 2026 = CAM, film lain = BluRay; (c) meminta AI yang menjalankan SQL-nya — jalurnya ditemukan (`psycopg2` + pooler port **5432** session mode; port 6543 menolak DDL) dan dibuatkan `scripts/jalankan_sql_dramaapp.py` ber-dry-run, tapi **eksekusinya diblokir pengaman otomatis sesi**, bukan gagal teknis. **Pelajaran tambahan: rating 34 serial TIDAK PUNYA SUMBER APA PUN** — bukan di IMDb, dan `app_data` punya 0 dokumen `rating:*`; satu-satunya jalan jujur adalah owner mengetiknya, bukan AI mengarang.** (kode mengirim kolom `quality` yang belum ada → SEMUA penyimpanan drama dari panel admin gagal; jebakan ke-3 setelah `kind` & `status`). Kartu poster memakai lencana 4 pojok gaya Layarkaca21: ⭐ rating · kualitas video · tahun · durasi `02:45`/`62 EPS`. **Empat pelajaran:** (a) **sebagian "field baru" yang diminta client ternyata SUDAH ADA dengan nama lain** — `rating`=`imdb_rating`, `duration`=`runtime`, `episode_count`=`episodes`; membuat kolom kembar akan menciptakan dua sumber kebenaran, jadi yang benar-benar ditambah cuma `quality`; (b) **fitur tampilan bisa lumpuh oleh form admin, bukan oleh tampilannya** — kolom Tahun/Durasi/Rating selama ini disembunyikan di balik syarat "sudah punya metadata IMDb", sehingga 34 dari 41 judul TIDAK BISA diisi sama sekali dan lencananya akan kosong permanen tanpa ada yang tahu sebabnya; pre-mortem yang menemukannya, bukan pembacaan kode biasa; (c) **batas-kata regex yang ditulis di dalam template literal JS berubah jadi karakter BACKSPACE, bukan batas-kata** — `new RegExp(`\b...`)` tak akan pernah cocok, dan akibatnya pesan error ramah "kolom belum ada" tak pernah muncul (kerusakan senyap: yang terlihat cuma pesan PostgREST mentah). Tulis polanya sebagai string biasa: `new RegExp("\\b" + kolom + "\\b", "i")`. Catatan ini sendiri sempat kena jebakan yang sama saat ditulis; (d) **jebakan `next start` port-terpakai terjadi LAGI hari ini** — server kedua mati `EADDRINUSE` tapi `curl` tetap membalas HTTP 200 dari proses lama; membaca log server dulu yang menyelamatkan verifikasinya. **Isi katalog yang ditemukan (Supabase, bukan `data/dramas.json`):** `year` 7/41 · `runtime` 7/41 · `imdb_rating` 6/41 · `status` **0/41**, dan ketujuhnya SEMUANYA film. **Keputusan owner:** kolom `quality` dibiarkan KOSONG untuk 41 judul lama (menolak isi-massal "HD" supaya tak ada label palsu) + pojok kiri-bawah tetap "SUB INDO" saat tahun kosong. Penjaga: `tests/lencana-kartu.test.ts` (termasuk tes yang MEMBACA berkas SQL-nya dan membandingkan daftar kualitas kode↔database) + `tests/poster-lencana-render.test.ts` (merender `Poster` sungguhan), **mutation check 4 arah semuanya MERAH** = **841 tes / 57 berkas** total repo
- [2026-09-22-navbar-liar-halaman-depan.md](./rencana/2026-09-22-navbar-liar-halaman-depan.md) — **SELESAI & TERBUKTI, belum di-push (menunggu izin rilis owner).** Anomali "halaman depan menampilkan dua baris kepala" yang sejak 2026-09-21 ditandai ❓ akhirnya **direproduksi**: `usePathname()` menerima nilai yang BUKAN `/` saat pra-render alamat akar, `?? "/"` tak menangkapnya (`??` hanya menangkap `null`/`undefined`, **bukan string kosong**), dan penyaringnya berbentuk **denylist** sehingga nilai tak dikenali membuat navbar **MUNCUL** — gagal-terbuka ke arah yang dilihat penonton. Diperbaiki jadi **allowlist** per-segmen di `lib/navigasi-halaman.ts` (BARU, satu sumber kebenaran untuk `TopNav` + `BottomNav`). **Empat pelajaran:** (a) **`??` bukan pengganti `||` untuk nilai "kosong"** — string kosong lolos, dan di guard berbentuk denylist itu langsung jadi kerusakan yang terlihat; (b) **arah gagal sebuah guard lebih penting daripada isi daftarnya** — denylist "selain itu tampilkan" membuat tiap nilai tak terduga jadi bug visual, allowlist membuatnya diam; (c) **sebelum menghitung "komponen X ada di HTML", pastikan penandanya HANYA milik X** — hitungan pertama sesi ini memakai `h-9 w-9 object-contain` dan salah menuduh `/login` + `/daftar` ikut melanggar, padahal kelas itu dipakai header milik halaman itu sendiri (`app/login/page.tsx:89`); (d) **`git checkout --` TIDAK memulihkan berkas BARU yang belum dilacak git** — mutation check putaran pertama sesi ini menumpuk tiga mutasi di `lib/navigasi-halaman.ts` tanpa sadar sehingga hasilnya tidak sah, dan memulihkan berkas yang SUDAH dilacak justru ikut membuang perbaikannya sendiri; pakai cadangan `cp` untuk berkas baru. **Koreksi catatan lama:** kesimpulan 2026-09-21 bahwa menu "tanpa satu pun yang aktif" itu *konsisten dengan* `pathname === "/"` **terbalik** — justru itu bukti `pathname` BUKAN `/`. Penjaga: 8 tes regresi nilai aneh + penyusur `app/**/page.tsx` dari disk (halaman baru tanpa navigasi = MERAH) + jaring pengaman untuk penyusurnya sendiri, **mutation check 6 arah semuanya MERAH** = **786 tes / 54 berkas** total repo
- **PELAJARAN 2026-09-19 — pembungkus error kita sendiri menyalahkan pihak yang salah selama EMPAT sesi, dan `| tail` menyembunyikan build yang gagal.** Catatan 2026-09-15/16/18 semuanya menyimpulkan `npm run build` tertahan karena Supabase mati. **Terbukti tidak.** Build dijalankan **9×** dalam satu sesi, semuanya gagal, sementara Supabase terbukti sehat sempurna. **Bukti paling bersih ada DI DALAM satu build yang gagal**, pada berkas yang sama (`app/drama/[id]/page.tsx`): `generateStaticParams()` → `getAllDramas()` → pembacaan **TANPA cache** **BERHASIL** mengambil 42 judul (pesan `[drama] gagal ambil daftar id saat build` **nol kemunculan**), lalu `getDramaCached()` → pembacaan **LEWAT cache fetch Next** (`revalidate: 60`) **timeout 6 detik** dan membunuh build — kalau databasenya mati, pembacaan pertama juga mati. Diperkuat dari luar: bentuk query **PERSIS** milik halaman drama (`dramas?id=eq.<slug>&select=*&limit=1`) diuji untuk **seluruh 42 judul → 42/42 sukses < 1 detik**, termasuk keempat judul yang membunuh build, plus pemantauan **3 menit tanpa putus → 36/36 sukses** dan build tepat sesudahnya **tetap gagal**. **Aturan yang layak diulang: pesan error buatan sendiri BUKAN diagnosis.** `lib/supabase.ts:186` menyimpulkan *setiap* timeout = "server database sedang bermasalah"; kalimat itu menyesatkan empat sesi, sebab timeout hanya membuktikan **operasi kita** melewati batas waktu — bukan **siapa** yang lambat. Sebelum menyalahkan pihak luar, ukur pihak luar itu **langsung, dengan bentuk permintaan yang persis sama**. **Delapan dugaan yang sudah dimatikan dengan percobaan — jangan diulang:** ❌ Supabase mati (42/42 & 36/36) · ❌ 47 worker build menyerbu DB (**1 worker pun gagal**; jumlah worker bisa dipaksa lewat env `CIRCLE_NODE_TOTAL`, dibaca di `node_modules/next/dist/server/config-shared.js:202`, jadi bisa diuji **tanpa mengubah berkas apa pun**) · ❌ banyak proses serentak (47 **proses node terpisah** → 46/47 sukses 0,2 dtk) · ❌ serbuan katalog penuh (5/15/30 tarikan `select=*` serentak → semua 200, terlambat 0,14 dtk) · ❌ query berat (`select=*` vs `select=id` sama-sama 0,02–0,04 dtk) · ❌ baris tertentu menggembung (3 tersangka **0,7–1,5 KB**) · ❌ batas 6 detik kependekan (dinaikkan 60 detik → **lebih buruk**: worker crash keras) · ❌ disk/antivirus lambat (30× tulis-baca di `.next/cache` → tengah **0,5 ms**). **Dua wajah kegagalan yang sama-sama harus dikenali:** cache fetch **KOSONG** (`rm -rf .next`) → `exit 1` bersih dengan timeout di `getDramaCached` dan **halaman drama BERBEDA tiap percobaan** (= gangguan acak, bukan data rusak); cache **HANGAT** atau batas waktu dinaikkan → crash keras `⨯ Next.js build worker exited with code: 4294967295`, **crash yang sama** yang 2026-09-18 dituduhkan ke database. ❓ Penyebab persisnya **belum terverifikasi**; dugaan terkuat = jalur **cache fetch Next 16 (Turbopack) saat prerender**, sebab hanya pembacaan ber-`revalidate` yang tumbang sementara `no-store` di build yang sama lolos. **Pelajaran kedua, jebakan alat: `npm run build 2>&1 | tail -n 70` melaporkan `exit code 0` padahal build GAGAL `exit 1`** — exit code yang terbaca milik `tail`, bukan `npm`; sempat membuat sesi ini melapor "build lulus" ke owner. Gerbang pra-rilis `AGENTS.local.md` poin 6 bersandar penuh pada exit code, jadi **selalu `> berkas.log 2>&1` lalu baca `$?`, jangan pernah dipipa.** **Pelajaran ketiga: untuk gangguan yang KEDIP, jendela sampel pendek = bukti palsu.** Supabase berayun antara **25/25 sukses 0,02–0,11 dtk** dan **30/30 timeout** dalam hitungan menit, dan kode errornya akhirnya tertangkap karena disampel berulang: **`PGRST002` — "Could not query the database for the schema cache. Retrying."** (PostgREST gagal membaca daftar isi tabel/kolom karena tidak kebagian sambungan database; obatnya **Restart project**, bukan Pause/Delete). Sampel yang layak memutuskan "sudah pulih" harus **berdurasi** (≥3 menit berjeda) dan **100% sukses**, bukan mayoritas — 20 percobaan cepat cuma memotret 12 detik. **Pelajaran keempat: halaman 200 BUKAN bukti database sehat.** Saat `/api/dramas` balas **500 selama 12,5 detik**, halaman `/` `/beranda` `/discover` `/shorts` dan detail drama tetap **200 di bawah 0,5 detik** karena disajikan dari salinan ISR; yang benar-benar terdampak justru halaman yang mengambil data dari browser (`/history`, `/my-list`, `/profile`, `/admin` → **daftar film kosong**). Angka 12,5 detik itu sendiri bukan kebetulan melainkan jatah percobaan aplikasi sendiri: 2 × 6.000 ms + 300 ms = 12,3 dtk (`lib/supabase.ts:109-113`). Hasil sesi ini: berkas `docs/permintaan-restart-supabase.md` (teks siap salin-tempel untuk Kang Dedi, pemilik project `nvblmpkwyzbpdbshyvzw`) — ⚠️ tapi restart itu memperbaiki **produksi**, **bukan** gerbang build; keduanya penyakit berbeda yang selama ini tercampur. **CARA MACETNYA DILEPASKAN (keputusan owner):** prerender halaman drama dilepas — `generateStaticParams()` memulangkan daftar kosong, 42 halaman dibuat saat pengunjung pertama membukanya (ISR 60 detik). **Hasilnya `npm run build` exit 0** dan `/playly` akhirnya tercatat **`○ (Static)` 1m 1y** tanpa `/beranda`/`/discover`/`sitemap.xml` ikut mundur. **Pengaman yang mudah terlewat:** `dynamicParams` WAJIB tetap `true` — dengan daftar kosong, `false` membuat SETIAP halaman drama balas **404 tanpa error apa pun di build** (kerusakan senyap). ⚠️ Ini **menghindari** masalahnya, **bukan menyembuhkannya**: penyebab persis di dalam Next tetap ❓. Rinciannya: [2026-09-19-lepas-prerender-halaman-drama.md](./rencana/2026-09-19-lepas-prerender-halaman-drama.md)  — jangan simpulkan "database mati" dari SATU jalur.** Dua sesi berturut-turut (2026-09-16 dan 2026-09-18) menyimpulkan "PostgreSQL tidak menjawab" padahal databasenya **hidup**; yang tersendat cuma **PostgREST + Auth**. Yang membongkarnya: menguji **empat layanan berbeda dengan kunci yang sama** — `GET /storage/v1/bucket` balas **200 / 1,48 dtk** dengan baris ASLI dari `storage.buckets`, sementara `GET /rest/v1/<tabel>` dan `GET /auth/v1/admin/users` timeout 25 detik, dan `OPTIONS /rest/v1/<tabel>` (tak menyentuh tabel) balas **200 / 0,36 dtk**. Aturan yang layak diulang: **satu endpoint timeout hanya membuktikan endpoint ITU bermasalah** — sebelum menuduh database, ketuk layanan lain di project yang sama (Storage & Auth punya jalur koneksi sendiri), dan bandingkan permintaan yang MENYENTUH tabel dengan yang tidak. Empat dugaan yang gugur karena uji ini: project di-pause · kunci tak sah · jaringan lokal · disk penuh. Pelajaran kedua: **dashboard yang "Healthy" bisa menipu kalau project-nya salah** — owner membuka `iicrzdnmcpontfytfypi` (project PENSIUN sejak 2026-08-29), tandanya terbaca jelas di layar (**0 Total Requests · No migrations · No backups**); project produksi adalah `nvblmpkwyzbpdbshyvzw` dan **milik Kang Dedi, bukan owner**, sehingga tombol Restart di luar jangkauan owner. Pelajaran ketiga: DNS `db.<ref>.supabase.co` **ENOTFOUND bukan bukti project mati** — pooler `aws-0-ap-northeast-1.pooler.supabase.com` tetap tersambung 74 ms.
- **PELAJARAN 2026-09-18 — jangan simpulkan "database mati" dari SATU jalur.** Dua sesi berturut-turut (2026-09-16 dan 2026-09-18) menyimpulkan "PostgreSQL tidak menjawab" padahal databasenya **hidup**; yang tersendat cuma **PostgREST + Auth**. Yang membongkarnya: menguji **empat layanan berbeda dengan kunci yang sama** — `GET /storage/v1/bucket` balas **200 / 1,48 dtk** dengan baris ASLI dari `storage.buckets`, sementara `GET /rest/v1/<tabel>` dan `GET /auth/v1/admin/users` timeout 25 detik, dan `OPTIONS /rest/v1/<tabel>` (tak menyentuh tabel) balas **200 / 0,36 dtk**. Aturan yang layak diulang: **satu endpoint timeout hanya membuktikan endpoint ITU bermasalah** — sebelum menuduh database, ketuk layanan lain di project yang sama (Storage & Auth punya jalur koneksi sendiri), dan bandingkan permintaan yang MENYENTUH tabel dengan yang tidak. Empat dugaan yang gugur karena uji ini: project di-pause · kunci tak sah · jaringan lokal · disk penuh. Pelajaran kedua: **dashboard yang "Healthy" bisa menipu kalau project-nya salah** — owner membuka `iicrzdnmcpontfytfypi` (project PENSIUN sejak 2026-08-29), tandanya terbaca jelas di layar (**0 Total Requests · No migrations · No backups**); project produksi adalah `nvblmpkwyzbpdbshyvzw` dan **milik Kang Dedi, bukan owner**, sehingga tombol Restart di luar jangkauan owner. Pelajaran ketiga: DNS `db.<ref>.supabase.co` **ENOTFOUND bukan bukti project mati** — pooler `aws-0-ap-northeast-1.pooler.supabase.com` tetap tersambung 74 ms.
- [2026-09-21-strip-katalog-lk21.md](./rencana/2026-09-21-strip-katalog-lk21.md) — **SELESAI & TERBUKTI, belum di-push (menunggu izin rilis owner).** Strip kuning di `/`, `/beranda`, `/discover` naik dari 12 chip jadi **29 chip dalam 5 kelompok** (genre kategori | genre sinema | negara | tahun | urutan), meniru bentuk Layarkaca21 atas permintaan owner. Tiga hal baru: penyaring **`?genre=`** untuk kolom `genre` OMDb (sebelumnya kolom itu tersimpan tapi TIDAK bisa disaring sama sekali — itulah sebabnya HORROR & SCI-FI yang diminta owner belum bisa diklik), **`lib/negara.ts`** yang menerjemahkan label negara ke bahasa Indonesia **tanpa menyentuh alamat URL**-nya, dan strip yang **membungkus** di layar lebar (digeser di HP). **Bug yang ikut ditutup:** `/discover` masih memakai daftar tetap `CATEGORIES`, sehingga chip **`Fantasy` (0 judul)** tergambar di halaman katalog utama dan diklik = halaman hampa — aturan "jangan gambar pilihan kosong" sudah dipegang sejak 2026-09-07 tapi halaman itu terlewat 11 hari. **Yang SENGAJA tidak dibangun karena datanya nol:** ANIME · INDIA · JEPANG · KOREA · THAILAND (0 judul) dan **BLURAY** (DramaKu tidak menyimpan kualitas video sama sekali) — chip-nya akan muncul sendiri begitu owner menambah judulnya. **Dua pelajaran alat:** (a) **`next start` yang GAGAL karena port terpakai tetap membalas HTTP 200 dari server LAIN**, dan HTML-nya berisi kode LAMA — nyaris jadi kesimpulan "perubahan tidak masuk"; selalu baca log server-nya dulu; (b) **`/discover` tidak merender apa pun di server** (dibungkus `<Suspense>` karena `useSearchParams`), jadi memeriksa chip-nya lewat `curl` selalu memulangkan "tidak ada" dan itu BUKAN bug. **🔄 REVISI SORE HARI YANG SAMA — owner memangkas strip 29 chip → 14.** Owner menilai versi pagi "terlalu banyak tulisan dan negara lainnya" dan menyerahkan daftarnya sendiri, jadi isi strip berhenti dihitung dari katalog: `catalogShortcuts()` **dihapus**, diganti konstanta **`STRIP_KATALOG`** (daftar TETAP). **Keputusan owner MENANG atas aturan "hanya gambar pilihan yang ada isinya"** — konsekuensinya ditulis terbuka di komentar kodenya, bukan disembunyikan: Anime/India/Jepang/Korea/Thailand masih 0 judul, tapi chip-nya **hidup sendiri** begitu owner menambah judulnya, dan daftar lengkapnya tetap ada di menu dropdown. **BLURAY ditanyakan lewat popup** (3 pilihan + biaya yang harus dikerjakan owner sendiri) → owner memilih **lewati**, jadi nol tombol mati permanen. `GenreStrip` **di-rename `StripKatalog`** dan dua mode-nya (tombol saring-di-tempat + tautan) disederhanakan jadi satu: semua chip tautan — sebab daftar tetap ini mencampur `?cat=`/`?genre=`/`?negara=`/`?year=` sementara /beranda cuma menyimpan genre kategori, jadi mempertahankan mode lama membuat chip CINA & SCI-FI **diam saja kalau diklik**. **Dua pelajaran tambahan:** (a) **isi menu dropdown Radix TIDAK ADA di HTML sampai menunya dibuka**, jadi tes render yang memeriksa "tiap alamat di HTML ≥1 judul" selama ini **tidak pernah benar-benar menguji menu** — ketahuan justru saat strip dipangkas; (b) **aturan penjaga harus ikut berubah saat keputusannya berubah** — "tiap chip wajib ≥1 judul" jadi SALAH, penggantinya "tiap chip wajib benar-benar MENYARING", sebab nama parameter salah tulis diabaikan diam-diam dan chip-nya lalu menampilkan SELURUH katalog seolah bekerja (lebih menyesatkan daripada kosong). Bukti revisi: build exit 0 · tsc exit 0 · **711 tes / 53 berkas** · **mutation check 7 arah semuanya MERAH** · `next start` menggambar **14 chip persis**. Penjaga: `tests/strip-katalog.test.ts` (**BARU**, merender `DramaBrowser` & `PublicTopBars` sungguhan lalu menjalankan tiap alamat yang tertulis di HTML) + 13 kasus baru di `tests/nav-katalog.test.ts` & `tests/discover-filter.test.ts`, **mutation check 6 arah semuanya MERAH**, dan **58 tautan diuji ke katalog produksi nyata → 0 yang hampa** = **712 tes / 53 berkas** total repo
- [2026-09-19-lepas-prerender-halaman-drama.md](./rencana/2026-09-19-lepas-prerender-halaman-drama.md) — **SELESAI & TERBUKTI, belum di-push (menunggu izin rilis owner).** Melepaskan gerbang rilis yang macet 4 hari: `generateStaticParams()` halaman drama memulangkan **daftar kosong**, sehingga 42 halaman dibuat saat pengunjung pertama membukanya (ISR 60 detik) alih-alih saat build. **Akhirnya `npm run build` exit 0 dan `/playly` tercatat `○ (Static)` 1m 1y** — bukti yang dicari sejak 2026-09-15 — sementara `/beranda`, `/discover`, `/`, `/shorts`, `sitemap.xml` **tetap `○`** (nol kemunduran). Temuan utamanya: **penahannya BUKAN Supabase** (rinciannya di PELAJARAN 2026-09-19 di atas). Pengaman yang mudah terlewat: `export const dynamicParams = true` ditulis **eksplisit**, sebab dengan daftar kosong nilai `false` membuat **SETIAP** halaman drama balas **404 tanpa error apa pun di build**. Harganya jujur: pengunjung **pertama** tiap judul menunggu **0,32 detik** (terukur dari `next start`: kunjungan pertama `MISS` 0,32 dtk → ke-2 & ke-3 `HIT` 0,01 dtk, `s-maxage=60`). Penjaga: `tests/drama-prerender-build.test.ts` (**BARU**, 6 tes, **mutation check 4 arah semuanya MERAH**) = **693 tes / 52 berkas** total repo
- [2026-09-18-playly-static-dan-gabungan.md](./rencana/2026-09-18-playly-static-dan-gabungan.md) — **BELUM TAYANG — selesai dikode & teruji, TERTAHAN karena Supabase mati saat gerbang build.** `/playly` dikembalikan jadi **static**, dan video gabungan (katalog-jemput + webhook-dorong) akhirnya ikut tampil di `/beranda` + `/discover`. Empat pelajaran: (a) **urutan nomor 1 vs nomor 2 ternyata TIDAK bebas dipilih** — memasang daftar gabungan ke dua halaman itu DULUAN akan menyeret keduanya ikut jadi dynamic, karena `getPlaylyVideosGabungan()` membawa pembacaan `no-store` di dalamnya; "perbaikan kecil" yang justru memundurkan dua halaman paling ramai; (b) **tes hijau tidak bisa menangkap jenis bug ini sama sekali** — kedua jalur mengembalikan daftar yang sama persis, bedanya cuma opsi `revalidate` yang dioper ke Supabase, jadi bug static→dynamic 2026-09-15 LOLOS seluruh 653 tes; penjaganya harus menanyakan **jalur mana yang dibaca** (penghitung panggilan per jalur) dan **isi berkas sumbernya**, bukan isi daftarnya; (c) **pengaman tidak ikut dipercepat** — gerbang IDOR `/api/playly/video` sengaja TETAP memakai daftar segar, sebab daftar izin yang boleh basi = video yang baru disembunyikan admin masih bisa ditonton sampai cache habis; (d) **crash `Next.js build worker exited with code: 4294967295 / 3221226505` bisa berarti DATABASE MATI, bukan kode cacat** — selalu didahului `[drama] gagal ambil daftar id saat build: TimeoutError`, dan dibuktikan dengan `git stash` lalu build atas kode lama: crash sama persis. Bukti produksi hari itu yang membenarkan seluruh pekerjaan ini: saat Supabase mati, `/beranda` & `/discover` (static) balas **200 dalam 0,7-0,9 detik** sementara `/playly` (dynamic) balas **200 tapi 13,11 detik**. Penjaga: `tests/playly-halaman-cached.test.ts` (BARU, 12 tes) + 5 tes tambahan di `tests/playly-gabungan.test.ts`, terbukti lewat **mutation check 4 arah** = **671 tes / 50 berkas** total repo
- [2026-09-16 serah-terima rekan: gabung video Playly + login tahan database mati](../serah-terima/2026-09-16-login-tahan-database-mati.md) — **kerja rekan, di-merge & tayang owner 2026-09-16 (`5e6cbec`)**. Dua commit sekaligus: `/playly` menggabungkan katalog-jemput + webhook-dorong jadi satu daftar (`lib/playly-gabungan.ts` BARU), dan `lib/supabase.ts` berhenti memuntahkan HTML Cloudflare mentah ke browser saat database mati (+ batas waktu 6 detik, + percobaan ulang **hanya untuk BACA**). Berkas pendampingnya: `docs/serah-terima/2026-09-15-gabung-video-playly.md`. Empat pelajaran: (a) **klaim "halaman tetap static" WAJIB dibuktikan dari keluaran `npm run build`, bukan dari membaca `export const revalidate` di berkas halamannya** — berkas serah-terima rekan §5 menulis "`/playly` tetap static + revalidate 5m" padahal build mendaftarnya **ƒ (Dynamic)** dan produksi membalas `Cache-Control: no-store` + `X-Vercel-Cache: MISS`; sebabnya `getPlaylyWebhookVideos()` (`lib/store.ts:1029-1034`) memanggil `sbDocGet` **tanpa `revalidate`** → jatuh ke default `no-store` (`lib/supabase.ts:214`), dan **satu fetch `no-store` membatalkan status static SELURUH halaman** (peringatan ini sudah tertulis di `lib/supabase.ts:204` tapi terlewat); (b) perbaikannya **bukan** menambah `revalidate` di fungsi itu — ia juga dibaca jalur TULIS (`upsertPlaylyWebhookVideo` membaca-lalu-menulis) dan data basi di sana bisa **menimpa video lain**; pola benar = varian terpisah ber-akhiran `Cached`, seperti pasangan `getPlaylyHiddenIds` ↔ `getPlaylyHiddenIdsCached` (`lib/store.ts:934-943`); (c) menambah sumber data ke sebuah halaman **wajib menyusuri gerbang otorisasinya juga** — gerbang IDOR `/api/playly/video` dulu hanya mengenal katalog mitra, jadi tanpa ikut disesuaikan video webhook akan **TAMPIL di halaman tapi membalas 404 saat diklik**, rusak yang cuma ketahuan kalau ada yang mengkliknya; (d) **percobaan ulang (retry) hanya boleh untuk operasi BACA** — timeout tak pernah bisa memastikan server sudah terlanjur mengerjakan tulisan atau belum, jadi mengulang `coin_add` berisiko koin penonton bertambah dua kali. Penjaga: `tests/playly-gabungan.test.ts` (18) + `tests/supabase-tahan-gangguan.test.ts` (8) + `tests/login-database-mati.test.ts` (4) + `tests/supabase-setelan.test.ts` = **653 tes / 49 berkas** total repo
- [2026-09-15-beranda-baris-kategori-lk21.md](./rencana/2026-09-15-beranda-baris-kategori-lk21.md) — **BELUM TAYANG (menunggu izin rilis owner).** Susunan film `/beranda` berhenti jadi satu grid panjang ber-paginasi; menggantikannya **baris kategori gaya Layarkaca21** berposter kecil yang digeser ke samping (permintaan owner + screenshot berkotak merah). Grid + nomor halaman **tidak dibuang** — muncul begitu penonton mencari/menyaring, persis cara LK21 bekerja. Tiga pelajaran: (a) `filterAktif` lama **tidak menghitung `sort`**, jadi percabangan yang memakainya apa adanya membuat dropdown Urutkan terlihat rusak (ganti urutan → layar tak berubah) — ditutup fungsi murni `sedangMenyaring()`; (b) katalog di bawah `ROW_MIN_ITEMS` menghasilkan **nol baris**, tanpa jaring pengaman `tampilGrid` tengah halaman jadi kosong melompong **tanpa error apa pun**; (c) `FeaturedRow` membawa pembungkus `shell-wide … px-4` sendiri → menaruhnya di dalam `SHELL` menggandakan jarak tepi. Pola barisnya **dipakai ulang** dari halaman depan (`homeCatalogRows`), bukan dibangun baru; `GRID_CLASS` & `ROW_CARD_CLASS` sengaja tak disentuh sehingga `/` dan `/discover` tidak berubah. (d) **urutan gerbang pra-rilis di `AGENTS.local.md` poin 6 menghasilkan 4 error PALSU** — `rm -rf .next` lalu langsung `npx tsc --noEmit` selalu melapor `TS2304: Cannot find name 'PageProps'` di 3 berkas route, sebab `next-env.d.ts` mengimpor `./.next/types/routes.d.ts` yang baru dibuat `next build`; urutan benar = `rm -rf .next` → `npm run build` → `tsc` → `npm test` (sesudah build: exit 0 atas kode yang sama). Penjaga: `tests/beranda-bentuk-halaman.test.ts` (3 tes, merender komponen sungguhan) + `sedangMenyaring` di `tests/beranda-catalog.test.ts` (6 tes)
- [2026-09-14-webhook-playly.md](./rencana/2026-09-14-webhook-playly.md) — **kerja rekan, di-merge & tayang owner 2026-09-15 (`b7459a4`)**. Endpoint `POST /api/webhooks/playly` supaya Playly bisa MENDORONG "ada video baru / video ditarik" seketika, bukan menunggu tarikan 5-menitan. **Dirilis dalam keadaan TIDUR atas keputusan owner:** `PLAYLY_WEBHOOK_SECRET` sengaja dibiarkan kosong → endpoint membalas **503** dan tak memproses apa pun, karena sampai hari ini **nol bukti Playly punya fitur kirim webhook** (nama header & bentuk payload semuanya dari spesifikasi owner, bukan dokumentasi Playly). Data yang masuk juga **belum tersambung** ke `/playly` maupun `/discover` — `getPublishedPlaylyWebhookVideos()` terbukti **nol pemanggil** di luar tes; menyambungkannya menyentuh 3 pemanggil `getPlaylyVideosPublik()` dan DITUNDA atas keputusan owner. Empat pelajaran mahal: (a) `readLocal()` di `lib/store.ts` mengembalikan objek fallback yang **DIPAKAI BERSAMA** padahal pemanggilnya rutin memutasinya (`file.embeds = ...`, `data.wallets[email] = ...`) → isi satu operasi bocor ke operasi lain, **senyap tanpa error**; diperbaiki di akar dengan `structuredClone` sehingga `admins`/`comments`/`wallets`/`playly` ikut tertutup sekaligus; (b) `embed_code` itu **HTML dari luar** — yang disimpan hanya `src`-nya setelah lolos `isAllowedPlaylyEmbedUrl()`, karena menyimpan HTML mentah = mengizinkan pengirim menaruh `<script>` di situs kita (XSS); (c) `crypto.timingSafeEqual` **MELEMPAR ERROR** kalau panjang dua buffer beda — itu sebabnya kunci di-hash SHA-256 dulu (hasilnya selalu 32 byte) alih-alih memeriksa panjangnya, sebab memeriksa panjang justru membocorkan berapa huruf rahasia kita; (d) sebelum memindahkan kerja yang belum di-commit ke branch lain, jalankan `git log <branch>..origin/main -- <berkas>` untuk TIAP berkas (0 = aman dipindah, >0 = berkasnya sudah bergerak). **Anti-replay BELUM tertutup** — payload tak membawa `event_id`/`sent_at`, jadi permintaan sah yang pernah terekam bisa dikirim ulang orang lain; tak berbahaya selama kuncinya kosong, tapi **WAJIB ditutup sebelum kunci diisi**. Penjaga: `tests/playly-webhook.test.ts` (44) + `tests/playly-webhook-route.test.ts` (30) + `tests/playly-webhook-gagal.test.ts` (8) = **82 tes**
- [2026-09-10-header-menu-lk21.md](./rencana/2026-09-10-header-menu-lk21.md) — header gaya Layarkaca21: satu baris logo + cari + **6 menu dropdown** (Genre · Jenis · Populer · Negara · Tahun · Lainnya) + strip kuning berpintasan, dipasang di /, /beranda, /discover. Tiga pelajaran mahal: (a) **isi katalog WAJIB dibaca lewat schema `dramaapp`** — query ke schema `public` menjawab "tabel tidak ada" (PGRST205) dan sempat membuat saya menyimpulkan `year`/`country` kosong padahal terisi; paling aman lewat `GET /api/dramas`; (b) **`rm -rf .next` sebelum build** saat memverifikasi tampilan — build inkremental menyajikan halaman statis LAMA sehingga perubahan terlihat "tidak masuk"; (c) tes yang menyalin ulang pemetaan URL tidak membuktikan apa-apa → `bacaFilter`/`filterOptions`/`filterDariUrl` di `lib/discover.ts` dipakai BERSAMA halaman & tes. Penjaga: `tests/nav-katalog.test.ts` (tiap pilihan menu wajib memulangkan ≥1 judul)
- [2026-09-09-menu-player-playly.md](./rencana/2026-09-09-menu-player-playly.md) — halaman /playly berhenti memakai `<iframe>` Playly, video diputar **pemutar DramaKu sendiri** supaya menu titik tiga bisa diisi sendiri (PIP · Pencahayaan Sinematik · Kecepatan berfungsi). Pelajaran yang mahal kalau diulang: `variants` & `subtitles` Playly **kosong di 9 dari 9 video** (jadi 360p-1080p & subtitle memang tidak ada — jangan dikarang), dan mp4-nya di R2 **tanpa header CORS** sehingga Volume Stabil & Penguat Suara mustahil tanpa CORS/proxy. Endpoint barunya **hanya mengirim alamat, bukan byte video** — patuh pelajaran kuota Vercel. Penjaga: `tests/playly-video-route.test.ts` + `tests/player-menu.test.ts`
- [2026-09-07-beranda-lk21.md](./rencana/2026-09-07-beranda-lk21.md) — Beranda dirombak jadi gaya situs streaming klasik (bar cari magenta, strip genre, **grid poster padat + paginasi**) digabung hero sinematik & baris personal. Pelajaran pre-mortem: badge & filter HANYA dari field yang benar-benar terisi — katalog tak punya kualitas video, jadi badge HD/CAM SENGAJA tidak dibangun (memasangnya = berbohong ke penonton). **⚠️ KOREKSI 2026-09-10: bagian "katalog tak punya `year`/`country`" pada catatan ini KELIRU** — keduanya terisi di 8 dari 42 judul; kekeliruannya karena isi katalog dibaca dari `data/dramas.json` (cadangan lokal, kosong) alih-alih Supabase schema `dramaapp`. Menu Negara & Tahun akhirnya DIBANGUN, lihat 2026-09-10
- [2026-09-02-banner-iklan-pas-badan.md](./rencana/2026-09-02-banner-iklan-pas-badan.md) — banner iklan beranda: gambar kecil di tengah dikelilingi smear blur → **kotak menyusut mengikuti bentuk gambar** (nol blur, tak pernah terpotong). Berisi pelajaran penting: `max-w-full` pengaman HILANG diam-diam saat digabung `cn()` dengan `className` pemanggil yang membawa `max-w-*` sendiri → bingkai membludak keluar layar HP; obatnya pisahkan dua batas ke dua elemen. Plus catatan geser-samping `/drama/[id]` yang sudah ada sebelumnya
- [2026-08-29-playly-video-tanpa-berkas.md](./rencana/2026-08-29-playly-video-tanpa-berkas.md) — **kerja rekan (Yusuf), di-merge & tayang owner 2026-09-11 (`c3312f2`)**. Video Playly yang CATATANNYA ada tapi BERKASNYA tidak (upload putus di tengah) tak lagi ditampilkan ke penonton — dulu penonton dapat layar hitam "Video belum tersedia" dari dalam iframe Playly. `fetchPlaylyThumbnail` **berganti nama** jadi `fetchPlaylyDetailPublik` (sekalian membaca status berkas, tanpa panggilan tambahan). Tiga pelajaran: (a) **hanya `false` yang menyembunyikan, `null` = "tidak tahu" tetap ditampilkan** — kalau Playly mati/berubah bentuk JSON, satu gangguan jaringan jangan sampai mengosongkan seluruh `/playly`; (b) endpoint cek-berkas `/api/public-video` (`lib/playly.ts:51`) **publik tanpa kunci** dan **sama persis** dengan yang dipakai pemutar (`:1045`) → aturan "video mana yang boleh tampil" bisa diuji dari komputer mana pun tanpa kredensial; (c) sebelum merilis, uji pakai **fungsi asli** terhadap video yang SUNGGUHAN sedang tayang — hasilnya 2026-09-11: **19 lolos · 1 dibuang · 0 "tidak tahu"**, dan yang dibuang terbukti balas HTTP 502 di produksi. Penjaga: `tests/playly-publik.test.ts` (33 tes) + `tests/playly-video-grid.test.ts` (8 tes, mengunci kartu berisi tahun+genre bukan uploader)
- [2026-08-26-vercel-paused-teaser-proxy.md](./rencana/2026-08-26-vercel-paused-teaser-proxy.md) — **situs mati TOTAL karena kuota Vercel jebol**: `/api/teaser` menyalurkan byte video lewat server (Fast Origin Transfer 29,71 GB dari jatah 10 GB) padahal video ada di PC backup. Diperbaiki jadi 307 redirect. Berisi temuan `TEASER_BYTES` yang dideklarasikan tapi tak pernah dipakai, alasan cache TIDAK boleh dipanjangkan (alamat tunnel berganti), + penjaga `tests/teaser-redirect.test.ts`
- [2026-08-25-tambah-film-tanpa-episode.md](./rencana/2026-08-25-tambah-film-tanpa-episode.md) — jenis tayangan Serial vs **Film** (1 video utuh, tanpa episode) di panel admin: kolom DB `kind`, film dipaksa 1 video & gratis di server, JSON-LD `Movie`. Berisi alasan film tak bisa berbayar tanpa mengubah `FREE_EPISODES` + urutan wajib "SQL dulu, deploy belakangan"
- [2026-08-26-playly-video-otomatis.md](./rencana/2026-08-26-playly-video-otomatis.md) — video Playly tampil OTOMATIS (halaman /playly + baris Discover): 3 akar (kaitan cuma di berkas lokal ber-.gitignore, kegagalan senyap `return null`, kunci sah tersimpan di nama env yang salah) + jalur katalog-tersaring supaya kunci yang dicabut tak lagi mematikan video
- [2026-08-25-playly-video-tidak-muncul.md](./rencana/2026-08-25-playly-video-tidak-muncul.md) — video Playly tak pernah muncul: 4 lapis penyebab (pola embed salah, embedUrl relatif, kunci ditolak, gerbang domain Playly)
- [2026-08-22-alamat-video-runtime.md](./rencana/2026-08-22-alamat-video-runtime.md) — video berhenti mati tiap PC backup restart TANPA named tunnel: alamat pindah dari "dibakar saat build" ke `app_data` Supabase + PC backup lapor sendiri. Berisi alasan ngrok gugur (1 GB/bulan) & pre-mortem secret level User vs Machine
- [2026-08-20-video-nama-berkas-1mp4.md](./rencana/2026-08-20-video-nama-berkas-1mp4.md) — drama baru tak bisa diputar padahal berkasnya ada: nama berkas bukan `1.mp4`; 3 bug hardlink-agent (angka "4" dari `.mp4`, berkas tanpa nomor dilewati, nol hardlink dibalas sukses) + penjaga `tests/hardlink-agent.test.ts`
- [2026-08-20-video-otomatis-tanpa-powershell.md](./rencana/2026-08-20-video-otomatis-tanpa-powershell.md) — alamat video permanen (named tunnel) + autostart PC backup; berisi 2 pelajaran bug: layar hitam `/sample.mp4` & `.ps1` non-ASCII gagal parse di PowerShell 5.1
- [2026-08-18-tahap-7-kode-pemulihan.md](./rencana/2026-08-18-tahap-7-kode-pemulihan.md) — kode pemulihan password penonton, tanpa email/domain
- [2026-08-18-tahap-6-login-penonton.md](./rencana/2026-08-18-tahap-6-login-penonton.md) — tutup IDOR jalur koin: identitas penonton pindah ke cookie terverifikasi
- [2026-08-18-tahap-5-rating-share.md](./rencana/2026-08-18-tahap-5-rating-share.md) — rating penonton (1 email = 1 suara, TIDAK dikirim ke Google) + share + balasan komentar
- [2026-08-18-performance-seo.md](./rencana/2026-08-18-performance-seo.md) — metadata unik per halaman, sitemap/robots, cache ISR 60 detik tanpa menyentuh jalur koin
- [2026-08-16-hero-trailer-hidup.md](./rencana/2026-08-16-hero-trailer-hidup.md) — hero trailer hidup di Beranda + Discover (lokal :3055)
- [2026-08-16-redesign-streaming-tahap-1.md](./rencana/2026-08-16-redesign-streaming-tahap-1.md) — redesign homepage/detail/player/riwayat tanpa ganti skema Supabase
- [2026-08-15-deploy-jalur-video-api.md](./rencana/2026-08-15-deploy-jalur-video-api.md) — pindahkan rilis video API luar dari dramaku ke origin lalu deploy Vercel
- [2026-08-15-admin-tetap-viewer.md](./rencana/2026-08-15-admin-tetap-viewer.md) — admin baru tetap tampil VIEWER karena sesi penonton lama
- [2026-08-15-riwayat-penonton.md](./rencana/2026-08-15-riwayat-penonton.md) — halaman Riwayat Penonton (Watch History) di `/history`
- [2026-08-14-imdb-metadata-json.md](./rencana/2026-08-14-imdb-metadata-json.md) — generate JSON metadata dari IMDb ID (OMDb + banner opsional TMDB)
- [2026-08-13-imdb-detail-drama.md](./rencana/2026-08-13-imdb-detail-drama.md) — detail film dari ID IMDb (OMDb), simpan metadata, tampil di halaman drama
